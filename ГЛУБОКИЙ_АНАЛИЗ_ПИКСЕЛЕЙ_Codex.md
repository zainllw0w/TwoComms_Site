# 🔬 ГЛУБОКИЙ АНАЛИЗ ПИКСЕЛЕЙ (Codex, 2025‑02‑02)

**Цель:** сопоставить выводы из `ГЛУБОКИЙ_АНАЛИЗ_ПИКСЕЛЕЙ_С_ПРЕДПОЛОЖЕНИЯМИ.md` и текущего состояния кода, выдвинуть гипотезы о причинах низкого покрытия событий и сформировать вопросы/контрольный список для разработчика, который будет вносить изменения.

---

## 🧭 Где мы сходимся с прошлым анализом
- **Отсутствие `#checkout-payload` в корзине.** Данные для ивентов готовятся в `twocomms/storefront/views/cart.py:253`–293, но в `twocomms/twocomms_django_theme/templates/pages/cart.html` нет соответствующего контейнера. Функция `getCheckoutAnalyticsPayload()` (`twocomms/twocomms_django_theme/static/js/main.js:358`) всегда возвращает `null`, поэтому `InitiateCheckout` и `StartPayment` не уходят — это полностью совпадает с гипотезой из предыдущего отчёта.
- **Зависимость фронта от `payment_status`.** Скрипт успеха (`twocomms/twocomms_django_theme/templates/pages/order_success.html:1760`+) отправляет Lead/Purchase только при `prepaid/paid`, что было отмечено раньше. Мы подтвердили, что любое отклонение статуса блокирует события.

---

## 🔍 Новые наблюдения и гипотезы

### 1. Обновление статусов Monobank Checkout
- В `twocomms/storefront/views.py:7061`–7067` реализованы заглушки `_fetch_and_apply_checkout_status` и `_update_order_from_checkout_result`. Если используется checkout-режим (не invoice), статусы заказа не меняются, `payment_status` остаётся `checking`, и ни браузерные, ни серверные события не отправляются.
- **Что проверить:** используется ли именно checkout-флоу в конкретном кейсе с предоплатой; приходят ли webhooks на `/payments/monobank/webhook/`; есть ли записи в логах об вызовах `_record_monobank_status`.
- **Варианты развития:** если команда собирается активировать checkout, придётся реализовать эти функции или переключить трафик обратно на invoice API.

### 2. Статус для Monobank Pay и соответствие `pay_type`
- Для MonoPay (`twocomms/storefront/views/monobank.py:295`+) статусы зависят от поля `order.pay_type`. Если на этапе создания заказа оно осталось `online_full`, `_record_monobank_status` (`twocomms/storefront/views/utils.py:335`+) переводит заказ в `paid`, а не `prepaid`.
- **Что проверить:** какое значение реально попадает в payload `pay_type`. Валидация на фронте (`twocomms/twocomms_django_theme/static/js/main.js:688`–757) логгирует выбранное значение, можно посмотреть в консоли. Имеет смысл сравнить с фактическими заказами в БД, чтобы исключить несоответствия.
- **Дополнительная гипотеза:** если Monobank возвращает промежуточный `processing`, а успешный статус приходит позже webhook'ом, важно, чтобы webhook точно отработал. В логах (`monobank_logger`) должно быть видно смену `payment_status`.

### 3. Серверные события Facebook (Coverage ≈ 20 %)
- **SDK и окружение.** `facebook_business` не входит в `twocomms/requirements.txt` (строки 1–66), а `FacebookConversionsService` (`twocomms/orders/facebook_conversions_service.py:41`–92) без него отключается. Если на проде нет установленного пакета или не заданы переменные `FACEBOOK_CONVERSIONS_API_TOKEN` / `FACEBOOK_PIXEL_ID`, Conversions API просто не работает. Стоит проверить логи на наличие сообщений `facebook-business SDK not installed` или `Facebook Conversions API не настроен`.
- **Дедупликация и user data.** Для MonoPay в `order.payment_payload` попадают только `request/create/history` (`twocomms/storefront/views/monobank.py:694`–703). `tracking` (fbp/fbc/external_id) туда не записывается, поэтому серверные события отправляются с минимальным набором данных. Это снижает качество матчей и coverage. Возможно, достаточно дополнительно складывать `tracking_context` при создании заказа (сценарий аналогичен `twocomms/storefront/views/checkout.py:318`–323).
- **Advanced Matching в браузере.** `order_success` скрипт хеширует данные и кладёт их в `__meta`. Но `sessionStorage` помечается вне зависимости от успеха `trackEvent` (`twocomms/twocomms_django_theme/templates/pages/order_success.html:2065`–2134). Если пиксели не успели прогрузиться или включён блокировщик, событие помечается как отправленное и повторно не пойдёт — coverage падает. Нужно решить, приемлемо ли это поведение или стоит откладывать `sessionStorage.setItem` до подтверждения отправки.

### 4. Клиентские события TikTok и Meta
- `analytics-loader.js` (`twocomms/twocomms_django_theme/static/js/analytics-loader.js`) буферизует события до готовности пикселей. В логах браузера можно посмотреть, отрабатывает ли `win._fbqLoaded` / `win._ttqLoaded`. Если видим постоянные сообщения вида `Pixel not detected`, это признак того, что скрипт не может пробиться через CSP/блокировщик.
- Для TikTok Conversions API аналогичная проверка: включены ли переменные `TIKTOK_EVENTS_ACCESS_TOKEN` и `TIKTOK_EVENTS_PIXEL_CODE`, нет ли ошибок инициализации в логах сервера (`orders/tiktok_events_service.py`).

---

## ❓ Вопросы для команды
- Какой фактический путь оплаты был у лида на 200 ₴ (MonoPay invoice или Checkout)? От этого зависит, какие ветки кода нужно чинить в первую очередь.
- Установлены ли на продакшене `facebook-business` и токен CAPI? Видели ли в логах успешные сообщения `✅ Facebook Conversions API initialized successfully`?
- Есть ли события `Lead`/`Purchase` в истории Monobank webhook логов за тот день? Если их нет, почему? Была ли проверена подпись `X-Sign`?
- Видели ли в консоли браузера пользователей сообщения `console.warn('[Analytics] ⚠ Meta Pixel not detected')` / `console.debug('Lead tracking error…')`?

---

## ✅ Что перепроверить при изменениях
- После добавления `#checkout-payload` в корзину — убедиться, что `getCheckoutAnalyticsPayload()` возвращает актуальные данные и `InitiateCheckout` фиксируется в Pixel Helper.
- После доработки обработки Monobank — проверить, что `order.payment_status` переходит в `prepaid` до рендера `order_success`, а `payment_payload.facebook_events.lead_sent` записывается.
- Если будут правки в `order_success` скрипте — не забыть про дедупликацию `event_id` (метод `Order.get_facebook_event_id()` в `twocomms/orders/models.py:115`+) и синхронизацию с серверными событиями.
- При настройке Conversions API — мониторить coverage в Events Manager и убедиться, что нет новых ошибок дедупликации (`event_id`, `external_id`, `fbp/fbc`).

---

**План действий для разработчика** строить только после ответов на вопросы выше — тогда будет понятно, какая ветка (checkout, invoice, CAPI) требует первоочередного внимания.
