# 🔬 ГЛУБОКИЙ АНАЛИЗ ПИКСЕЛЕЙ С ПРЕДПОЛОЖЕНИЯМИ О ПРОБЛЕМАХ

**Дата анализа:** 2025-01-30  
**Методология:** Context7 + Последовательное мышление + Перепроверка  
**Цель:** Выявить предположения о причинах отсутствия событий в пикселях

---

## 📋 СОДЕРЖАНИЕ

1. [Полная карта покупок](#полная-карта-покупок)
2. [Анализ каждого события](#анализ-каждого-события)
3. [Предположения о проблемах](#предположения-о-проблемах)
4. [Мысли о синхронизации](#мысли-о-синхронизации)
5. [Мысли о загрузке пикселей](#мысли-о-загрузке-пикселей)

---

## 🗺️ ПОЛНАЯ КАРТА ПОКУПОК

### Этап 1: Просмотр товара → ViewContent

**Путь:** Пользователь открывает страницу товара (`/product/<slug>/`)

**Код отправки:**
```827:839:twocomms/twocomms_django_theme/templates/pages/product_detail.html
window.trackEvent('ViewContent', {
    content_ids: [selection.offerId],
    content_name: title,
    content_type: 'product',
    content_category: category,
    value: price,
    currency: 'UAH',
    contents: [{
      id: selection.offerId,
      quantity: 1,
      item_price: price
    }]
});
```

**Условия:**
- Элемент `#product-analytics-payload` должен быть на странице
- `window.trackEvent` должна быть определена (загружается через `analytics-loader.js`)
- Задержка 200ms через `setTimeout` для готовности DOM

**💭 МОИ МЫСЛИ:**
- ViewContent отправляется с задержкой 200ms - может быть race condition если пиксели еще не загрузились
- Если `window.trackEvent` не определена, событие просто не отправится (try-catch игнорирует ошибку)
- `selection.offerId` зависит от выбранного размера - если размер не выбран, может быть default

---

### Этап 2: Смена цвета/размера → CustomizeProduct

**Путь:** Пользователь выбирает цвет или размер товара

**Код отправки:**
```786:802:twocomms/twocomms_django_theme/templates/pages/product_detail.html
document.querySelectorAll('.color-swatch').forEach(function(button){
  button.addEventListener('click', function(){
    updateCurrentOfferId();
    
    // Отправляем CustomizeProduct с правильным offer_id
    try{
      if(window.trackEvent){
        var selection = getCurrentSelection();
        window.trackEvent('CustomizeProduct', { 
          content_ids: [selection.offerId], 
          content_type: 'product',
          variant_id: selection.colorVariantId
        });
      }
    }catch(_){ }
  });
});
```

**💭 МОИ МЫСЛИ:**
- CustomizeProduct отправляется только при смене цвета, НЕ при смене размера
- Событие отправляется без `value` и `currency` - может быть неполным для Facebook/TikTok
- Если пользователь не меняет цвет, это событие не отправится вообще

---

### Этап 3: Добавление в корзину → AddToCart

**Путь:** Пользователь нажимает "Добавить в корзину"

**Код отправки:**
```1448:1518:twocomms/twocomms_django_theme/static/js/main.js
// Унифицированный трекинг AddToCart с offer_id
try{
  if(window.trackEvent){
    var analyticsItem = (d && d.item) ? d.item : null;
    var productContainer = document.getElementById('product-detail-container');
    var productAnalyticsEl = document.getElementById('product-analytics-payload');
    var offerId = analyticsItem && analyticsItem.offer_id;
    if(!offerId){
      offerId = productContainer && productContainer.getAttribute('data-current-offer-id');
    }
    if(!offerId){
      offerId = 'TC-' + productId + '-default-S';
    }

    // ... вычисление цены, количества, названия ...

    window.trackEvent('AddToCart', payload);
  }
}catch(_){ }
```

**💭 МОИ МЫСЛИ:**
- AddToCart отправляется только ПОСЛЕ успешного ответа сервера (`d.ok`)
- Если сервер вернул ошибку, событие не отправится
- `offer_id` может быть fallback `'TC-{id}-default-S'` если не найден в ответе
- Событие отправляется с полными данными (contents, value, currency) - это хорошо

---

### Этап 4: Страница корзины → InitiateCheckout

**Путь:** Пользователь переходит в корзину и нажимает кнопку оплаты

**Код отправки:**
```658:673:twocomms/twocomms_django_theme/static/js/main.js
const analytics = getCheckoutAnalyticsPayload();
if(analytics){
  try{
    if(window.trackEvent){
      window.trackEvent('InitiateCheckout', {
        value: analytics.value,
        currency: analytics.currency,
        num_items: analytics.num_items,
        payment_method: 'monobank',
        content_ids: analytics.content_ids,
        contents: analytics.contents
      });
    }
  }catch(_){ }
}
```

**Функция получения данных:**
```358:377:twocomms/twocomms_django_theme/static/js/main.js
function getCheckoutAnalyticsPayload(){
  const el = document.getElementById('checkout-payload');
  if(!el) return null;
  let contents = [];
  let ids = [];
  const rawContents = el.dataset.contents ? decodeURIComponent(el.dataset.contents) : '[]';
  const rawIds = el.dataset.ids ? decodeURIComponent(el.dataset.ids) : '[]';
  try { contents = JSON.parse(rawContents || '[]'); } catch(_){ contents = []; }
  try { ids = JSON.parse(rawIds || '[]'); } catch(_){ ids = []; }
  const value = parseFloat(el.dataset.value || '0');
  const currency = el.dataset.currency || 'UAH';
  let numItems = parseInt(el.dataset.numItems || '0', 10);
  if(Number.isNaN(numItems) || numItems <= 0)
```

**💭 КРИТИЧЕСКАЯ МЫСЛЬ:**
- **Элемент `#checkout-payload` ОТСУТСТВУЕТ в шаблоне `cart.html`!**
- Данные готовятся в `cart.py:253-292` (`checkout_payload`, `checkout_payload_ids`, `checkout_payload_contents`)
- Но элемент не рендерится в шаблоне - это означает что `getCheckoutAnalyticsPayload()` всегда вернет `null`
- **InitiateCheckout НЕ МОЖЕТ отправиться если элемент отсутствует!**

**Проверка:**
- В `cart.html` нет `<div id="checkout-payload" ...>`
- Данные передаются в контекст, но не используются в шаблоне

---

### Этап 5: Начало оплаты → StartPayment

**Путь:** Пользователь нажимает кнопку оплаты и открывается Monobank

**Код отправки:**
```612:624:twocomms/twocomms_django_theme/static/js/main.js
const analytics = getCheckoutAnalyticsPayload();
try{
  if(window.trackEvent && analytics){
    window.trackEvent('StartPayment', {
      value: analytics.value,
      currency: analytics.currency,
      num_items: analytics.num_items,
      payment_method: 'monobank',
      content_ids: analytics.content_ids,
      contents: analytics.contents
    });
  }
}catch(_){ }
```

**💭 МОИ МЫСЛИ:**
- StartPayment также зависит от `#checkout-payload`
- Если элемент отсутствует, событие не отправится
- Это кастомное событие, не стандартное для Facebook/TikTok

---

### Этап 6: Оплата → Webhook → payment_status

**Путь:** Monobank отправляет webhook на сервер

**Логика установки статуса:**
```367:378:twocomms/storefront/views/utils.py
if status in MONOBANK_SUCCESS_STATUSES:
    previous_status = order.payment_status
    
    # НОВАЯ ЛОГИКА (30.10.2024):
    # - prepay_200 → payment_status = 'prepaid' + Lead event
    # - online_full → payment_status = 'paid' + Purchase event
    if order.pay_type == 'prepay_200':
        order.payment_status = 'prepaid'
        monobank_logger.info(f'✅ Order {order.order_number}: prepayment successful → payment_status=prepaid')
    else:
        order.payment_status = 'paid'
        monobank_logger.info(f'✅ Order {order.order_number}: full payment successful → payment_status=paid')
```

**💭 МОИ МЫСЛИ:**
- Статус устанавливается ТОЛЬКО если `order.pay_type == 'prepay_200'`
- Если `pay_type` не установлен или установлен неправильно, статус не изменится
- Webhook может не прийти или прийти с ошибкой
- Если `pay_type` установлен в `'online_full'` вместо `'prepay_200'`, статус будет `'paid'` вместо `'prepaid'`

---

### Этап 7: Страница успеха → Lead/Purchase

**Путь:** Пользователь перенаправляется на `/orders/success/<order_id>/`

**Логика отправки Lead:**
```2070:2135:twocomms/twocomms_django_theme/templates/pages/order_success.html
// Lead событие - только для prepaid (предоплата)
if (shouldSendLead && !sessionStorage.getItem(leadStorageKey)) {
  // paymentStatus === 'prepaid'
  // ... отправка Lead события
}
```

**Условия:**
- `shouldSendLead = paymentStatus === 'prepaid'`
- Элемент `#purchase-payload` должен быть на странице
- `data-payment-status` должен быть `'prepaid'`

**💭 МОИ МЫСЛИ:**
- Если `payment_status` не установлен в `'prepaid'` на сервере, событие не отправится
- Если элемент `#purchase-payload` отсутствует, скрипт выйдет раньше (строка 1802)
- Если `data-prepaid-value` не заполнен, используется fallback 200 грн (строка 1850-1852)
- `sessionStorage` защищает от дублей, но может предотвратить повторную отправку если событие не дошло

---

## 🔍 АНАЛИЗ КАЖДОГО СОБЫТИЯ

### 1. ViewContent

**Что должно быть:**
- Отправляется при загрузке страницы товара
- Содержит `content_ids`, `value`, `currency`, `contents`

**Что может быть не так:**
- Пиксели могут не успеть загрузиться за 200ms
- `window.trackEvent` может быть не определена если `analytics-loader.js` не загрузился
- Элемент `#product-analytics-payload` может отсутствовать

**💭 МОЯ МЫСЛЬ:**
ViewContent может отправляться, но если пиксели еще не загрузились, событие буферизуется. Нужно проверить что буфер обрабатывается после загрузки пикселей.

---

### 2. CustomizeProduct

**Что должно быть:**
- Отправляется при смене цвета товара
- Содержит `content_ids`, `variant_id`

**Что может быть не так:**
- Отправляется только при смене цвета, НЕ при смене размера
- Не содержит `value` и `currency` - может быть неполным
- Если пользователь не меняет цвет, событие не отправится

**💭 МОЯ МЫСЛЬ:**
CustomizeProduct может быть не критичным для конверсий, но отсутствие его может влиять на качество данных для ремаркетинга.

---

### 3. AddToCart

**Что должно быть:**
- Отправляется после успешного добавления в корзину
- Содержит полные данные: `content_ids`, `value`, `currency`, `contents`

**Что может быть не так:**
- Отправляется только если сервер вернул `ok: true`
- Если сервер вернул ошибку, событие не отправится
- `offer_id` может быть fallback значением если не найден

**💭 МОЯ МЫСЛЬ:**
AddToCart, вероятно, работает правильно, так как пользователь видит "добавления в корзину" в пикселях. Проблема скорее в других событиях.

---

### 4. InitiateCheckout

**Что должно быть:**
- Отправляется при клике на кнопку оплаты
- Требует элемент `#checkout-payload`

**💭 КРИТИЧЕСКАЯ МЫСЛЬ:**
- **Элемент `#checkout-payload` ОТСУТСТВУЕТ в `cart.html`!**
- Данные готовятся в `cart.py`, но не рендерятся в шаблоне
- `getCheckoutAnalyticsPayload()` всегда вернет `null`
- **InitiateCheckout НЕ МОЖЕТ отправиться!**

**Проверка синхронизации:**
- Данные готовятся: `checkout_payload`, `checkout_payload_ids`, `checkout_payload_contents`
- Но элемент не создается в шаблоне
- Это критическая проблема синхронизации между бэкендом и фронтендом

---

### 5. StartPayment

**Что должно быть:**
- Отправляется при открытии Monobank
- Тоже зависит от `#checkout-payload`

**💭 МОЯ МЫСЛЬ:**
StartPayment также не может отправиться из-за отсутствия `#checkout-payload`. Это кастомное событие, не стандартное для Facebook/TikTok.

---

### 6. Lead (предоплата 200 грн)

**Что должно быть:**
- Отправляется в браузере на `order_success.html` если `payment_status='prepaid'`
- Отправляется на сервере через Conversions API

**Логика отправки в браузере:**
```1814:1815:twocomms/twocomms_django_theme/templates/pages/order_success.html
var shouldSendLead = paymentStatus === 'prepaid';  // ТОЛЬКО предоплата
var shouldSendPurchase = paymentStatus === 'paid';  // ТОЛЬКО полная оплата
```

**Логика отправки на сервере:**
```403:422:twocomms/storefront/views/utils.py
if order.payment_status == 'prepaid':
    # Предоплата → Lead event
    # Проверяем, не было ли уже отправлено
    payment_payload = order.payment_payload or {}
    facebook_events = payment_payload.get('facebook_events', {})
    if not facebook_events.get('lead_sent', False):
        success = fb_service.send_lead_event(order)
```

**💭 МОИ МЫСЛИ:**
- Lead отправляется ТОЛЬКО если `payment_status='prepaid'`
- Если webhook не пришел или обработался с ошибкой, статус не изменится
- Если `pay_type` не установлен в `'prepay_200'`, статус не будет `'prepaid'`
- Элемент `#purchase-payload` должен быть на странице с правильными данными

**Проверка синхронизации event_id:**
- Браузер: `event_id = '{{ order.get_facebook_event_id }}_lead'`
- Сервер: `event_id = f"{order.get_facebook_event_id()}_lead"`
- Метод `get_facebook_event_id()` возвращает `"{order_number}_{timestamp}"`
- Если timestamp отличается, event_id не синхронизируется

---

### 7. Purchase (полная оплата)

**Что должно быть:**
- Отправляется в браузере на `order_success.html` если `payment_status='paid'`
- Отправляется на сервере через Conversions API

**💭 МОИ МЫСЛИ:**
- Purchase отправляется ТОЛЬКО если `payment_status='paid'`
- Для предзаказов на 200 грн это событие НЕ должно отправляться (только Lead)
- Если `payment_status` установлен неправильно, могут быть проблемы

---

## 💭 ПРЕДПОЛОЖЕНИЯ О ПРОБЛЕМАХ

### Предположение 1: Элемент #checkout-payload отсутствует

**Что я думаю:**
- Данные готовятся в `cart.py:253-292` (`checkout_payload`, `checkout_payload_ids`, `checkout_payload_contents`)
- Но элемент не рендерится в шаблоне `cart.html`
- `getCheckoutAnalyticsPayload()` всегда вернет `null`
- InitiateCheckout и StartPayment не могут отправиться

**Почему это может быть проблемой:**
- InitiateCheckout - важное событие для Facebook/TikTok
- Без него пиксели не видят начало оформления заказа
- Это может влиять на оптимизацию рекламы

**Что нужно проверить:**
- Есть ли элемент `#checkout-payload` в `cart.html`?
- Если нет, то почему данные готовятся но не используются?

---

### Предположение 2: payment_status не устанавливается в 'prepaid'

**Что я думаю:**
- Статус устанавливается ТОЛЬКО если `order.pay_type == 'prepay_200'`
- Если `pay_type` не установлен или установлен неправильно, статус не изменится
- Webhook может не прийти или прийти с ошибкой
- Если webhook обработался с ошибкой, статус может остаться `'checking'` или `'unpaid'`

**Почему это может быть проблемой:**
- Lead событие отправляется ТОЛЬКО если `payment_status='prepaid'`
- Если статус не установлен, Lead не отправится
- Браузерное событие не отправится (проверка `shouldSendLead`)
- Серверное событие не отправится (проверка `if order.payment_status == 'prepaid'`)

**Что нужно проверить:**
- Установлен ли `pay_type='prepay_200'` для предзаказа?
- Пришел ли webhook от Monobank?
- Установился ли `payment_status='prepaid'` после webhook?

---

### Предположение 3: Элемент #purchase-payload отсутствует или не содержит данных

**Что я думаю:**
- Элемент должен быть на странице `order_success.html`
- Должен содержать `data-payment-status`, `data-prepaid-value`, `data-order-id`
- Если элемент отсутствует, скрипт выйдет раньше (строка 1802)
- Если данные не заполнены, могут быть проблемы

**Почему это может быть проблемой:**
- Lead/Purchase события не могут отправиться если элемент отсутствует
- Если `data-payment-status` не `'prepaid'`, Lead не отправится
- Если `data-prepaid-value` не заполнен, используется fallback 200 грн

**Что нужно проверить:**
- Есть ли элемент `#purchase-payload` в `order_success.html`?
- Заполнены ли все необходимые `data-*` атрибуты?
- Правильно ли передается `payment_status` в шаблон?

---

### Предположение 4: Проблемы с синхронизацией event_id

**Что я думаю:**
- Браузер: `event_id = '{{ order.get_facebook_event_id }}_lead'`
- Сервер: `event_id = f"{order.get_facebook_event_id()}_lead"`
- Метод `get_facebook_event_id()` возвращает `"{order_number}_{timestamp}"`
- Если timestamp отличается (например, из-за кэширования шаблона), event_id не синхронизируется

**Почему это может быть проблемой:**
- Facebook/TikTok дедуплицируют события по `event_id`
- Если event_id не совпадает, события могут дублироваться
- Если event_id не уникален, события могут не дедуплицироваться правильно

**Что нужно проверить:**
- Совпадает ли `event_id` между браузером и сервером?
- Генерируется ли `event_id` одинаково в обоих местах?
- Используется ли один и тот же метод `get_facebook_event_id()`?

---

### Предположение 5: Проблемы с синхронизацией external_id

**Что я думаю:**
- Браузер: `external_id` генерируется из `userIdAttr`, `sessionKey`, `orderNumber`
- Сервер: `external_id` генерируется из `order.user_id`, `order.session_key`, `order.order_number`
- Логика генерации должна совпадать, но может отличаться

**Код в браузере:**
```1875:1891:twocomms/twocomms_django_theme/templates/pages/order_success.html
var externalSource = '';
if (userIdAttr) {
  externalSource = 'user:' + userIdAttr;
} else if (sessionKey) {
  externalSource = 'session:' + sessionKey;
} else if (orderNumber) {
  externalSource = 'order:' + orderNumber;
} else if (orderId) {
  externalSource = 'order:' + orderId;
}
```

**Код на сервере:**
```160:178:twocomms/orders/facebook_conversions_service.py
external_source = tracking_data.get('external_id')
if not external_source:
    # Fallback: генерируем external_id если не передан из checkout
    if order.user_id:
        external_source = f"user:{order.user_id}"
    elif order.session_key:
        external_source = f"session:{order.session_key}"
    elif order.order_number:
        external_source = f"order:{order.order_number}"
```

**Почему это может быть проблемой:**
- Если `external_id` не совпадает, события могут не дедуплицироваться
- Facebook/TikTok используют `external_id` для связи событий
- Если логика отличается, могут быть проблемы

**Что нужно проверить:**
- Совпадает ли `external_id` между браузером и сервером?
- Используется ли одна и та же логика генерации?
- Передается ли `external_id` из checkout в `payment_payload`?

---

### Предположение 6: Пиксели не загружаются или блокируются

**Что я думаю:**
- Facebook Pixel загружается через `analytics-loader.js`
- TikTok Pixel загружается через `analytics-loader.js`
- Ad blockers могут блокировать загрузку пикселей
- Скрипты могут не загрузиться из-за ошибок сети

**Проверка загрузки:**
```1094:1114:twocomms/twocomms_django_theme/static/js/analytics-loader.js
setTimeout(function() {
  if (win.fbq && typeof win.fbq === 'function') {
  if (console && console.log) {
      console.log('[Analytics] ✓ Meta Pixel initialized');
    }
  } else {
    if (console && console.warn) {
      console.warn('[Analytics] ⚠ Meta Pixel not detected');
  }
  }
  
  if (win.ttq && typeof win.ttq.track === 'function') {
    if (console && console.log) {
      console.log('[Analytics] ✓ TikTok Pixel initialized');
    }
  } else {
    if (console && console.warn) {
      console.warn('[Analytics] ⚠ TikTok Pixel not detected');
    }
  }
}, 1000);
```

**Почему это может быть проблемой:**
- Если пиксели не загрузились, события не отправятся
- События буферизуются, но если пиксели не загрузились, они не отправятся
- Ad blockers могут блокировать пиксели

**Что нужно проверить:**
- Загружаются ли пиксели в браузере?
- Видны ли они в консоли как `[Analytics] ✓ Meta Pixel initialized`?
- Блокируются ли они ad blockers?

---

### Предположение 7: Проблемы с буферизацией событий

**Что я думаю:**
- События буферизуются если пиксели не загрузились
- Буфер обрабатывается после загрузки пикселей
- Но если пиксели не загрузились, буфер не обработается

**Код буферизации:**
```161:205:twocomms/twocomms_django_theme/static/js/analytics-loader.js
// Buffer Meta Pixel events until loaded
// Защита от блокировки: проверяем что Pixel действительно доступен
try {
  if (PIXEL_ID && win.fbq && typeof win.fbq === 'function') {
    // Проверяем что Pixel инициализирован
    if (win._fbqLoaded) {
      // ... отправка события
    } else if (PIXEL_ID) {
      // Pixel еще не загружен - буферизуем
      if (!win._fbqBuffer) {
        win._fbqBuffer = [];
      }
    win._fbqBuffer.push({ event: eventName, data: fbPayload });
    }
  }
}
```

**Почему это может быть проблемой:**
- Если пиксели не загрузились, события остаются в буфере
- Буфер не обрабатывается если пиксели не загрузились
- События теряются если пиксели не загрузились

**Что нужно проверить:**
- Обрабатывается ли буфер после загрузки пикселей?
- Есть ли таймаут для обработки буфера?
- Что происходит если пиксели не загрузились?

---

### Предположение 8: Проблемы с Conversions API

**Что я думаю:**
- Conversions API отправляет события на сервере
- Но может быть не настроен или отключен
- Может быть ошибка при отправке

**Проверка настроек:**
```41:57:twocomms/orders/facebook_conversions_service.py
self.access_token = getattr(settings, 'FACEBOOK_CONVERSIONS_API_TOKEN', None)
self.pixel_id = getattr(settings, 'FACEBOOK_PIXEL_ID', None)
self.test_event_code = getattr(settings, 'FACEBOOK_CAPI_TEST_EVENT_CODE', None)

# Проверяем наличие обязательных настроек
if not self.access_token or not self.pixel_id:
    logger.error(
        "❌ Facebook Conversions API не настроен! "
        "Необходимо установить FACEBOOK_CONVERSIONS_API_TOKEN и FACEBOOK_PIXEL_ID в ENV. "
        f"Access Token: {'установлен' if self.access_token else 'НЕ установлен'}, "
        f"Pixel ID: {'установлен' if self.pixel_id else 'НЕ установлен'}"
    )
    self.enabled = False
```

**Почему это может быть проблемой:**
- Если Conversions API не настроен, серверные события не отправятся
- Если Access Token неверный, события не отправятся
- Если Pixel ID неверный, события не отправятся

**Что нужно проверить:**
- Настроен ли Conversions API на сервере?
- Установлены ли `FACEBOOK_CONVERSIONS_API_TOKEN` и `FACEBOOK_PIXEL_ID`?
- Работает ли Conversions API (нет ли ошибок в логах)?

---

### Предположение 9: Проблемы с TikTok Events API

**Что я думаю:**
- TikTok Events API отправляет события на сервере
- Но может быть не настроен или отключен
- Может быть ошибка при отправке

**Проверка настроек:**
```31:45:twocomms/orders/tiktok_events_service.py
self.access_token = getattr(settings, 'TIKTOK_EVENTS_ACCESS_TOKEN', None)
self.pixel_code = getattr(settings, 'TIKTOK_EVENTS_PIXEL_CODE', None)
self.test_event_code = getattr(settings, 'TIKTOK_EVENTS_TEST_EVENT_CODE', None)

if not self.access_token or not self.pixel_code:
    logger.error(
        "❌ TikTok Events API не настроен! "
        "Необходимо установить TIKTOK_EVENTS_ACCESS_TOKEN и TIKTOK_EVENTS_PIXEL_CODE в ENV. "
        f"Access Token: {'установлен' if self.access_token else 'НЕ установлен'}, "
        f"Pixel Code: {'установлен' if self.pixel_code else 'НЕ установлен'}"
    )
    self.enabled = False
```

**Почему это может быть проблемой:**
- Если TikTok Events API не настроен, серверные события не отправятся
- Если Access Token неверный, события не отправятся
- Если Pixel Code неверный, события не отправятся

**Что нужно проверить:**
- Настроен ли TikTok Events API на сервере?
- Установлены ли `TIKTOK_EVENTS_ACCESS_TOKEN` и `TIKTOK_EVENTS_PIXEL_CODE`?
- Работает ли TikTok Events API (нет ли ошибок в логах)?

---

### Предположение 10: Проблемы с дедупликацией

**Что я думаю:**
- Facebook/TikTok дедуплицируют события по `event_id`, `fbp`, `fbc`, `external_id`
- Если эти параметры не совпадают, события могут дублироваться
- Если `event_id` не уникален, события могут не дедуплицироваться правильно

**Параметры дедупликации:**
- `event_id`: Уникальный идентификатор события
- `fbp`: Facebook Browser Pixel cookie
- `fbc`: Facebook Click ID cookie
- `external_id`: Внешний идентификатор пользователя

**Почему это может быть проблемой:**
- Если `event_id` не синхронизирован, события дублируются
- Если `fbp`/`fbc` не передаются, дедупликация может не работать
- Если `external_id` не синхронизирован, события могут дублироваться

**Что нужно проверить:**
- Совпадает ли `event_id` между браузером и сервером?
- Передаются ли `fbp` и `fbc` в события?
- Совпадает ли `external_id` между браузером и сервером?

---

## 🔄 МЫСЛИ О СИНХРОНИЗАЦИИ

### Синхронизация event_id

**Браузер:**
```2018:2018:twocomms/twocomms_django_theme/templates/pages/order_success.html
var eventId = '{{ order.get_facebook_event_id }}';
```

**Сервер:**
```307:307:twocomms/orders/facebook_conversions_service.py
event_id = order.get_facebook_event_id()
```

**Метод:**
```115:127:twocomms/orders/models.py
def get_facebook_event_id(self):
    """
    Генерирует уникальный event_id для Facebook Pixel/Conversions API.
    Используется для дедупликации событий между браузером и сервером.
    
    Format: {order_number}_{timestamp}
    Example: TWC30102025N01_1730304000
    """
    import hashlib
    import time
    # Используем order_number + timestamp создания как уникальный идентификатор
    timestamp = int(self.created.timestamp()) if self.created else int(time.time())
    return f"{self.order_number}_{timestamp}"
```

**💭 МОЯ МЫСЛЬ:**
- Метод использует `order.created.timestamp()` - это хорошо для синхронизации
- Но если шаблон кэшируется, `{{ order.get_facebook_event_id }}` может быть старым значением
- Если объект `order` обновляется после рендеринга шаблона, timestamp может измениться
- Нужно убедиться что `order.created` не изменяется после создания заказа

---

### Синхронизация external_id

**Браузер (order_success.html):**
```1875:1891:twocomms/twocomms_django_theme/templates/pages/order_success.html
var externalSource = '';
if (userIdAttr) {
  externalSource = 'user:' + userIdAttr;
} else if (sessionKey) {
  externalSource = 'session:' + sessionKey;
} else if (orderNumber) {
  externalSource = 'order:' + orderNumber;
} else if (orderId) {
  externalSource = 'order:' + orderId;
}
```

**Браузер (checkout.py):**
```263:293:twocomms/storefront/views/checkout.py
# КРИТИЧНО: External ID должен ВСЕГДА быть определен
external_source = None
if request.user.is_authenticated:
    external_source = f"user:{request.user.id}"
else:
    # Пытаемся получить session_key
    try:
        session_key = request.session.session_key
        if not session_key:
            # Создаем сессию если еще не создана
            request.session.create()
            session_key = request.session.session_key
        if session_key:
            external_source = f"session:{session_key}"
    except Exception:
        pass
    
    # Если нет session_key, используем order_number
    if not external_source and order.order_number:
        external_source = f"order:{order.order_number}"
    
    # Если нет order_number, используем order.id
    if not external_source and order.id:
        external_source = f"order:{order.id}"

# ГАРАНТИРУЕМ что external_id ВСЕГДА определен
if not external_source:
    import time
    external_source = f"order:unknown_{int(time.time())}"

tracking_context['external_id'] = external_source
```

**Сервер (facebook_conversions_service.py):**
```160:178:twocomms/orders/facebook_conversions_service.py
external_source = tracking_data.get('external_id')
if not external_source:
    # Fallback: генерируем external_id если не передан из checkout
    if order.user_id:
        external_source = f"user:{order.user_id}"
    elif order.session_key:
        external_source = f"session:{order.session_key}"
    elif order.order_number:
        external_source = f"order:{order.order_number}"
```

**💭 МОЯ МЫСЛЬ:**
- Логика генерации `external_id` похожа, но может отличаться
- В checkout используется `request.user.id`, на сервере `order.user_id` - это должно совпадать
- В checkout используется `request.session.session_key`, на сервере `order.session_key` - это должно совпадать
- Но если `external_id` не передается в `payment_payload['tracking']['external_id']`, сервер будет использовать fallback логику
- Нужно убедиться что `external_id` сохраняется в `payment_payload` при создании заказа

---

## 🌐 МЫСЛИ О ЗАГРУЗКЕ ПИКСЕЛЕЙ

### Facebook Pixel

**Загрузка:**
```757:847:twocomms/twocomms_django_theme/static/js/analytics-loader.js
function loadMetaPixel() {
  if (!PIXEL_ID || (win.fbq && win.__fbPixelLoaded)) {
    return;
  }
  
  // Защита от блокировки: инициализируем Pixel с проверками
  try {
  !function (f, b, e, v, n, t, s) {
    // ... код загрузки Facebook Pixel
    win.fbq('init', PIXEL_ID, advancedMatching);
    win.fbq('track', 'PageView');
  }
  
  // Mark pixel as loaded and process buffered events
  win._fbqLoaded = true;
  if (win._fbqBuffer && win._fbqBuffer.length > 0) {
    // ... обработка буфера
  }
}
```

**💭 МОЯ МЫСЛЬ:**
- Facebook Pixel загружается сразу при загрузке страницы
- Если скрипт не загрузился (блокировка, ошибка сети), `win._fbqLoaded` останется `false`
- Буфер обрабатывается только если `win._fbqLoaded = true`
- Если пиксель не загрузился, события останутся в буфере

---

### TikTok Pixel

**Загрузка:**
```849:1077:twocomms/twocomms_django_theme/static/js/analytics-loader.js
function loadTikTokPixel() {
  if (!TIKTOK_PIXEL_ID || (win.ttq && win.__ttqPixelLoaded)) {
    return;
  }
  
  // ... код загрузки TikTok Pixel
  win.ttq.load(TIKTOK_PIXEL_ID, loadOptions);
  
  // Проверка готовности через интервал
  var checkReady = setInterval(function() {
    if (w.ttq && typeof w.ttq.track === 'function') {
      // ... проверка что это реальная функция
      w._ttqLoaded = true;
      // ... обработка буфера
    }
  }, 100);
}
```

**💭 МОЯ МЫСЛЬ:**
- TikTok Pixel загружается через `ttq.load()`
- Проверка готовности через интервал каждые 100ms
- Таймаут 5 секунд - если за это время не загрузился, помечается как загруженный
- Если пиксель не загрузился, события могут отправиться в пустоту

---

## 🎯 ИТОГОВЫЕ ПРЕДПОЛОЖЕНИЯ

### Главные проблемы (в порядке вероятности):

1. **Элемент #checkout-payload отсутствует в cart.html**
   - InitiateCheckout не может отправиться
   - StartPayment не может отправиться
   - Это критическая проблема синхронизации между бэкендом и фронтендом

2. **payment_status не устанавливается в 'prepaid'**
   - Если `pay_type != 'prepay_200'`, статус не изменится
   - Если webhook не пришел, статус не изменится
   - Lead событие не отправится если статус не `'prepaid'`

3. **Элемент #purchase-payload отсутствует или не содержит данных**
   - Lead/Purchase события не могут отправиться
   - Если `data-payment-status` не `'prepaid'`, Lead не отправится

4. **Проблемы с синхронизацией event_id**
   - Если timestamp отличается, event_id не синхронизируется
   - События могут дублироваться или не дедуплицироваться

5. **Проблемы с синхронизацией external_id**
   - Если логика отличается, external_id не синхронизируется
   - События могут не дедуплицироваться правильно

6. **Пиксели не загружаются или блокируются**
   - Ad blockers могут блокировать пиксели
   - События остаются в буфере и не отправляются

7. **Conversions API не настроен или не работает**
   - Серверные события не отправляются
   - Только браузерные события (если они работают)

---

## 🔍 ЧТО НУЖНО ПРОВЕРИТЬ (НЕ ИНСТРУКЦИИ, А ПРЕДПОЛОЖЕНИЯ)

### Для InitiateCheckout:
- Есть ли элемент `#checkout-payload` в `cart.html`?
- Если нет, то почему данные готовятся но не используются?
- Может быть элемент был удален при рефакторинге?
- Может быть данные передаются но не рендерятся?

### Для Lead:
- Установлен ли `payment_status='prepaid'` для предзаказа на сервере?
- Пришел ли webhook от Monobank?
- Установился ли `pay_type='prepay_200'` при создании заказа?
- Есть ли элемент `#purchase-payload` в `order_success.html`?
- Заполнены ли все необходимые `data-*` атрибуты?

### Для синхронизации:
- Совпадает ли `event_id` между браузером и сервером?
- Совпадает ли `external_id` между браузером и сервером?
- Передается ли `external_id` из checkout в `payment_payload`?
- Используется ли один и тот же метод `get_facebook_event_id()`?

### Для пикселей:
- Загружаются ли пиксели в браузере?
- Видны ли они в консоли?
- Блокируются ли они ad blockers?
- Обрабатывается ли буфер после загрузки?

### Для Conversions API:
- Настроен ли Conversions API на сервере?
- Установлены ли все необходимые ENV переменные?
- Работает ли Conversions API (нет ли ошибок в логах)?
- Отправляются ли события на сервере?

---

## 📝 ЗАКЛЮЧЕНИЕ

Основные предположения о проблемах:

1. **InitiateCheckout не отправляется** - элемент `#checkout-payload` отсутствует в `cart.html`
2. **Lead не отправляется** - `payment_status` не устанавливается в `'prepaid'` или элемент `#purchase-payload` отсутствует
3. **Проблемы с синхронизацией** - `event_id` или `external_id` не синхронизируются между браузером и сервером
4. **Пиксели не загружаются** - блокируются ad blockers или не загружаются из-за ошибок сети
5. **Conversions API не работает** - не настроен или есть ошибки при отправке

**Рекомендуется начать с проверки:**
1. Элемент `#checkout-payload` в `cart.html`
2. `payment_status` для предзаказа на сервере
3. Элемент `#purchase-payload` в `order_success.html`
4. Загрузка пикселей в браузере
5. Настройки Conversions API на сервере

---

**Следующие шаги для другого разработчика:**
1. Проверить наличие элементов в шаблонах
2. Проверить установку `payment_status` для предзаказов
3. Проверить синхронизацию `event_id` и `external_id`
4. Проверить загрузку пикселей в браузере
5. Проверить настройки Conversions API на сервере

**Не инструкции, а предположения:** Это не инструкции как исправить, а предположения о том, что может быть не так. Каждое предположение нужно проверить и подтвердить или опровергнуть.














