# ✅ Проверка TikTok Pixel на реальных страницах

## 🔍 Что было проверено:

### 1. **Страница товара (product_detail.html)**

✅ **ViewContent** - Работает правильно:
- Использует `trackEvent('ViewContent', ...)`
- Правильно заполняет `offer_id` из текущего выбора (цвет + размер)
- Передает все данные: `content_ids`, `content_name`, `content_type`, `content_category`, `value`, `currency`, `contents`
- Учитывает предвыбранный размер из URL параметра

✅ **CustomizeProduct** - Работает правильно:
- Использует `trackEvent('CustomizeProduct', ...)`
- Обновляет `offer_id` при изменении цвета/размера
- Передает правильный `offer_id` в `content_ids`

✅ **AddToCart** (из main.js) - Работает правильно:
- Использует `trackEvent('AddToCart', ...)`
- Берет `offer_id` из `data-current-offer-id` на странице товара
- Передает все данные: `content_ids`, `value`, `currency`, `num_items`, `contents`

---

### 2. **Каталог товаров**

✅ **ViewContent** (при клике на карточку) - Работает правильно:
- Использует `trackEvent('ViewContent', ...)`
- Генерирует базовый `offer_id`: `TC-{pid}-default-S`
- Передает все данные: `content_ids`, `content_name`, `value`, `currency`, `contents`

✅ **AddToCart** (из каталога) - Работает правильно:
- Использует `trackEvent('AddToCart', ...)`
- Генерирует базовый `offer_id`: `TC-{pid}-default-S`
- Передает все данные: `content_ids`, `value`, `currency`, `num_items`, `contents`

---

### 3. **Корзина и Checkout**

✅ **InitiateCheckout** - Работает правильно:
- Использует `trackEvent('InitiateCheckout', ...)`
- Передает все данные: `value`, `currency`, `num_items`, `content_ids`, `contents`

✅ **StartPayment** - Работает правильно:
- Использует `trackEvent('StartPayment', ...)`
- Передает все данные: `value`, `currency`, `num_items`, `content_ids`, `contents`

---

### 4. **Страница успешного заказа (order_success.html)**

#### ❌ **БЫЛО (ПРОБЛЕМЫ):**

1. **Purchase событие:**
   - Использовался прямой вызов `ttq.track('CompletePayment', ...)` ❌
   - Неправильное имя события: `CompletePayment` вместо `Purchase` ❌
   - Не использовалась система `trackEvent()` и `buildTikTokPayload()` ❌
   - Advanced Matching не применялся через единую систему ❌

2. **Lead событие:**
   - Использовался прямой вызов `ttq.track('CompletePayment', ...)` ❌
   - Неправильное имя события: `CompletePayment` вместо `Lead` ❌
   - Не использовалась система `trackEvent()` ❌

#### ✅ **СТАЛО (ИСПРАВЛЕНО):**

1. **Purchase событие:**
   - ✅ Использует `trackEvent('Purchase', ...)`
   - ✅ Правильное имя события: `Purchase`
   - ✅ Использует `buildTikTokPayload()` для автоматического форматирования
   - ✅ Все данные заполняются правильно: `content_ids`, `contents`, `value`, `currency`
   - ✅ `contents` массив содержит: `id`, `item_price`, `quantity`, `item_name`

2. **Lead событие:**
   - ✅ Использует `trackEvent('Lead', ...)`
   - ✅ Правильное имя события: `Lead`
   - ✅ Использует единую систему обработки

---

### 5. **Advanced Matching (ttqIdentify)**

✅ **Работает правильно:**
- Вызывается при инициализации TikTok Pixel (в `loadTikTokPixel()`)
- Использует данные из элемента `#am` в `base.html`
- Для авторизованных пользователей передаются:
  - ✅ `email` (если валидный)
  - ✅ `phone_number` (если валидный, 10-15 цифр)
  - ✅ `external_id` (ID пользователя)
- Для гостевых пользователей:
  - ✅ Advanced Matching не отправляется (нормально)
  - ✅ События работают без Advanced Matching

---

## 📊 Сводная таблица всех событий:

| Событие | Страница | Использует trackEvent? | offer_id | contents | value | currency | Статус |
|---------|----------|------------------------|----------|----------|-------|----------|--------|
| ViewContent | product_detail | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| ViewContent | каталог | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| CustomizeProduct | product_detail | ✅ Да | ✅ Да | ❌ Нет | ❌ Нет | ❌ Нет | ✅ Работает |
| AddToCart | product_detail | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| AddToCart | каталог | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| AddToWishlist | каталог | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| RemoveFromWishlist | каталог | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| InitiateCheckout | корзина | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| StartPayment | checkout | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Работает |
| Purchase | order_success | ✅ Да (исправлено) | ✅ Да | ✅ Да | ✅ Да | ✅ Да | ✅ Исправлено |
| Lead | order_success | ✅ Да (исправлено) | ❌ Нет | ❌ Нет | ✅ Да | ✅ Да | ✅ Исправлено |
| Search | хедер | ✅ Да | ❌ Нет | ❌ Нет | ❌ Нет | ❌ Нет | ✅ Работает |

---

## ✅ Что заполняется автоматически:

### Для всех e-commerce событий:

1. **`value`** - автоматически:
   - Используется из `payload.value` если есть
   - Иначе вычисляется из `contents` (item_price * quantity)
   - Если всё равно 0 → ставится `0.01` (минимум для TikTok)

2. **`currency`** - автоматически:
   - Используется из `payload.currency` или по умолчанию `UAH`

3. **`contents`** - автоматически:
   - Преобразуется в формат TikTok
   - Добавляется `brand: 'TwoComms'` в каждый item
   - Добавляется `num_items` если есть `quantity`

4. **`content_ids`** - автоматически:
   - Генерируется из `contents` если не указан явно

---

## 🎯 Итоговые результаты:

### ✅ **Все события работают правильно:**

1. **Используют единую систему `trackEvent()`:**
   - ✅ Все события проходят через `buildTikTokPayload()`
   - ✅ Автоматическая валидация и форматирование
   - ✅ Единообразная обработка данных

2. **Заполняют все доступные данные:**
   - ✅ `offer_id` (формат: `TC-{id}-{variant}-{SIZE}`)
   - ✅ `contents` массив с полной информацией
   - ✅ `value` и `currency` для всех e-commerce событий
   - ✅ Advanced Matching для авторизованных пользователей

3. **Правильная обработка гостевых пользователей:**
   - ✅ События отправляются без Advanced Matching
   - ✅ Все остальные данные заполняются правильно
   - ✅ Никаких ошибок

---

## 🔧 Исправления внесены:

### Commit: `aeb1c63`

**Исправлено в order_success.html:**
1. ❌ `ttq.track('CompletePayment', ...)` → ✅ `trackEvent('Purchase', ...)`
2. ❌ `ttq.track('CompletePayment', ...)` → ✅ `trackEvent('Lead', ...)`
3. ✅ Теперь используется `buildTikTokPayload()` для автоматического форматирования
4. ✅ Все доступные данные заполняются правильно

---

## 📝 Рекомендации:

### ✅ Всё работает правильно, дополнительных изменений не требуется.

Все события:
- ✅ Используют единую систему `trackEvent()`
- ✅ Заполняют все доступные данные
- ✅ Правильно обрабатывают гостевых пользователей
- ✅ Используют правильные имена событий

---

**Версия:** 1.0  
**Дата:** Январь 2025  
**Статус:** ✅ Все проверено и исправлено

