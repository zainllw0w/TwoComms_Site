# 🔍 Детальное объяснение: Как TikTok Pixel обрабатывает отсутствующие параметры

## ❓ Ваш вопрос:

> "TikTok игнорирует только ОДИН параметр (например, phone_number) или ВЕСЬ запрос?
> Если нет phone_number - что именно отправляется? Всё без phone_number или ничего?"

---

## ✅ ОТВЕТ: TikTok игнорирует ТОЛЬКО отсутствующие параметры, НЕ весь запрос!

### Как это работает:

**TikTok отправляет ВСЁ что есть, БЕЗ отсутствующих параметров.**

---

## 📊 Конкретные примеры:

### Пример 1: Гостевой пользователь (нет phone_number)

**Что вы отправляете:**
```javascript
ttq.track('Purchase', {
  value: '599',
  currency: 'UAH',
  contents: [
    {
      content_id: 'TC-123-cv1-M',
      content_name: 'Футболка',
      price: 599,
      quantity: 1
    }
  ]
  // phone_number НЕТ!
});
```

**Что TikTok получает и сохраняет:**
```json
{
  "event": "Purchase",
  "value": "599",
  "currency": "UAH",
  "contents": [
    {
      "content_id": "TC-123-cv1-M",
      "content_name": "Футболка",
      "price": 599,
      "quantity": 1
    }
  ]
  // phone_number отсутствует - это нормально!
}
```

**Результат:**
- ✅ Событие **отправляется** и **сохраняется** в TikTok
- ✅ Все остальные данные (value, currency, contents) используются
- ✅ Phone_number просто **отсутствует** в данных (не ошибка!)

---

### Пример 2: Нет num_items, но есть contents

**Что вы отправляете:**
```javascript
ttq.track('AddToCart', {
  value: '599',
  currency: 'UAH',
  contents: [
    {
      content_id: 'TC-123-cv1-M',
      price: 599
      // quantity/num_items НЕТ!
    }
  ]
});
```

**Что TikTok получает:**
```json
{
  "event": "AddToCart",
  "value": "599",
  "currency": "UAH",
  "contents": [
    {
      "content_id": "TC-123-cv1-M",
      "price": 599
      // num_items отсутствует
    }
  ]
}
```

**Результат:**
- ✅ Событие **отправляется** и **сохраняется**
- ✅ TikTok может вычислить количество из других данных или использовать значение по умолчанию (1)
- ✅ Все остальные данные используются

---

### Пример 3: Нет нескольких параметров (гостевой пользователь)

**Что вы отправляете:**
```javascript
ttq.track('Purchase', {
  value: '599',
  currency: 'UAH',
  contents: [
    {
      content_id: 'TC-123-cv1-M',
      price: 599
    }
  ]
  // НЕТ: phone_number, email, external_id, num_items, description
});
```

**Что TikTok получает:**
```json
{
  "event": "Purchase",
  "value": "599",
  "currency": "UAH",
  "contents": [
    {
      "content_id": "TC-123-cv1-M",
      "price": 599
    }
  ]
  // Все отсутствующие параметры просто не включаются в запрос
}
```

**Результат:**
- ✅ Событие **отправляется** и **сохраняется**
- ✅ TikTok использует **только те данные, которые есть**
- ✅ Отсутствующие параметры **игнорируются** (не ошибка!)

---

## 🔑 Ключевой принцип:

### TikTok НЕ отменяет событие если параметра нет!

**Если чекбокс параметра ВКЛЮЧЕН в настройках:**

1. **Параметр ЕСТЬ в событии** → TikTok использует его ✅
2. **Параметра НЕТ в событии** → TikTok **игнорирует ТОЛЬКО этот параметр**, но отправляет **ВСЁ ОСТАЛЬНОЕ** ✅
3. **Параметр пустой/null** → TikTok может показать предупреждение, но событие отправляется ⚠️

---

## 📋 Что происходит с каждым типом параметра:

### 1. **Обязательные параметры (value, currency):**

Если их НЕТ:
- ⚠️ TikTok **может** показать предупреждение
- ✅ Но событие **всё равно отправляется** (если есть хотя бы базовые данные)
- ✅ В нашем коде мы всегда отправляем `value` и `currency` (см. `buildTikTokPayload`)

### 2. **Рекомендуемые параметры (content_id, content_name, etc.):**

Если их НЕТ:
- ✅ TikTok просто **не использует** эти параметры
- ✅ Событие **отправляется нормально**
- ✅ Отсутствие не влияет на работу события

### 3. **Опциональные параметры (phone_number, email, description):**

Если их НЕТ:
- ✅ TikTok **игнорирует** их полностью
- ✅ Событие **отправляется** со всеми остальными данными
- ✅ Это **нормальное поведение** для гостевых пользователей

---

## 🎯 Чекбоксы в настройках = Разрешение принять параметр

### Если чекбокс ВКЛЮЧЕН:

```
Чекбокс "phone_number" включен:
  → TikTok: "Хорошо, я буду искать phone_number в событиях"
  → Если phone_number есть → использует его ✅
  → Если phone_number нет → просто пропускает, отправляет остальное ✅
```

### Если чекбокс ВЫКЛЮЧЕН:

```
Чекбокс "phone_number" выключен:
  → TikTok: "Я игнорирую phone_number даже если он отправлен"
  → Даже если вы отправили phone_number → TikTok его проигнорирует ❌
```

---

## 💡 Практический пример из вашего кода:

Посмотрим на функцию `buildTikTokPayload`:

```javascript
// Строка 311-314
if (payload.search_string) {
  ttqPayload.search_string = payload.search_string;
}
// Если search_string НЕТ → просто не добавляем его в ttqPayload
// TikTok получит запрос БЕЗ search_string (нормально!)
```

```javascript
// Строка 316-319
if (payload.description) {
  ttqPayload.description = payload.description;
}
// Если description НЕТ → просто не добавляем
// TikTok получит запрос БЕЗ description (нормально!)
```

**Важно:** Мы добавляем параметр в `ttqPayload` **только если он есть**. Если параметра нет - он просто не попадает в финальный объект, который отправляется в TikTok.

---

## 📊 Визуализация процесса:

### Сценарий: Гостевой пользователь делает Purchase

```
1. Ваш код отправляет:
   ttq.track('Purchase', {
     value: '599',
     currency: 'UAH',
     contents: [...]
     // phone_number, email, external_id отсутствуют
   })

2. TikTok Pixel получает:
   {
     event: "Purchase",
     value: "599",
     currency: "UAH",
     contents: [...]
     // phone_number, email, external_id НЕ включены в объект
   }

3. TikTok проверяет настройки:
   ✅ phone_number чекбокс включен? → Да
   ✅ Ищет phone_number в данных? → Нет
   ✅ Решение: Игнорирую phone_number, использую остальное

4. TikTok сохраняет событие:
   ✅ Событие Purchase сохранено
   ✅ Value: 599
   ✅ Currency: UAH
   ✅ Contents: [...]
   ⚠️ Advanced Matching данные отсутствуют (нормально для гостей)
```

---

## ✅ Итоговые ответы:

### 1. Игнорирует ли TikTok весь запрос если параметра нет?

**НЕТ!** TikTok игнорирует **только отсутствующий параметр**, но отправляет **всё остальное**.

### 2. Что отправляется если нет phone_number?

**Отправляется ВСЁ что есть, БЕЗ phone_number:**
- ✅ value
- ✅ currency
- ✅ contents
- ✅ content_id
- ✅ и т.д.
- ❌ phone_number (просто отсутствует)

### 3. Отправляется ли событие для гостевых пользователей?

**ДА!** Событие **всегда отправляется**, просто без Advanced Matching данных (email, phone).

### 4. Что делать с чекбоксами?

**Рекомендация:**
- ✅ Включайте чекбоксы для параметров, которые **иногда** могут быть
- ✅ Если параметр **никогда** не будет (например, search_string для Purchase) - можно выключить
- ✅ Но можно и включить всё - TikTok просто проигнорирует отсутствующие

---

## 🔍 Как проверить что именно отправляется:

### В браузере (Chrome DevTools):

1. Откройте **Network** tab
2. Фильтр: `analytics.tiktok.com`
3. Найдите запрос события
4. Посмотрите **Payload** или **Request Payload**

**Вы увидите точно что отправляется - все присутствующие параметры и отсутствие тех, которых нет.**

---

**Версия:** 1.0  
**Дата:** Январь 2025  
**Статус:** ✅ Детальное объяснение

