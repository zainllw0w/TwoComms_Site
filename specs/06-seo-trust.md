# 06 — SEO / Trust Pages (Draft)

## Pages Added
- `/quality` — Lab Proof / QC summary
- `/templates` — 60cm templates + how-to
- `/how-to-press` — transfer instructions
- `/preflight` — file check lead magnet (upload + example report + CTA)
- `/price` — tooltips “що входить” + cutoff/терміни block

## Requirements Page
- Added OK vs RISK visual placeholder, direct template download, and link to templates.

## Notes
- Added canonical + OG tags in base template.
- Added DTF sitemap at `/sitemap.xml` via `DtfStaticSitemap`.
- All pages use existing design system components and placeholders.
- Assets referenced via `ASSETS_MANIFEST.md`.
- Gallery JSON‑LD includes ImageObject entries (when images present).
- Added FAQPage JSON‑LD for requirements + quality pages.
- Added BreadcrumbList JSON‑LD on internal pages (status, gallery, price, requirements, templates, preflight, quality).
