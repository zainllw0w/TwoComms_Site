# 🔍 ГЛУБОКИЙ АНАЛИЗ ПРОБЛЕМ С ПИКСЕЛЯМИ (Facebook и TikTok)

**Дата анализа:** 2025-01-30  
**Проблема:** Предзаказ на 200 грн не отображается в пикселях. Видны только "добавления в корзину", но нет Lead, InitiateCheckout и Purchase событий.

---

## 📋 СОДЕРЖАНИЕ

1. [Текущая архитектура отправки событий](#текущая-архитектура)
2. [Анализ InitiateCheckout события](#анализ-initiatecheckout)
3. [Анализ Lead события](#анализ-lead-события)
4. [Анализ Purchase события](#анализ-purchase-события)
5. [Выявленные проблемы](#выявленные-проблемы)
6. [Рекомендации по исправлению](#рекомендации)

---

## 🏗️ ТЕКУЩАЯ АРХИТЕКТУРА ОТПРАВКИ СОБЫТИЙ

### 1. InitiateCheckout (Начало оформления)

**Место отправки:** `twocomms/twocomms_django_theme/static/js/main.js:663`

```javascript
window.trackEvent('InitiateCheckout', {
  value: analytics.value,
  currency: analytics.currency,
  num_items: analytics.num_items,
  payment_method: 'monobank',
  content_ids: analytics.content_ids,
  contents: analytics.contents
});
```

**Условия отправки:**
- ✅ Отправляется при клике на кнопку оплаты (`data-mono-checkout-trigger`)
- ✅ Требуется элемент `#checkout-payload` на странице
- ✅ Функция `getCheckoutAnalyticsPayload()` должна вернуть данные

**Источник данных:**
- `checkout-payload` создается в `storefront/views/cart.py:253-292`
- Данные включают: `content_ids`, `contents`, `value`, `currency`, `num_items`

### 2. Lead (Предоплата 200 грн)

**Места отправки:**

#### A. Браузер (order_success.html:2070)
```javascript
if (shouldSendLead && !sessionStorage.getItem(leadStorageKey)) {
  // paymentStatus === 'prepaid'
  window.trackEvent('Lead', {
    event_id: leadEventId,
    value: leadValue,  // 200 грн
    currency: currency,
    __meta: leadMeta  // user_data, external_id, fbp, fbc
  });
}
```

**Условия:**
- ✅ `payment_status === 'prepaid'`
- ✅ Проверка `sessionStorage` для предотвращения дублей
- ✅ Зависит от данных в `#purchase-payload` элементе

#### B. Сервер (Facebook Conversions API)
**Место:** `orders/facebook_conversions_service.py:370-455`

```python
def send_lead_event(self, order, ...):
    if order.payment_status == 'prepaid':
        prepayment_amount = order.get_prepayment_amount()
        # Отправка через Conversions API
```

**Триггер:** `storefront/views/utils.py:403-422`
- Срабатывает когда `payment_status` меняется на `'prepaid'`
- Проверка дублей через `payment_payload['facebook_events']['lead_sent']`

### 3. Purchase (Полная оплата)

**Места отправки:**

#### A. Браузер (order_success.html:1982)
```javascript
if (shouldSendPurchase && !sessionStorage.getItem(purchaseStorageKey)) {
  // paymentStatus === 'paid'
  window.trackEvent('Purchase', {
    event_id: eventId,
    value: purchaseValue,
    currency: currency,
    contents: purchaseContents,
    __meta: purchaseMeta
  });
}
```

#### B. Сервер (Facebook Conversions API)
**Место:** `orders/facebook_conversions_service.py:281-368`

---

## 🔍 АНАЛИЗ INITIATECHECKOUT

### Код отправки

```663:670:twocomms/twocomms_django_theme/static/js/main.js
window.trackEvent('InitiateCheckout', {
  value: analytics.value,
  currency: analytics.currency,
  num_items: analytics.num_items,
  payment_method: 'monobank',
  content_ids: analytics.content_ids,
  contents: analytics.contents
});
```

### Функция получения данных

```358:377:twocomms/twocomms_django_theme/static/js/main.js
function getCheckoutAnalyticsPayload(){
  const el = document.getElementById('checkout-payload');
  if(!el) return null;
  let contents = [];
  let ids = [];
  const rawContents = el.dataset.contents ? decodeURIComponent(el.dataset.contents) : '[]';
  const rawIds = el.dataset.ids ? decodeURIComponent(el.dataset.ids) : '[]';
  try { contents = JSON.parse(rawContents || '[]'); } catch(_){ contents = []; }
  try { ids = JSON.parse(rawIds || '[]'); } catch(_){ ids = []; }
  const value = parseFloat(el.dataset.value || '0');
  const currency = el.dataset.currency || 'UAH';
  let numItems = parseInt(el.dataset.numItems || '0', 10);
  if(Number.isNaN(numItems) || numItems <= 0)
```

### ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ:

1. **Элемент `#checkout-payload` отсутствует**
   - Если элемент не найден, `getCheckoutAnalyticsPayload()` вернет `null`
   - InitiateCheckout не отправится
   - **Проверка:** Элемент должен быть на странице корзины (`cart.html`)

2. **Данные не загружены**
   - Если `data-contents` или `data-ids` пустые, события отправятся без товаров
   - **Проверка:** Проверить что `checkout_payload` заполняется в `cart.py`

3. **Блокировка браузером**
   - Ad blockers могут блокировать `window.trackEvent`
   - **Проверка:** Проверить консоль браузера на ошибки

---

## 🔍 АНАЛИЗ LEAD СОБЫТИЯ

### Логика отправки в браузере

```1814:1815:twocomms/twocomms_django_theme/templates/pages/order_success.html
var shouldSendLead = paymentStatus === 'prepaid';  // ТОЛЬКО предоплата
var shouldSendPurchase = paymentStatus === 'paid';  // ТОЛЬКО полная оплата
```

### Код отправки Lead

```2070:2135:twocomms/twocomms_django_theme/templates/pages/order_success.html
// Lead событие - только для prepaid (предоплата)
if (shouldSendLead && !sessionStorage.getItem(leadStorageKey)) {
  var leadData = {
    event: 'lead',
    lead_data: {
      order_id: orderNumber,
      value: leadValue,
      currency: currency,
      payment_status: paymentStatus
    }
  };
  
  if (Object.keys(userData).length > 0) {
    leadData.user_data = userData;
  }
  
  // Защита от блокировки: проверяем что dataLayer доступен
  try {
    if (window.dataLayer && Array.isArray(window.dataLayer)) {
  window.dataLayer.push(leadData);
      console.log('📋 GTM Lead event sent (PAYMENT: ' + paymentStatus + '):', leadData);
    } else {
      console.debug('GTM dataLayer not available for Lead - possible blocker');
    }
  } catch (gtmErr) {
    console.debug('GTM Lead push error (possible blocker):', gtmErr);
  }
  
  if (window.trackEvent && typeof window.trackEvent === 'function') {
    try {
      var leadEventId = '{{ order.get_facebook_event_id }}_lead';
      var leadMeta = {
        event_id: leadEventId,
        user_data: fbUserData
      };
      if (externalHash) {
        leadMeta.external_id = externalHash;
      }
      var fbpValueLead = readCookieValue('_fbp');
      var fbcValueLead = readCookieValue('_fbc');
      if (fbpValueLead) {
        leadMeta.fbp = fbpValueLead;
      }
      if (fbcValueLead) {
        leadMeta.fbc = fbcValueLead;
      }
      var leadTrackPayload = {
        event_id: leadEventId,
        value: leadValue,
        currency: currency,
        content_name: 'Order ' + orderNumber,
        content_category: 'order_lead',
        content_type: 'lead',
        __meta: leadMeta
      };
      window.trackEvent('Lead', leadTrackPayload);
      console.log('📋 Pixel events dispatched for Lead (PAYMENT: ' + paymentStatus + ') with eventID:', leadEventId, leadTrackPayload);
    } catch (leadErr) {
      console.debug('Lead tracking error (possible blocker):', leadErr);
    }
  } else {
    console.debug('trackEvent bridge not available for Lead - possible blocker');
  }

  // Сохраняем что Lead отправлен
  sessionStorage.setItem(leadStorageKey, 'true');
}
```

### Логика установки payment_status

```373:375:twocomms/storefront/views/utils.py
if order.pay_type == 'prepay_200':
    order.payment_status = 'prepaid'
    monobank_logger.info(f'✅ Order {order.order_number}: prepayment successful → payment_status=prepaid')
```

### ⚠️ КРИТИЧЕСКИЕ ПРОБЛЕМЫ:

1. **payment_status не установлен в 'prepaid'**
   - Если `pay_type != 'prepay_200'`, статус не установится
   - Если webhook не пришел или обработался с ошибкой
   - **Проверка:** Проверить логи сервера на установку `payment_status='prepaid'`

2. **Элемент `#purchase-payload` отсутствует**
   - Если элемент не найден, скрипт выйдет раньше (строка 1802)
   - **Проверка:** Элемент должен быть в `order_success.html`

3. **Данные не переданы в шаблон**
   - `data-prepaid-value` может быть пустым
   - Fallback: используется 200 грн (строка 1850-1852)
   - **Проверка:** Проверить что `order.get_prepayment_amount()` возвращает правильное значение

4. **Блокировка trackEvent**
   - Если `window.trackEvent` не определен, Lead не отправится
   - **Проверка:** Проверить что `analytics-loader.js` загружен

5. **Проблемы с синхронизацией event_id**
   - Браузер: `event_id = '{{ order.get_facebook_event_id }}_lead'`
   - Сервер: `event_id = f"{order.get_facebook_event_id()}_lead"`
   - Если метод не синхронизирован, события могут дублироваться или не дедуплицироваться

---

## 🔍 АНАЛИЗ PURCHASE СОБЫТИЯ

### Код отправки в браузере

```1982:2067:twocomms/twocomms_django_theme/templates/pages/order_success.html
// Purchase событие - сразу при полной оплате (не ждем получения товара)
if (shouldSendPurchase && !sessionStorage.getItem(purchaseStorageKey)) {
  var purchaseData = {
    event: 'purchase',
    ecommerce: {
      transaction_id: orderNumber,
      value: purchaseValue,
      currency: currency,
      items: contents.map(function(item) {
        return {
          item_id: item.id,
          item_name: item.name || 'Product ' + item.id,
          quantity: item.quantity,
          price: item.price || 0
        };
      })
    }
  };
  
  if (Object.keys(userData).length > 0) {
    purchaseData.user_data = userData;
  }
  
  // Защита от блокировки: проверяем что dataLayer доступен
  try {
    if (window.dataLayer && Array.isArray(window.dataLayer)) {
  window.dataLayer.push(purchaseData);
  console.log('✅ GTM Purchase event sent (PAID):', purchaseData);
    } else {
      console.debug('GTM dataLayer not available - possible blocker');
    }
  } catch (gtmErr) {
    console.debug('GTM push error (possible blocker):', gtmErr);
  }

  if (window.trackEvent && typeof window.trackEvent === 'function') {
    try {
      var eventId = '{{ order.get_facebook_event_id }}';
      var purchaseContents = contents.map(function(item) {
        return {
          id: String(item.id),
          item_price: item.price || 0,
          quantity: item.quantity || 1,
          item_name: item.name || ''
        };
      });
      
      var purchaseMeta = {
        event_id: eventId,
        user_data: fbUserData
      };
      if (externalHash) {
        purchaseMeta.external_id = externalHash;
      }
      
      // КРИТИЧНО: Добавляем fbp и fbc для дедупликации
      var fbpValue = readCookieValue('_fbp');
      var fbcValue = readCookieValue('_fbc');
      if (fbpValue) {
        purchaseMeta.fbp = fbpValue;
      }
      if (fbcValue) {
        purchaseMeta.fbc = fbcValue;
      }
      
      var purchasePayloadPixels = {
        event_id: eventId,
        value: purchaseValue,
        currency: currency,
        content_type: 'product',
        content_ids: purchaseContents.map(function(item) { return item.id; }),
        contents: purchaseContents,
        content_name: 'Order ' + orderNumber,
        __meta: purchaseMeta
      };
      window.trackEvent('Purchase', purchasePayloadPixels);
      console.log('✅ Pixel events dispatched for Purchase with eventID:', eventId, purchasePayloadPixels);
    } catch (purchaseErr) {
      console.debug('Purchase tracking error (possible blocker):', purchaseErr);
    }
  } else {
    console.debug('trackEvent bridge not available for Purchase - possible blocker');
  }
  
  // Сохраняем что Purchase отправлен
  sessionStorage.setItem(purchaseStorageKey, 'true');
}
```

---

## 🚨 ВЫЯВЛЕННЫЕ ПРОБЛЕМЫ

### 1. ❌ InitiateCheckout может не отправляться

**Причины:**
- Элемент `#checkout-payload` отсутствует на странице корзины
- `getCheckoutAnalyticsPayload()` возвращает `null`
- Данные не загружены в `data-contents` или `data-ids`

**Проверка:**
```javascript
// В консоли браузера на странице корзины:
console.log(document.getElementById('checkout-payload'));
console.log(getCheckoutAnalyticsPayload());
```

### 2. ❌ Lead не отправляется если payment_status != 'prepaid'

**Причины:**
- Webhook не пришел или обработался с ошибкой
- `pay_type != 'prepay_200'` при создании заказа
- `payment_status` не установлен сервером

**Проверка на сервере:**
```python
# Проверить последний предзаказ:
from orders.models import Order
from django.utils import timezone
from datetime import timedelta

today = timezone.now().date()
orders = Order.objects.filter(
    payment_status='prepaid',
    total_sum__gte=180,
    total_sum__lte=220,
    created__date=today
).order_by('-created')[:5]

for o in orders:
    print(f"Order {o.order_number}: payment_status={o.payment_status}, pay_type={o.pay_type}, total_sum={o.total_sum}")
```

### 3. ❌ Элемент #purchase-payload отсутствует или не содержит данных

**Проверка:**
```javascript
// На странице order_success:
const el = document.getElementById('purchase-payload');
console.log('Element:', el);
console.log('Payment status:', el?.dataset.paymentStatus);
console.log('Prepaid value:', el?.dataset.prepaidValue);
```

### 4. ❌ Блокировка пикселей браузером

**Признаки:**
- В консоли: `GTM dataLayer not available - possible blocker`
- `trackEvent bridge not available - possible blocker`
- `Meta Pixel script failed to load - possible ad blocker`

**Решение:**
- Использовать Conversions API (серверная отправка) как fallback
- Проверить что Conversions API настроен и работает

### 5. ❌ TikTok Pixel не загружается

**Проверка:**
```javascript
// В консоли браузера:
console.log('TikTok Pixel loaded:', window._ttqLoaded);
console.log('TikTok Pixel script loaded:', window._ttqScriptLoaded);
console.log('TikTok track function:', typeof window.ttq?.track);
```

**Проблемы:**
- Скрипт TikTok может не загружаться из-за блокировщика
- `_ttqLoaded` может быть `false` даже если пиксель загружен
- События буферизуются, но не отправляются

### 6. ❌ Facebook Pixel не загружается

**Проверка:**
```javascript
// В консоли браузера:
console.log('Facebook Pixel loaded:', window._fbqLoaded);
console.log('Facebook Pixel function:', typeof window.fbq);
console.log('Facebook Pixel ID:', window._fbq?.id);
```

### 7. ❌ Проблемы с синхронизацией event_id

**Браузер:**
```javascript
var eventId = '{{ order.get_facebook_event_id }}';
var leadEventId = '{{ order.get_facebook_event_id }}_lead';
```

**Сервер:**
```python
event_id = order.get_facebook_event_id()
lead_event_id = f"{order.get_facebook_event_id()}_lead"
```

**Проблема:** Если метод `get_facebook_event_id()` не синхронизирован, события могут дублироваться или не дедуплицироваться правильно.

---

## 💡 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### 1. ✅ Проверить наличие элементов на страницах

**Для InitiateCheckout:**
- Убедиться что `#checkout-payload` присутствует на `cart.html`
- Проверить что данные заполняются в `cart.py:253-292`

**Для Lead/Purchase:**
- Убедиться что `#purchase-payload` присутствует на `order_success.html`
- Проверить что все `data-*` атрибуты заполнены

### 2. ✅ Добавить логирование для отладки

**В order_success.html добавить:**
```javascript
console.log('=== PIXEL DEBUG ===');
console.log('Payment status:', paymentStatus);
console.log('Should send Lead:', shouldSendLead);
console.log('Should send Purchase:', shouldSendPurchase);
console.log('Element:', el);
console.log('Prepaid value:', prepaidValue);
console.log('Lead value:', leadValue);
```

### 3. ✅ Проверить работу Conversions API

**Проверка на сервере:**
```python
from orders.facebook_conversions_service import get_facebook_conversions_service
from orders.models import Order

service = get_facebook_conversions_service()
print(f"Facebook Conversions API enabled: {service.enabled}")
print(f"Pixel ID: {service.pixel_id}")

# Проверить последний предзаказ:
order = Order.objects.filter(payment_status='prepaid').order_by('-created').first()
if order:
    print(f"Order {order.order_number}: payment_status={order.payment_status}")
    # Проверить что Lead отправлен:
    payload = order.payment_payload or {}
    fb_events = payload.get('facebook_events', {})
    print(f"Lead sent: {fb_events.get('lead_sent', False)}")
```

### 4. ✅ Улучшить обработку ошибок

**В analytics-loader.js:**
- Добавить более детальное логирование ошибок
- Сохранять неудачные события для повторной отправки
- Использовать Conversions API как fallback

### 5. ✅ Проверить синхронизацию event_id

**Убедиться что:**
- Браузер и сервер используют одинаковый формат `event_id`
- Метод `order.get_facebook_event_id()` возвращает одинаковое значение на клиенте и сервере
- `external_id` синхронизирован между браузером и сервером

### 6. ✅ Добавить проверку загрузки пикселей

**В order_success.html:**
```javascript
// Проверка перед отправкой событий
if (!window._fbqLoaded) {
  console.warn('Facebook Pixel not loaded, events may not be sent');
}
if (!window._ttqLoaded) {
  console.warn('TikTok Pixel not loaded, events may not be sent');
}
```

### 7. ✅ Использовать Test Events для проверки

**Facebook Events Manager:**
- Включить Test Events
- Проверить что события приходят в реальном времени

**TikTok Events Manager:**
- Использовать `test_event_code` для проверки
- Проверить что события видны в Test Events

---

## 📊 ЧЕКЛИСТ ПРОВЕРКИ

### Для InitiateCheckout:
- [ ] Элемент `#checkout-payload` присутствует на странице корзины
- [ ] `getCheckoutAnalyticsPayload()` возвращает данные
- [ ] Событие отправляется при клике на кнопку оплаты
- [ ] Событие видно в Facebook Events Manager (Test Events)
- [ ] Событие видно в TikTok Events Manager (Test Events)

### Для Lead:
- [ ] `payment_status` установлен в `'prepaid'` для предзаказов
- [ ] Элемент `#purchase-payload` присутствует на `order_success.html`
- [ ] `data-prepaid-value` заполнен (или используется fallback 200 грн)
- [ ] Событие отправляется в браузере (`window.trackEvent('Lead')`)
- [ ] Событие отправляется на сервере (Conversions API)
- [ ] `event_id` синхронизирован между браузером и сервером
- [ ] Событие видно в Facebook Events Manager
- [ ] Событие видно в TikTok Events Manager

### Для Purchase:
- [ ] `payment_status` установлен в `'paid'` для полных оплат
- [ ] Событие отправляется в браузере
- [ ] Событие отправляется на сервере
- [ ] Событие видно в пикселях

---

## 🔧 КОНКРЕТНЫЕ ДЕЙСТВИЯ ДЛЯ ИСПРАВЛЕНИЯ

### 1. Проверить предзаказ на сервере

```bash
# Подключиться к серверу и проверить последний предзаказ
sshpass -p 'trs5m4t1' ssh -o StrictHostKeyChecking=no qlknpodo@195.191.24.169
cd /home/qlknpodo/TWC/TwoComms_Site/twocomms
source /home/qlknpodo/virtualenv/TWC/TwoComms_Site/twocomms/3.13/bin/activate
python manage.py shell
```

```python
from orders.models import Order
from django.utils import timezone
from datetime import timedelta

today = timezone.now().date()
order = Order.objects.filter(
    payment_status='prepaid',
    total_sum__gte=180,
    total_sum__lte=220,
    created__date=today
).order_by('-created').first()

if order:
    print(f"Order {order.order_number}:")
    print(f"  payment_status: {order.payment_status}")
    print(f"  pay_type: {order.pay_type}")
    print(f"  total_sum: {order.total_sum}")
    print(f"  created: {order.created}")
    
    # Проверить Conversions API
    payload = order.payment_payload or {}
    fb_events = payload.get('facebook_events', {})
    print(f"  Facebook Lead sent: {fb_events.get('lead_sent', False)}")
    
    tiktok_events = payload.get('tiktok_events', {})
    print(f"  TikTok Lead sent: {tiktok_events.get('lead_sent', False)}")
```

### 2. Проверить в браузере

**На странице order_success:**
1. Открыть DevTools → Console
2. Проверить логи:
   - `📋 GTM Lead event sent`
   - `📋 Pixel events dispatched for Lead`
   - `No pixel events to send` (если событие не отправляется)

3. Проверить элементы:
```javascript
const el = document.getElementById('purchase-payload');
console.log('Payment status:', el?.dataset.paymentStatus);
console.log('Prepaid value:', el?.dataset.prepaidValue);
```

### 3. Проверить в Facebook Events Manager

1. Открыть Facebook Events Manager
2. Выбрать Pixel (ID: `823958313630148`)
3. Перейти в Test Events
4. Оформить предзаказ
5. Проверить что событие `Lead` появилось

### 4. Проверить в TikTok Events Manager

1. Открыть TikTok Events Manager
2. Перейти в Test Events
3. Оформить предзаказ
4. Проверить что событие `Lead` появилось

---

## 📝 ЗАКЛЮЧЕНИЕ

Основные проблемы, которые могут приводить к отсутствию событий в пикселях:

1. **payment_status не установлен в 'prepaid'** - самая вероятная причина
2. **Элементы #checkout-payload или #purchase-payload отсутствуют**
3. **Блокировка пикселей браузером** (ad blockers)
4. **Пиксели не загружаются** (TikTok, Facebook)
5. **Проблемы с синхронизацией event_id**

**Рекомендуется начать с проверки:**
1. Установлен ли `payment_status='prepaid'` для предзаказа
2. Отправляется ли Lead событие через Conversions API на сервере
3. Загружаются ли пиксели в браузере
4. Видны ли события в Test Events в Facebook/TikTok Events Manager

---

**Следующие шаги:**
1. Проверить предзаказ на сервере (установлен ли payment_status)
2. Проверить логи сервера на отправку Lead через Conversions API
3. Проверить в браузере загрузку пикселей и отправку событий
4. Проверить в Events Manager (Test Events) видимость событий
















