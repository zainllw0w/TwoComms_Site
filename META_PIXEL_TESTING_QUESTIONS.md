# 🧪 ТЕСТИРОВАНИЕ И ВОПРОСЫ ПО META PIXEL

**Дата создания:** 2025-01-XX  
**Цель:** Выяснить все детали для правильной настройки Meta Pixel

---

## ❓ КРИТИЧЕСКИЕ ВОПРОСЫ (Требуют немедленного ответа)

### 1. ФОРМАТ ID В FACEBOOK КАТАЛОГЕ

**Вопрос:** Какой формат ID товаров используется в вашем Facebook каталоге?

**Варианты ответов:**
- [ ] Числовой формат (например: `1`, `2`, `3`)
- [ ] Строковый формат (например: `"1"`, `"2"`, `"3"`)
- [ ] С префиксом (например: `"TC-1"`, `"PROD-2"`, `"SKU-123"`)
- [ ] Другой формат (указать какой)

**Как проверить:**
1. Зайти в Meta Business Suite
2. Events Manager → Data Sources → Ваш пиксель (823958313630148)
3. Проверить подключенные каталоги
4. Открыть каталог
5. Посмотреть на ID товаров в каталоге

**Текущий формат в коде:**
```javascript
content_ids: [String(productId)]  // Преобразует в строку: ["1", "2", "3"]
```

**Если в каталоге:**
- Числовые ID → нужно изменить на: `[productId]` (без String)
- С префиксом → нужно добавить префикс: `["TC-" + productId]`
- Другой формат → нужно адаптировать код

---

### 2. СИНХРОНИЗАЦИЯ ТОВАРОВ С КАТАЛОГОМ

**Вопрос:** Как товары попадают в Facebook каталог?

**Варианты ответов:**
- [ ] Автоматически через API (Facebook Catalog API)
- [ ] Вручную загружаются через Business Manager
- [ ] Через Google Merchant Feed (если подключен)
- [ ] Другой способ (указать какой)

**Если автоматически:**
- Какой формат ID используется при синхронизации?
- Используется ли поле `external_id` или `retailer_id`?
- Как часто происходит синхронизация?

**Если вручную:**
- Нужно проверить что все товары загружены в каталог
- Проверить что ID в каталоге совпадают с ID на сайте

---

### 3. CONVERSIONS API ИНТЕГРАЦИЯ

**Вопрос:** Есть ли интеграция с Facebook Conversions API (серверная отправка событий)?

**Варианты ответов:**
- [ ] Да, есть интеграция
- [ ] Нет, только браузерная отправка через пиксель
- [ ] Не уверен / Нужно проверить

**Если да:**
- Какие события отправляются через CAPI?
- Используется ли дедупликация через `eventID`?
- Нужно ли отправлять те же события через пиксель (для дублирования)?

**Файлы для проверки:**
- `twocomms/orders/facebook_conversions_service.py` - есть этот файл!

**Если нет:**
- Рассмотреть добавление CAPI для надежности
- События не будут теряться при блокировке adblock

---

### 4. БЛОКИРОВКА ADBLOCK

**Вопрос:** Есть ли проблемы с блокировкой рекламных скриптов на сайте?

**Варианты ответов:**
- [ ] Да, много пользователей с блокировщиками
- [ ] Нет, не знаю о проблемах
- [ ] Не проверял

**Если да:**
- Какой процент пользователей использует блокировщики?
- Нужен ли резервный путь через сервер (CAPI)?

---

## 🧪 ТЕСТОВЫЕ СЦЕНАРИИ

### Тест 1: Проверка загрузки пикселя

**Цель:** Убедиться что пиксель загружается и PageView отправляется

**Шаги:**
1. Открыть сайт в режиме инкогнито (без расширений)
2. Открыть консоль разработчика (F12)
3. Перейти на главную страницу
4. Проверить в консоли:
   ```javascript
   // Проверить что пиксель загружен
   console.log('fbq exists:', typeof window.fbq === 'function');
   console.log('fbq loaded:', window._fbqLoaded);
   console.log('fbq buffer:', window._fbqBuffer);
   ```
5. Установить [Meta Pixel Helper](https://chrome.google.com/webstore/detail/facebook-pixel-helper/fdgfkebogiimcoedlicjlajpkdmockpc)
6. Обновить страницу
7. Проверить что Pixel Helper видит:
   - ✅ Пиксель найден
   - ✅ PageView событие отправлено

**Ожидаемый результат:**
- `window.fbq` существует и это функция
- `window._fbqLoaded === true` (после загрузки)
- Meta Pixel Helper показывает PageView

**Текущая проблема:**
- Meta Pixel Helper показывает что PageView не активирован

---

### Тест 2: Проверка AddToCart

**Цель:** Убедиться что AddToCart отправляется корректно

**Шаги:**
1. Открыть страницу товара
2. Открыть консоль разработчика
3. Добавить товар в корзину
4. Проверить в консоли:
   ```javascript
   // Проверить что событие отправлено
   // Должны увидеть в Network tab запрос к facebook.com
   ```
5. Проверить в Meta Pixel Helper:
   - ✅ AddToCart событие отправлено
   - ✅ content_ids содержат правильный ID

**Ожидаемый результат:**
- В Network tab виден запрос к `facebook.com/tr`
- Meta Pixel Helper показывает AddToCart событие
- content_ids содержат ID товара

**Текущая проблема:**
- Возможно дублирование события (две отправки)
- Content IDs могут не совпадать с каталогом

---

### Тест 3: Проверка Purchase

**Цель:** Убедиться что Purchase отправляется после оплаты

**Шаги:**
1. Добавить товар в корзину
2. Оформить заказ
3. Оплатить (или выбрать наложенный платеж)
4. Перейти на страницу успешного заказа (`/orders/success/{order_id}/`)
5. Проверить в консоли:
   ```javascript
   // Проверить что событие отправлено
   // Должны увидеть логи: "✅ Meta Pixel Purchase event sent"
   ```
6. Проверить в Meta Pixel Helper:
   - ✅ Purchase событие отправлено
   - ✅ content_ids содержат правильные ID
   - ✅ value и currency правильные

**Ожидаемый результат:**
- В консоли виден лог о отправке Purchase
- Meta Pixel Helper показывает Purchase событие
- Все параметры корректны

**Текущая проблема:**
- Purchase события не получены за последние 7 дней
- Возможно пиксель не загружен на момент отправки

---

### Тест 4: Проверка Content IDs

**Цель:** Сравнить ID в событиях с ID в каталоге

**Шаги:**
1. Получить ID товара с сайта (например из URL или data-атрибутов)
2. Отправить событие (AddToCart или Purchase)
3. Проверить в Meta Pixel Helper какой ID отправлен:
   - Открыть событие
   - Посмотреть на content_ids
4. Зайти в Facebook каталог
5. Найти товар с таким же ID
6. Сравнить форматы

**Ожидаемый результат:**
- ID в событии совпадает с ID в каталоге
- Формат одинаковый

**Текущая проблема:**
- Meta показывает предупреждение о несоответствии Content IDs
- Catalog Match Rate = 0%

---

### Тест 5: Проверка всех событий

**Цель:** Убедиться что все события отправляются

**Список событий для проверки:**

1. **PageView**
   - ✅ Должно отправляться на каждой странице
   - ❌ Сейчас: не активировано

2. **ViewContent**
   - ✅ Должно отправляться на странице товара
   - ❓ Нужно проверить отправляется ли

3. **AddToCart**
   - ✅ Должно отправляться при добавлении в корзину
   - ❌ Сейчас: не получено за 7 дней
   - ⚠️ Проблема: возможно дублирование

4. **InitiateCheckout**
   - ✅ Должно отправляться при переходе в корзину
   - ❓ Нужно проверить отправляется ли при загрузке страницы корзины

5. **Purchase**
   - ✅ Должно отправляться после оплаты
   - ❌ Сейчас: не получено за 7 дней

**Как проверить:**
1. Использовать Meta Pixel Helper для проверки в реальном времени
2. Проверить в Events Manager → Test Events
3. Проверить в консоли браузера (логи и Network tab)

---

## 📊 МОНИТОРИНГ И ПРОВЕРКА

### Где проверять события:

1. **Meta Pixel Helper (Расширение Chrome)**
   - Видно события в реальном времени
   - Показывает параметры события
   - Показывает ошибки

2. **Events Manager → Test Events**
   - События отправленные с сайта
   - Детальная информация о каждом событии
   - Можно фильтровать по типу события

3. **Events Manager → Overview**
   - Общая статистика событий
   - Графики по времени
   - Процент совпадения с каталогом

4. **Catalog Match Rate**
   - Events Manager → Data Sources → Pixel → Catalog Match Rate
   - Показывает процент совпадения ID с каталогом
   - Сейчас: 0% (нужно минимум 90%)

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### Проблема: PageView не активирован

**Симптомы:**
- Meta Pixel Helper показывает: "Pixel Helper found your Meta Pixel, but the pixel has not been activated for this event"

**Возможные причины:**
1. Пиксель загружается с задержкой (500ms)
2. Скрипт блокируется adblock
3. Ошибка в коде инициализации

**Диагностика:**
```javascript
// В консоли браузера:
console.log('fbq:', typeof window.fbq);
console.log('_fbqLoaded:', window._fbqLoaded);
console.log('_fbqBuffer:', window._fbqBuffer);

// Проверить Network tab:
// Должен быть запрос к connect.facebook.net/en_US/fbevents.js
```

---

### Проблема: События не отправляются

**Симптомы:**
- В Events Manager нет событий за последние 7 дней
- Meta Pixel Helper не показывает события

**Возможные причины:**
1. Пиксель не загружается (блокировка)
2. События отправляются до загрузки пикселя
3. Ошибки в коде отправки событий

**Диагностика:**
```javascript
// Проверить что события буферизуются:
console.log('Buffer:', window._fbqBuffer);

// Проверить что пиксель готов:
console.log('Ready:', window.fbq && window._fbqLoaded);

// Проверить отправку события вручную:
if (window.fbq) {
  window.fbq('track', 'PageView');
  console.log('PageView sent manually');
}
```

---

### Проблема: Content IDs не соответствуют каталогу

**Симптомы:**
- Catalog Match Rate = 0%
- Предупреждение в Events Manager о несоответствии ID

**Возможные причины:**
1. Неправильный формат ID (число vs строка)
2. Используется неправильное поле (id vs external_id)
3. Товары не синхронизированы с каталогом

**Диагностика:**
1. Проверить формат ID в каталоге Facebook
2. Проверить какой ID отправляется в событиях (через Pixel Helper)
3. Сравнить форматы

---

## 📝 ЧЕКЛИСТ ПЕРЕД ИСПРАВЛЕНИЯМИ

### Информация о каталоге:
- [ ] Формат ID в каталоге известен
- [ ] Способ синхронизации товаров известен
- [ ] Проверено что все товары в каталоге

### Информация о пикселе:
- [ ] Pixel ID правильный: 823958313630148
- [ ] Пиксель подключен к правильному аккаунту
- [ ] Каталог подключен к пикселю

### Информация о событиях:
- [ ] Проверено какие события должны отправляться
- [ ] Проверено какие события сейчас отправляются
- [ ] Выявлены расхождения

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### Шаг 1: Собрать информацию
1. Ответить на критические вопросы (формат ID, синхронизация)
2. Провести тесты (загрузка пикселя, отправка событий)
3. Проверить Catalog Match Rate

### Шаг 2: Подготовить исправления
1. Исправить формат Content IDs (если нужно)
2. Исправить загрузку PageView
3. Убрать дублирование AddToCart

### Шаг 3: Протестировать
1. Протестировать все события
2. Проверить в Meta Pixel Helper
3. Проверить в Events Manager

### Шаг 4: Развернуть
1. Развернуть исправления на продакшн
2. Мониторинг в течение 24-48 часов
3. Проверить Catalog Match Rate через несколько дней

---

**Статус:** ⏳ ОЖИДАНИЕ ОТВЕТОВ НА КРИТИЧЕСКИЕ ВОПРОСЫ

**Следующий шаг:** Ответить на вопросы в разделе "Критические вопросы" выше

