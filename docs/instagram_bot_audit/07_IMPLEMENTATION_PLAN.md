# 07_IMPLEMENTATION_PLAN — план внедрения

> **Канонический per-task статус после recovery/deploy 2026-08-05.**
> Всего 104 уникальные `IMP-*`: **79 закрыты, 21 открыта, 4 partial**
> (`IMP-043`, `IMP-081`, `IMP-082`, `IMP-083`). Решения — в `04_DECISION_LOG.md`, находки и evidence —
> в `03_FINDINGS_REGISTER.md`, общий порядок продолжения — в `00_PROGRESS.md`.
> Ниже находятся отдельные checkbox-матрицы всех 179 `F-*` и всех 51 `IMPR-*`:
> `[x]` означает verified completion, `[ ]` — любой незавершённый остаток,
> включая `PARTIAL`, `REFRAMED` и decision-gated работу.

## Ключевой принцип версии 2: фикс и улучшение — одним срезом

В версии 1 план был списком исправлений. Это неверно операционно: правя текст
сообщения о ТТН ради локализации, нелогично не добавить туда срок доставки и
гарантию обмена — это тот же файл, тот же тест, тот же деплой.
Поэтому каждая задача ниже содержит **и** устранение дефекта, **и** улучшение
того же места, когда они затрагивают один код.

Исключение — задачи, где улучшение меняет поведение безопасности или контракт:
такие разделены осознанно (например, guard'ы отдельно от восстановления cron).

## Что определило порядок

1. **Система почти не работает как автоматизация:** 149 входящих webhook → 16 ответов,
   115 отклонённых по подписи, поллинг платежей мёртв с 8 июля,
   `IgOrderAssignment` = 2 на 289 клиентов, `purchases_count` = 0 у всех,
   атрибуция рекламы = 0 у всех.
2. **Два новых P0 по безопасности** обнаружены в домене L: анонимное удаление
   данных клиента и стирание логов по подстроке. Они не связаны с ботом
   функционально, но это утечка/разрушение данных, поэтому идут в самое начало.
3. **Шесть машин состояний без арбитра** (F-STATE-001) — корень целого класса
   симптомов, а не отдельный баг.
4. **UX привязки заказа блокирует post-purchase автоматику**, поэтому поднят с P2 до P1.

## Сводка волн

| Волна | Содержание | Всего | Закрыто | Открыто | Partial |
|---|---|---:|---:|---:|---:|
| **W0** | Диагностика без изменения кода | 4 | 4 | 0 | 0 |
| **W1** | Безопасность данных | 3 | 3 | 0 | 0 |
| **W2** | Проходимость контура и деньги | 5 | 5 | 0 | 0 |
| **W3** | Истина о покупателе + скоринг + паттерны | 8 | 8 | 0 | 0 |
| **W4** | Доставка сообщений клиенту | 5 | 5 | 0 | 0 |
| **W4C** | Диалог ведёт модель, а не скрипт | 10 | 10 | 0 | 0 |
| **W4D** | Echo/media и автоматическое снятие takeover | 4 | 4 | 0 | 0 |
| **W4B** | Добивка воронки | 13 | 13 | 0 | 0 |
| **W5** | Качество продавца, каталог и память | 9 | 7 | 2 | 0 |
| **W6** | Арбитр состояния и воронка | 5 | 5 | 0 | 0 |
| **W7** | UX админки | 6 | 6 | 0 | 0 |
| **W8** | Наблюдаемость, аналитика, долг | 15 | 5 | 9 | 1 |
| **W9** | Product reselection и коммерческая семантика | 9 | 1 | 5 | 3 |
| **W10** | Неучтённые улучшения: follow-up, retention и аналитический UX | 4 | 0 | 4 | 0 |
| **W11** | Полное покрытие находок и orphan backlog | 2 | 1 | 1 | 0 |
| **W12** | Доказуемая доставка follow-up и event-driven continuation | 2 | 2 | 0 | 0 |
| **Итого** | | **104** | **79** | **21** | **4** |

---

## W0 — Диагностика (без изменения кода) ✅ ЗАКРЫТА 2026-08-01

Все четыре вопроса закрыты. Evidence — `03_FINDINGS_REGISTER.md`, раздел «Волна W0».
Изменения плана по итогам — `04_DECISION_LOG.md`, DR-005.

- [x] **IMP-001** — `bad_signature` (F-CORE-009): **реальный отказ ingress
  24–30.07, ~2268 отклонённых POST от Meta** (`facebookexternalua`, IPv6 Meta),
  26.07 — 100% отказ. **Уже устранён** коммитами 29–30.07; август — 0 отказов.
  В БД видно было 5% инцидента из-за `LOG_KEEP_ROWS=500` → новая F-OPS-004.
- [x] **IMP-002** — рекламных полей нет **ни в одном** из 438 payload'ов
  (полная инвентаризация ключевых путей). Гипотеза (A) подтверждена:
  Meta не присылает, код не виноват. → IMP-043 понижен до P3.
- [x] **IMP-003** — **100% HTTP 404**; 73 из 97 ошибок в день массового импорта
  74 исторических вложений. Не потеря свежих фото. → F-DATA-011 P1 → P2.
- [x] **IMP-004** — не активный баг: все 26 строк созданы до миграций 0077 (10.07)
  и 0081 (22.07), пустые поля — дефолты миграции. Кластер соответствует
  версии кода 14.06–10.07 с attempts-агностичным fail-fast на permanent-ошибке.
  → только инвариант-тест, данные не трогать.

**Побочный вывод, важнее самих ответов:** бот не мог отправлять 14.06–10.07
и не мог принимать 24–30.07. F-CORE-011 объясняется инфраструктурой,
а не качеством ответов. DR-004 усилен.

**⚠️ Перед началом W2 повторно проверить долю 403 на `/bot/webhook/`** —
вывод «здоров» опирается на 11 запросов августа.

---

## W1 — Безопасность данных (P0) ✅ ЗАКРЫТА 2026-08-01

Внедрено на базе `b450b5c2`, ветка `codex/ig-bot-w1-data-safety`.
Evidence — `03_FINDINGS_REGISTER.md`, раздел «Волна W1». Права reviewer — DR-006.

- [x] **IMP-005 (P0)** — закрыто анонимное удаление данных (**F-SEC-002**).
  Заменить прямое удаление в `/data-deletion/submit/` на заявку с подтверждением
  владения; soft-delete с окном отмены; отдельный rate-limit; неудаляемый audit.
  **Внимание:** тест `tests_ig_privacy_policy.py:203-231` закрепляет текущее
  поведение как ожидаемое — его надо менять осознанно.
  Образец безопасного пути уже есть рядом: `/data-deletion/request/` с HMAC.
- [x] **IMP-006 (P0)** — удаление логов только по структурированной связи, не по
  `detail__icontains` (**F-SEC-003**). Заодно перестать писать текст сообщений
  клиента в `InstagramBotLog.detail` — это закрывает и PII-проблему (F-SEC-009).
- [x] **IMP-007 (P1)** (по DR-006: три уровня прав, не полный read-only) — reviewer в sandbox (F-SEC-004): убрать у него
  глобальный старт/стоп, смену модели и `ai_enabled`, действия над реальными
  карточками и доступ к PII-списку и консоли с перепиской.

---

## W2 — Проходимость контура и деньги ✅ ЗАКРЫТА 2026-08-01

Порядок соблюдён: IMP-008 задеплоен до восстановления cron IMP-009.
Evidence — `03_FINDINGS_REGISTER.md`, раздел «Волна W2».

- [x] **IMP-008 (P0)** — guard'ы в `notify_shipped_deals` (F-CORE-001).
  Добавить `is_enabled`, `bot_paused`, `manager_takeover`, `is_blocked`,
  `hidden_at`, opt-out + `customer_send_boundary`. Эталон рядом —
  `ig_order_fulfillment.deliver_event:271-306`.
  **Блокер для IMP-009:** восстановление cron оживит эту функцию, и без guard'ов
  бот начнёт писать тем, кто просил не писать (риск для Meta-приложения).
- [x] **IMP-009 (P0)** — восстановлен cron `poll_ig_deal_payments` (F-OPS-001)
  с `flock` по образцу трёх существующих задач. Возвращает backstop оплаты
  при недоставленном webhook.
- [x] **IMP-010 (P0)** — платежи по замещённым инвойсам не теряются (F-PAY-001):
  сохранять историю `invoice_id`, искать сделку по ней в `handle_webhook_invoice`,
  алерт менеджеру при неизвестном инвойсе. Отмена инвойса в Monobank — вторым слоем.
- [x] **IMP-011 (P0)** — снята слепота деградации промпта (F-AI-001, F-AI-002):
  заменить `except: pass` на `log("error", ...)` для каталога, базы знаний,
  playbook, base64 изображений, `pin_product`.
- [x] **IMP-012 (P1, переформулирован после W0 — см. DR-005)** — **подпись НЕ трогаем**
  (дефекта нет, риск регрессии в стабилизированном месте). Вместо правки ingress:
  сохранять сырое тело до `json.loads` и логировать битый payload (F-CORE-002);
  подключить логгер `ig_bot` к handler (F-SEC-007); алерт на долю 4xx по
  `/bot/webhook/` за окно (F-OPS-004 — иначе следующий отказ ingress снова
  будет ненаблюдаем) остаётся за `IMP-041`.
- **IMP-004-тест (P2, дёшево)** ✅ — закрепить инвариант
  `failed ⇒ (attempts >= MAX_ATTEMPTS) OR (send_state != '')`.
  Единственная защита от повторного появления attempts-агностичного пути.

---

## W3 — Истина о покупателе, скоринг, паттерны ✅ ЗАКРЫТА 2026-08-02

**Критерий приёмки волны (проверяемый на живых данных):** карточка клиента #59
показывает «оплачено · обмін XL у дорозі» вместо «cold · Підтримка / скарга · 0%».
Первая половина критерия («оплачено») достигнута в IMP-013/014, вторая
(«обмін XL у дорозі» вместо «скарга») — задача IMP-015.

- [x] **IMP-013 (P0) — закрыта 2026-08-02** (коммит `3d7f8a46`, задеплоено) —
  разделить «оплачено провайдером» и «клиент является покупателем»
  (F-DATA-005, F-SCORE-004, F-SCORE-005).
  **Реализация отклонилась от буквы DR-001 — см. DR-007.** Рецепт
  «расширить `client_has_verified_payment`, переиспользовав
  `manual_confirmation_q`» оказался неисполним: предикат привязан к `IgDeal`,
  а на проде у всех 28 review `deal_id IS NULL` (F-DATA-013), и сам
  `client_has_verified_payment` читают **два денежных пути** (F-PAY-012).
  Сделано: новый `client_has_confirmed_purchase` + `annotate_confirmed_purchase`,
  строгий предикат не тронут, 9 CRM-читателей переключены,
  агрегаты считаются по различным заказам, `order_builder` перестал
  дублировать запись (F-DATA-014), команда `backfill_ig_buyer_truth`
  (по умолчанию отчёт, запись только с `--apply`).
  **Факт на проде:** 2 покупателя из 289, #59 → 1 покупка / 2100.00.
- [x] **IMP-014 (P1) — закрыта 2026-08-02** (тот же коммит — без IMP-013
  ненаблюдаема) — снято принудительное понижение `paid → checkout`
  (F-SCORE-003) там, где оплату подтвердила сама система. Найден и устранён
  **второй, недокументированный дубль** понижения в карточке клиента.
  **Проверка выполнена:** до правки `score_band='paid'` — 0 записей из 1945
  снапшотов прода; после — `_analysis_band(#59)` = `paid`.
- [x] **IMP-015 (P0) — закрыта 2026-08-02** — обмен и возврат перестают быть
  жалобой (F-SCORE-002, F-SCORE-006). Новые `interaction_type`
  `exchange_request`/`return_request` (миграция `0122`), порядок проверок
  «сначала покупка, потом постпродажный тип», новый тон `service` вместо
  красного «скарга», `objection=SIZE` не ставится постпродажному обращению.
  Попутно найдено: `RETURN_RE` не ловил «поверніть/верніть» — самую частую
  форму просьбы о возврате. Плюс исправлен побочный дефект IMP-014: блок
  «при подтверждённой оплате ставим PAID» затирал более конкретный тип.
- [x] **IMP-016 (P0) — закрыта 2026-08-02** — не продавать клиенту с открытым
  сервисным кейсом (F-SCORE-009 + F-CTX-002). Гашение на трёх уровнях, как
  требовала F-CTX-002: suppression follow-up (`service_case_open`), тег `sales`
  и `discount` не подаются в роутинг инструкций, в промпт вместо
  `SALES_AUTOMATION_GUARDRAILS` идёт `POST_SALE_SERVICE_GUARDRAILS`.
  Открытым считается любой нетерминальный статус, включая `in_transit`.
- [x] **IMP-017 (P1) — закрыта 2026-08-02** — ранжирование признаков вместо
  каскада `if/elif` (**F-PAT-001**). Явная таблица `INTENT_PRIORITY`, все восемь
  конфликтов закрыты red-тестами.
  **Найдена причина серьёзнее описанной в реестре — F-PAT-002:** `DELIVERY_RE`
  и `PREPAY_RE` построены как `\b(корень)\b` и не матчат «доставка»,
  «відправка», «передоплата», «наложкою» вообще. Подтверждено данными прода:
  `intent=delivery` — 0 клиентов из 289, `objection=prepayment` — 0,
  сигнал `prepayment_objection` — 0 из 989.
- [x] **IMP-018 (P1) — закрыта 2026-08-02** — терминальные факты важнее
  последней догадки модели (F-SCORE-008, F-STATE-008).
  **Оба механизма отклонились от формулировки — см. DR-008.** Гейт «кейс только
  покупателю» был реализован и отменён: `IgOrderAssignment` = 2 записи на 289
  клиентов, поэтому он ломал главный рабочий сценарий. Вместо него —
  трёхслойный гейт по формулировке плюс состоянию клиента. Вместо «выбора
  другого снапшота» — наложение факта на самый свежий (`_display_interaction_type`),
  потому что снапшот с правильным band'ом является и более старым.
  Плюс закрыта **F-UX-014** (наблюдение заказчика): бейдж обмена светился
  вечно, теперь гаснет на терминальном статусе и показывает статус.
- [x] **IMP-019 (P1) — закрыта 2026-08-02** — честная подпись метрики + бейдж
  покупателя (F-SCORE-001, DR-002 исполнено буквально: семантика не менялась).
  «ймовірність» → «намір купити зараз» с тултипом; бейдж
  `Вже купив · N · сума ₴` в списке и в шапке диалога; происхождение суммы
  названо (менеджер против провайдера), неизвестная сумма не показывается
  вместо `0.00`.
- [x] **IMP-062 (P1, новая задача) — закрыта 2026-08-02** — ТТН обмена
  привязывается к тому же заказу (прямой запрос заказчика, **F-OPS-006**).
  У клиента #59 три реальные посылки, в БД — одна; две жили цифрами в тексте
  сообщений, потому что `Order.tracking_number` — одно скалярное поле без
  истории во всём проекте.
  Сделано: append-only `IgOrderShipment` (миграция `0123`) с `direction`
  и `purpose`, автоматический вывод ноги обмена из смены ТТН при открытом
  кейсе, ручное подтверждение ТТН возврата (число в сообщении не доверяем),
  новый `Kind.EXCHANGE_SHIPPED` с текстом про заміну/розмір/«доплачувати
  не потрібно», таймлайн отправок в карточке, команда
  `backfill_ig_order_shipments`. Обоснование выбора против «второго заказа
  на замену» — **DR-009**.

---

## W4 — Доставка сообщений клиенту: фикс + тексты одним срезом

- [x] **IMP-020 (P1) — закрыта 2026-08-02.** Разблокирована привязка заказа (F-OPS-003, F-UX-003):
  переиспользовать **уже существующий** поиск кандидатов
  (`bot_order_candidates_api`, рабочий пример в payment-drawer `bot.html:1643-1654`)
  вместо ручного ввода точного номера; развести ⇄ и ⚙ (F-UX-002).
  **Это разблокировка автоматики, а не косметика:** `IgOrderAssignment`=2 на 289.
- [x] **IMP-021 (P0) — закрыта 2026-08-02.** Подтверждение оплаты клиенту (F-PAY-007 + IMPR-TXT-001):
  новый `Kind` в `IgOrderCustomerEvent` через существующий путь
  `ig_order_fulfillment` (там уже есть идемпотентность, guard'ы, lease и
  локализация uk/ru/en). Текст — из IMPR-TXT-001, с резюме заказа и окном на правки.
- [x] **IMP-022 (P1) — закрыта 2026-08-02.** Локализация + улучшение денежных текстов
  (F-PAY-009 + IMPR-TXT-002/003): ссылка на оплату (резюме заказа, «Монобанк»,
  «скрин не потрібен»), ТТН (факт пути, tracking URL, 1-3 рабочих дня),
  локализованный fallback и repeat-guard. ТТН не содержит заявлений об оплате,
  доплате или проактивного предложения обмена.
- [x] **IMP-023 (P1) — закрыта 2026-08-02.** Follow-up: локализованные тексты,
  наблюдаемый статус ссылки, следующий шаг не раньше 18 часов, manager task
  вне автодоставки и исправленный род (IMPR-TXT-005 + IMPR-FEAT-006).
- [x] **IMP-024 (P0) — закрыта 2026-08-02, выбран вариант B.** Staff-only
  ручная выдача одноразового промокода 10% после проверки UGC evidence для
  полученного заказа (`Order.status=done`). Один evidence/один заказ,
  идемпотентность и аудит менеджера; автоматического Instagram/Meta-сообщения нет.

---

---

## W4C — Диалог ведёт модель, а не скрипт ✅ ЗАКРЫТА 2026-08-02

Волна не планировалась. Она появилась из прямой жалобы заказчика: бот не выдаёт
ссылки, бесконечно просит размер, отвечает украиноязычному клиенту по-русски и
на вопрос «почему по-русски?» повторяет ту же фразу дословно.

Все находки воспроизведены на живых переписках прода (клиенты #2 и #5), evidence —
`03_FINDINGS_REGISTER.md`, раздел «Волна W4C».

**Принцип, который волна закрепляет:** детерминированный слой отвечает за
**факты и гарантии** (что выбрано, что доступно, можно ли создавать счёт),
формулирует всегда модель. Фиксированный текст допустим только там, где
отправляется факт (ТТН, подтверждение оплаты) или где ответа модели физически
нет (провайдер недоступен).

- [x] **IMP-063 (P0) — 92% webhook отклонялось (F-CORE-012).** `webhook_secrets()`
  возвращала один секрет из двух настроенных на проде. Access-log: **496 ответов
  403 против 44 успешных**, все отклонённые — от Meta. Это и есть причина
  «бот почти не отвечает» (27 ответов модели на 1025 сообщений клиентов),
  а не качество промпта. **Вывод W0 «ingress здоров» опровергнут.**
- [x] **IMP-064 (P0) — шаблон больше не подменяет ответ модели (F-AI-014).**
  Возврат готовой фразы из `_ASSISTED_CHECKOUT_COPY` вместо ответа Gemini убран.
  Подменённый текст сохранялся в историю как «ответ модели», поэтому следующая
  генерация продолжала чужой скрипт — контур самоусиливался. Таблица копий
  сокращена до двух ключей, сопровождающих реальную ссылку.
- [x] **IMP-065 (P0) — модель узнаёт состояние заказа до генерации.** Новый
  `services/ig_checkout_readiness.py` считает факты (товар, фасон и доступные
  варианты, размерная сетка выбранного фасона, выключенные размеры, цвета,
  количество, статус ссылки, чего именно не хватает) и кладёт их в промпт блоком
  `[СТАН ОФОРМЛЕННЯ]`. Модель сама решает, что спросить, своими словами и на
  языке клиента. Отдельным пунктом в блоке — прямой запрет повторять предыдущий
  вопрос дословно.
- [x] **IMP-066 (P0) — язык определяется честно (F-PAT-004).** `detect_language`
  больше не возвращает «ru» при неопределённости; наборы маркеров расширены;
  добавлен разбор прямой просьбы о языке, которая перекрывает и детектор, и
  гистерезис. В промпт идёт и сохранённый язык, и язык последних сообщений, и
  явное указание на расхождение.
- [x] **IMP-067 (P0) — наличие приведено к паритету с сайтом (F-CAT-002).**
  `stock > 0` был у 1 варианта из 81, поэтому IG-checkout отказывал по 70
  товарам из 71, тогда как сайт продаёт все. Нуль теперь означает «учёта нет»,
  а не «нет в наличии». Проверено на живых данных: 13 из 15 товаров проходят
  валидацию, остальные два просят уточнить цвет. Связанный catalog-completeness
  срез `3191e08c` поднял бюджет до 48k, вернул все 71 товара и режет только по
  целым строкам с честным счётчиком (IMPR-CAT-005).
- [x] **IMP-068 (P1) — выбор клиента сохраняется между ходами (F-CORE-013).**
  `persist_control_selection` фиксирует `[FIT]`, `[SIZE]`, `[QTY]` на каждом
  ходу, а не только когда в этом же ходу создаётся ссылка. Это и был механизм
  бесконечного уточнения: ответ клиента нигде не оставался.
- [x] **IMP-069 (P1) — ссылка клиента на товар читается как выбор (F-CORE-014).**
  Доверенный разбор URL storefront с отказом на похожие домены; факт идёт в
  промпт, смену товара подтверждает модель тегом `[PRODUCT:id]`. Требование
  «слова о покупке» для пина снято — именно оно мешало сменить товар обратно.
- [x] **IMP-070 (P1) — информационный ход не меняет платёжный выбор (F-PAT-005).**
  Фасон приходит только явным тегом модели. Вопрос «покажи на оверсайз размерную
  сетку» больше не переписывает выбор и не запускает выставление счёта.
- [x] **IMP-071 (P1) — повторная покупка разрешена (F-PAY-013).** Пожизненный
  запрет по `client_has_verified_payment` заменён на защиту от дубля счёта по
  текущей оплаченной сделке без заказа.
- [x] **IMP-072 (P1) — обещание «уточню у менеджера» стало правдивым.**
  `notify_size_gap` отправляет менеджеру запрос отсутствующего размера
  (дедуп на сутки по клиенту, товару, фасону и размеру). Раньше бот мог обещать
  уточнение, о котором никто не узнавал.

**Критерий приёмки волны (проверен на живых данных прода, read-only):**
для клиента #2 в промпте видно «бракує: фасон» и явное расхождение языка
(«збережено російська, а клієнт щойно писав українська»); для клиента #5 —
«бракує: розмір» и распознанная ссылка на `classic-tshirt` как другой товар;
валидацию заказа проходят 13 из 15 проверенных опубликованных товаров.

**Тесты:** 30 новых (`tests_ig_agentic_dialog.py`), 7 существующих переписаны
осознанно (закрепляли подмену ответа как ожидаемое поведение), 2 переписаны в
`tests_ig_checkout_service` (закрепляли нулевой сток как «нет в наличии»).
Регрессионный прогон IG-домена: 635 тестов, список падений **побайтово совпадает**
с чистым `origin/main` (32 предсуществующих, причина — лимит потоков хостинга).

**Что осталось открытым:** F-OPS-008 (лог живёт 4 часа) и F-DATA-015 (ответы
бота помечены как сообщения менеджера) — в W8. IMPR-FUP-013 (follow-up тоже
отдать модели) теперь имеет IMP-090; IMPR-INV-001 (склад как источник наличия)
покрыт интеграционной цепочкой IMP-081/084/086.

---

## W4D — Бот замолкал сам на себя ✅ ЗАКРЫТА 2026-08-02

Волна выросла из наблюдения заказчика: два клиента подряд попросили показать
футболки, получили по два фото и после этого — тишину. У клиента #2 фото пришли
вообще без подписи. Evidence — `03_FINDINGS_REGISTER.md`, раздел «Волна W4D».

- [x] **IMP-073 (P0) — своё echo больше не считается менеджером (F-CORE-007/015).**
  Распознавание по `message_id` из ответа Send API вместо отпечатка по тексту:
  у медиа-echo текста нет, поэтому прежняя проверка не работала в принципе.
  Новый `services/ig_outgoing_registry.py` (кэш + БД), поле
  `InstagramBotMessage.provider_message_id`, миграция `0129`. Регистрация внутри
  цикла отправки карусели: echo первого фото может прийти раньше, чем отправится
  второе. DB fallback переживает сбой/eviction кэша и закрывает риск ложного
  auto-takeover из F-CORE-007. Третий слой на случай гонки — активная lease
  автоматики, с записью в лог.
  **Масштаб дефекта:** 57 клиентов из 289 (20% базы) находились в takeover.
- [x] **IMP-074 (P0) — пауза от менеджера снимается сама (F-CORE-016).**
  `maybe_release_stale_takeover`: 12 часов тишины менеджера → бот возвращается,
  менеджер получает уведомление. До этого единственным способом снять паузу был
  ручной клик в админке, и самый старый takeover висел с 19 июня.
  Активный диалог менеджера не прерывается, opt-out сильнее таймаута.
- [x] **IMP-075 (P0) — система помнит, что показала на фото (F-CORE-017).**
  `record_shown_products` фиксирует порядок и `product_id`; блок
  `[НАДІСЛАНІ ФОТО]` в промпте даёт таблицу «позиция → товар». Раньше карусель
  попадала в историю как два одинаковых «Менеджер: (зображення менеджера)», и
  «давай первую» было неразрешимо — модель угадывала и описывала не тот товар.
- [x] **IMP-076 (P1) — фото после уточнения, а не вместо него (F-AI-015).**
  Раздел «ПОРЯДОК ПОКАЗУ ФОТО» в протоколе: сначала один уточняющий вопрос,
  к каждой отправке фото обязателен текст с нумерацией товаров и ценой,
  «звичайна/класична/стандартна» = базовая модель с логотипом, а не принт;
  вместо угадывания можно попросить ссылку с сайта или скриншот.

**Критерий приёмки:** отправка карусели не включает takeover и не съедает текст
ответа; после карусели «давай першу» разрешается в конкретный `product_id`;
клиент, которому менеджер не писал сутки, снова получает ответы бота.

**Тесты:** 16 новых в `tests_ig_agentic_dialog` (`OwnEchoRecognitionTests`,
`StaleTakeoverReleaseTests`, `ShownProductsMemoryTests`, `PhotoProtocolTests`),
2 переписаны в `tests_ig_media_workflow` (закрепляли пожизненный запрет ссылки
покупателю). Регрессионный прогон: **747 тестов IG-домена, 0 падений.**

**Открыто и вынесено дальше:** F-OPS-009 (Telegram-алерты пачками, 12 точек без
дедупа, ссылка в админку у 2 из 31) → W8, задача IMP-077 ниже.
F-AI-016 (инструкции без триггеров, 70% клиентов видят одну инструкцию из семи)
→ W5, задача IMP-078 ниже.

---

## W4B — Добивка воронки

Полный дизайн с текстами всех сообщений — в `06_FUNNEL_CLOSING_DESIGN.md`.
Прямой запрос заказчика: «добить покупателя до конца».

**Блокирующее противоречие, определившее порядок:** заказчик хочет сообщение
«у вас закінчилось посилання», но у ссылки **нет TTL** и истечение
ненаблюдаемо. Сначала делаем факт наблюдаемым, потом строим на нём каскад —
иначе бот будет утверждать неправду (сейчас он говорит «посилання ще активне»,
не проверив ничего).

### Порядок внутри волны обязателен: 047-049 → 050-052 → 053 → остальное

- [x] **IMP-047 (P0) — закрыта 2026-08-02.** `Kind.FINAL` 10% достижим (F-FUP-001):
  передавать `explicit_negotiation` для `Objection.PRICE`, разрешить rescue
  для `NEW`/`QUALIFYING`. Сейчас финальный офер не отправлялся ни разу,
  а ветка `pct == 10` — мёртвый код.
- [x] **IMP-048 (P0) — закрыта 2026-08-02.** `deal=` передаётся в `schedule_after_bot_reply` (F-FUP-002):
  оживляет мёртвую ветку и снимает зависимость от `set_stage` в `try/except`.
- [x] **IMP-049 (P0) — закрыта 2026-08-02.** Задача не создаётся сразу `SKIPPED` при выходе за окно Meta
  (F-FUP-003): делать `MANAGER_TASK` со `status=PENDING` + пометка дедлайна.
  Сейчас ≈половина «подумаю»-добивок исчезает молча и не видна как работа.
  Плюс добавить метку `meta_window_closed` в UI.
- [x] **IMP-050 (P0) — закрыта 2026-08-02.** TTL ссылки: `validity=86400` в payload +
  `IgDeal.invoice_expires_at` + задача на `T+23ч` **в момент выдачи**
  (не зависеть от опроса) + guard «reused только если ссылка ещё жива».
- [x] **IMP-051 (P0) — закрыта 2026-08-03, production `2a89d860`.**
  Payment backstop вызывается каждым циклом демона независимо от флага
  автоответов; cron и daemon используют общий mutex + cadence lease. Внешний
  batch ограничен 50 запросами, mutex покрывает worst-case provider timeout.
  Queryset больше не зависит только от `AWAITING_PAYMENT`: проверяет
  `draft/quoted/awaiting_payment`, но исключает заказ, терминальные статусы и
  терминальную авторитетную `IgPaymentProjection`. `--check-only` использует
  тот же queryset. Production MySQL: единственный invoice имел truth
  `cancelled` и после исправления не попадает в polling (`provider_invoices=0`).
  Проверено 160 связанными тестами; daemon `running`, heartbeat 0.4 с.
- [x] **IMP-089 (P1) — закрыта 2026-08-03, production `eaef5701`.** Для каждого
  superseded invoice создан bounded `IgDealInvoiceLifecycle` (migration `0134`):
  webhook и backstop используют один per-invoice ledger, terminal marker,
  expiry/age cap и идемпотентный manager alert. Старый invoice опрашивается с
  `apply=False`, поэтому найденная оплата не применяется автоматически к новой
  конфигурации товара/оплаты. Legacy JSON materialization выполняется
  ограниченной пачкой. Production `poll_ig_deal_payments --check-only --limit
  50`: `projections=0 provider_invoices=0 superseded_invoices=0 orders=0`;
  migration `0134` применена, lifecycle rows = 0 (исторических superseded ID
  нет), daemon после transient worker error восстановлен в `running=True`.
- [x] **IMP-052 (P0) — закрыта 2026-08-02.** Suppression расширен до полного списка (F-FUP-010,
  F-CTX-002): `opted_out_at` как самостоятельный флаг, `support_complaint`,
  `wholesale_b2b`, `collaboration`, `LEAD_TO_MANAGER`, `COLD`, открытая
  претензия, **частотный лимит** (≤1 автосообщение в 18 ч, ≤5 за 30 дней)
  и дедуп текста. **Строго до IMP-053:** удлинение каскада без лимитов = спам
  и риск для Meta-приложения.
- [x] **IMP-053 (P0) — закрыта 2026-08-03, production `cd070cba`.**
  Follow-up переведён с трёх веток `if/elif` на policy-таблицу из **9 сценариев
  и 25 шагов**: абсолютный offset, `trigger`, `kind`, `condition`, `copy_key`,
  скидка и явные terminal conditions. Резолвер сначала читает факты сделки,
  оплаты, доставки и дефицита, затем CRM-стадию/возражение. После отправки
  планируется следующий шаг policy; изменившийся факт отменяет сообщение;
  безопасное исчерпание переводит клиента в `COLD` через FSM с причиной.
  Лимиты IMP-052 не ослаблены: шаги, которые не помещаются в 18-часовой лимит
  или окно Meta, остаются видимыми `MANAGER_TASK` с готовым текстом. Встроены
  тексты uk/ru/en и переход `delivery_ready_unpaid -> payment_link_unpaid/A2`.
  **Граница закрытия:** настоящий `Kind.FULFILLMENT` остаётся в IMP-055; его
  event/claim контракт теперь закрыт IMP-056.
  Проверено: 15 policy-тестов и 119 связанных тестов; на production MySQL
  загружены 9 policies/25 steps, daemon `running`, transport `instagram_login`.
- [x] **IMP-054 (P1) — закрыта 2026-08-02.** Quiet hours (F-FUP-006/007): инициация 10:00–21:30,
  аварийное окно 09:00–22:30 ради окна Meta, реактивный ответ 24/7,
  целевые вечерние слоты для задач с горизонтом ≥20 ч, вынести в настройки
  (сейчас две несогласованные конфигурации тишины).
- [x] **IMP-055 (P1) — закрыта 2026-08-03, production `efc0ee10`.** Добавлен
  настоящий `Kind.FULFILLMENT`: G1 +20 минут, G2 +3 часа, G3 +20 часов и
  idempotent Telegram-эскалация менеджеру со ссылкой. Verified payment и
  sales frequency limit больше не подавляют сбор НП, но hidden/spam,
  takeover/pause, opt-out, service case, payment reversal и Meta-window
  остаются fail-closed. `on_deal_paid()` создаёт G1 идемпотентно; заполнение
  доставки или создание заказа отменяет остаток как
  `policy_condition_changed`; fulfillment не повышает sales follow-up level
  и скидку. Проверено 98 связанными тестами. Production после gate-fixes на
  `4ba4212d`: MySQL, migration `0130`, daemon `running`, `last_error` пуст;
  активных оплаченных сделок без доставки сейчас 0, поэтому pending
  fulfillment тоже 0.
- [x] **IMP-056 (P1) — закрыта 2026-08-03.** Для `IgFollowUpTask` добавлены
  nullable-unique `event_key`, двухфазные `claim_token`/`claim_until` и
  сохранённый provider `message_id`. `process_due_followups` теперь получает
  structured receipt, не повторяет unknown outcome, атомарно захватывает задачу
  после client lease и очищает claim в `finally`. Событие истечения ссылки
  материализуется из `invoice_expires_at` с ключом `invoice_expired:<deal>:<invoice>`;
  восстановление restock запускается readiness-проверкой после появления размера
  и использует ключ с фактом gap. Активная задача дедуплицируется по
  `(client, kind, level)` на scheduling-слое, event/manager continuation идёт
  по той же policy-таблице. Тесты: idempotent expiry/restock, receipt persistence,
  claim cleanup; миграция `0131`.
- [x] **IMP-057 (P1) — закрыта 2026-08-03, production `d0098d0b`.** Возражения
  как жизненный цикл (F-OBJ-001…008):
  развести вопрос о цене и возражение по цене; убрать одиночные `s|m|l`
  из детекта; добавить `thinking_objection` и 8 недостающих типов;
  модели `IgObjection` + `IgObjectionAttempt`; тег `[OBJHANDLE:type:method]`
  с валидатором отпечатков; блок `[ЗАПЕРЕЧЕННЯ]` в промпт (не повторять
  неработающий метод); 12 `BotInstruction` с готовыми текстами. Дополнительно
  закрыты compound-turn, ложный `PURCHASED` по `CHECKOUT_STARTED`, тихий сбой
  lifecycle и rollback MODEL-ledger. Production: migration `0132`, обе таблицы
  `InnoDB`, daemon `running`; 23/23 новых и 147/147 связанных тестов.
  **Инвариант:** в `handled` переводит только `verified=True`, иначе метрика
  «закрыто» станет самообманом.
- [x] **IMP-058 (P1) — закрыта 2026-08-03, production `92d46c5a`.** Статистика
  переходов и падений (F-STAT-001…004) теперь использует append-only
  `IgFunnelStepEvent` и `IgFunnelDropOff`, записанные в той же транзакции,
  что и мутация состояния. Cohort-аналитика режется по `occurred_at`,
  drop-off разделяет silence/refusal/unreachable/spam/opt-out/superseded,
  а silence измеряется в рабочих часах Kyiv. Добавлены dashboard/API блоки
  причин, времени на шаге, discount и manager-vs-bot, плюс идемпотентные
  `backfill_ig_funnel_events` и `scan_ig_funnel_dropoffs`.
  Фактически в модели **17 типов событий**, потому что к исходным 16
  добавлен отдельный `payment_confirmed`; это не скрытое расхождение дизайна.
  Production proof: migration `0133` применена на MariaDB, backfill создал
  5 канонических событий, silence scan материализовал 96 drop-off фактов;
  MySQL/API reconciliation видит 197 events, 96 drop-offs и 17 event types.
  Regression: 53 funnel/follow-up, 161 analysis/inbox/intelligence и 103
  commercial/funnel tests; `manage.py check`, migration drift и compileall
  зелёные.

**Критерий приёмки волны:** для клиента с выданной и неоплаченной ссылкой
в логе видны 5 касаний по расписанию из дизайна, каждое — с подтверждённым
статусом ссылки; после явного отказа касания прекращаются навсегда;
в дашборде виден шаг с максимальной потерей и причина отвала.

## W5 — Качество бота как продавца

- [x] **IMP-080 (P0) — ЗАКРЫТА 2026-08-02. Единая цена варианта в речи и checkout
  (F-CAT-003).** Семь каталоговых читателей, включая каталог, readiness,
  распознавание фото, рекламную память, product-decision и payment review,
  читали только `Product.final_price`. Основной assisted checkout уже считал
  `effective_cart_unit_price(product, variant, fit, option_values)`, но legacy
  paylink мог передать `variant=None`. На production это давало 1090 грн в речи для
  `product_id=110`, но 1450 грн в сформированном термо-варианте `variant_id=81`.
  Добавлен общий `services/ig_catalog_pricing.py`: он перечисляет разрешённые
  комбинации через те же `variant_public_context`/`variant_allows_options`,
  отдаёт точную цену или диапазон и причину надбавки. Контекст теперь содержит
  `variant_id`, цену каждого цвета и цену каждого фасона; до выбора
  конфигурации точную сумму называть запрещено. Для обоих paylink-путей сервер
  теперь сам подставляет единственный допустимый variant, сохраняет его в
  `IgDealItem`, а при нескольких вариантах возвращает `missing_color_variant`
  до платёжного провайдера. Тесты покрывают 1090/1450 (обычная/термо), 800/950
  (classic/oversize), неразрешимую матрицу и отсутствие variant ID.
  **Отдельная data-задача:** F-DATA-016 — белая конфигурация товара 110 ещё не
  заведена полным вариантом с изображениями и правилами; пустую строку
  автоматически не создаём. **Production verification:** коммит `8ccac4f9`
  задеплоен в `main`; на MySQL `product_id=110` разрешается как точные 1450 грн
  (`variant_id=81`), `product_id=91` как диапазон 800–950 грн. Принудительно
  пересобранный prompt-каталог содержит те же суммы и причины надбавок. Демон
  работает на `instagram_login`, heartbeat после рестарта — около 1 секунды.

- [ ] **IMP-095 (P1) — production merchandising белого варианта товара 110
  (F-DATA-016).** Создать полноценный `ProductColorVariant` «Білий» за 1090 грн
  только вместе с корректными фото, fit/size rules и default-политикой; проверить
  PDP, bot catalog и assisted checkout на диапазон 1090–1450. Не подменять
  термо-фото белому варианту и не считать это продолжением закрытого IMP-080.

- [x] **IMP-078 (P1) — ЗАКРЫТА 2026-08-02. Инструкции по триггеру, а не всегда (F-AI-016).**
  Прямой запрос заказчика. Сейчас `active_instruction_block(client)` видит только
  срез четырёх CRM-полей и ничего не знает ни о тексте сообщения, ни о сигналах.
  Что нужно, по порядку зависимостей:
  1. **Валидация словаря тегов.** Сейчас опечатка даёт инструкцию, которая
     никогда не сработает, без единого сигнала, а пустые теги означают «всегда» —
     то есть опечатка даёт результат, противоположный намерению. Нужен явный
     перечень допустимых тегов (значения enum'ов + служебные `global`/`core`) и
     проверка при сохранении в UI, с понятной ошибкой.
  2. **Убрать мёртвый маппинг** в `tags_for_client` (`bot_playbooks.py:41-53`):
     ветки для `custom_print`, `payment_pending`, `prepayment`, `price`, `size` —
     no-op, потому что строки `:30-37` уже добавляют значения enum-полей.
     Оставить только те, что действительно добавляют новое (`payment`,
     `discount`, `fit`, `product`, `catalog`). Явная таблица, которая врёт,
     опаснее отсутствия таблицы: на ней уже сгорела правка W3.
  3. **Триггеры от текущего хода.** Передавать в роутинг сигналы этого сообщения
     (`IgConversationSignal` уже пишется) и признак «клиент спросил про X сейчас»,
     а не «в карточке стоит objection=X с прошлой недели». Механизм рядом уже
     заложен и не подключён: `BotQuickLink.trigger_keywords`.
  4. **Исключающие условия** (F-CTX-004): выражать «не давать этот блок при Y»
     разметкой, а не правкой Python. Сейчас единственное исключение (сервисный
     кейс) захардкожено.
  5. **Лимит на блок инструкций** по образцу каталога и базы знаний. Сегодня не
     болит (1.7 KB), но это единственный слой промпта, который админ правит из UI
     без версионирования и без капа.
  **Сделано:** новый `services/bot_instruction_routing.py` — теги `on:<trigger>`
  (срабатывают от текущего сообщения), `not:<tag>` (исключения), валидация
  словаря с предупреждением в UI без блокировки сохранения. Мёртвый маппинг из
  пяти no-op ветвей убран. Лимит блока 6000 символов, режется по целым
  инструкциям. Команда `retag_ig_bot_playbooks --dry-run` перерасмечивает семь
  прод-инструкций, не трогая тексты.
  Разведены **вопрос** о цене и **возражение** по цене — их смешение и было
  причиной того, что rescue-playbook подключался на «скільки коштує».
  Тесты: `tests_ig_instruction_routing`, 17.


**Обязательное предусловие всей волны:** golden-conversations тесты (задача D10,
отсутствуют) + версионирование промпта (F-SEC-006). Без них правки промпта
непроверяемы и необратимы.

- [x] **IMP-025 (P1) — закрыта 2026-08-02 с уточнённым scope.** Append-only
  `BotPromptRevision` версионирует реально редактируемые `BotInstruction` и
  knowledge base: автор, время, diff и откат; сам откат также записывается в
  историю (F-SEC-006).
- [x] **IMP-026 (P1) — закрыта 2026-08-02 с уточнённым acceptance.** Выход
  Gemini в тестах замокан, поэтому property-тесты проверяют фактический prompt
  snapshot до сетевого вызова: стадию, язык, сигналы, сервисный кейс, запрет
  скидки и контекст покупателя.
- [x] **IMP-027 (P1) — закрыта ранее коммитом `770872ec`.** Каталог содержит
  эффективные фасоны/размеры и `variant_id`; используется
  `resolve_effective_sizes`, потому что `resolve_product_sizes` теряет отдельные
  сетки classic/oversize. Остаток по варианту не выдумывается: production-данные
  не ведут stock у вариантов.
- [ ] **IMP-028 (P1) — открыта; отдельные элементы частично реализованы.** Блоки промпта: размерный протокол, гарантия обмена,
  «дорого» без скидки, «подумаю», нет размера, ненавязчивость
  (IMPR-SALES-001/002/004/005/006/008), один вопрос за раз, один аккуратный
  апселл, конкретное закрытие и голос Соломии (IMPR-SALES-003/007/010/011),
  честный дефицит только из фактов (IMPR-SALES-009). Плюс устранение противоречий промпта
  (F-AI-009: язык, три «высших приоритета», скидка) и FAQ через `BotInstruction`
  (IMPR-TXT-006 — ноль кода).
  **Задеплоенный partial 2026-08-04, `042c48c8`:** runtime добавляет единый
  порядок источников, запрещает выдавать единственную цену до выбора variant/
  material/fit, отделяет catalog discount от rescue-discount и ограничивает один
  вопрос/CTA/контекстный upsell. Sales prompt использует compact catalog без
  потери ни одного товара, `variant_id`, цены конфигурации, fit/size или visual
  fingerprint (MySQL: 71/71 строк, 19 696 chars; полный каталог 27 157; prompt
  35 495). Brand/live/directives/links режутся только целыми смысловыми блоками.
  **Не закрыто:** сами сценарные playbooks, FAQ, concrete close/voice и golden
  conversations; поэтому checkbox намеренно остаётся `[ ]`.
- [x] **IMP-029 (P1) — закрыта 2026-08-02.** `[СИГНАЛИ КЛІЄНТА]` передаёт тип,
  confidence и давность фактических сигналов, исключая шумный takeover;
  `[СТАН ДІАЛОГУ]` содержит стадию, липкий язык, размер, повторную покупку и
  открытый сервисный кейс (F-AI-005/006/008).
- [x] **IMP-030 (M) — закрыта 2026-08-02 с суженным scope.** Структурированный
  профиль хранится под служебным ключом `sales_context`, не дублирует колонки
  клиента и добавляет append-only историю возражений. Подтверждённые факты
  переживают сброс воронки (IMPR-MEM-001).

---

## W6 — Арбитр состояния и воронка ✅ ЗАКРЫТА 2026-08-02

Заказчик назвал волну ключевой и потребовал не «вставить», а додумать. Его
постановка шире плана: воронка должна помнить **почему** клиент ушёл с товара, и
это должно менять то, что бот говорит. Такого пункта в плане не было — добавлен
как IMP-079.

Evidence — `03_FINDINGS_REGISTER.md`, раздел «Волна W6».

- [x] **IMP-031 (P1) — арбитр состояния подключён к промпту (F-STATE-001).**
  Модуль `services/ig_client_state.py` был написан ранее в отдельном worktree и
  не опубликован — забран как есть. Новое в этой волне: `client_state_note`
  больше не берёт стадию напрямую из поля карточки, а спрашивает арбитра.
  Раньше промпт получал тот же противоречивый срез, из-за которого клиент #59
  был одновременно `stage=paid` и «cold · Підтримка / скарга · 0%».
  Возврат денег даёт прямой запрет благодарить за покупку; сервисное обращение
  описывается как «сервис, не продажа»; повторный покупатель называется вместе с
  источником истины.
- [x] **IMP-032 (P1) — единый мутатор стадии с FSM (F-STATE-004).**
  `services/ig_funnel_fsm.py`: `reason` обязателен, направление перехода
  вычисляется явно, регресс требует `allow_regress=True`, платёжные стадии —
  `fact_verified=True`. Возврат из `cold`/`spam`/`lead_manager` в воронку не
  считается регрессом: клиент просто снова в диалоге.
- [x] **IMP-033 (P1) — откат при возврате денег (F-STATE-002, F-STATE-003).**
  `bot_payments.apply_payment_reversal_to_stage`: сделка → `CANCELLED`, стадия →
  `checkout` через явный `regress_stage` с актором `payment_provider`.
  Осознанные границы: `failed` не отменяет сделку (человек заплатит со второго
  раза), `partially_refunded` не аннулирует покупку, ниже `checkout` стадия не
  опускается.
- [x] **IMP-034 (P1) — воронка с ветвями (F-STATE-005).** Прогресс сохраняется,
  сервисная ветвь помечается признаком `side_flow` на том шаге, где реально
  происходит. Погашенный прогресс-бар у оплатившего клиента больше не появляется.
- [x] **IMP-079 (P1, НОВАЯ) — журнал выбора товара с причинами (F-STATE-010).**
  Прямой запрос заказчика, которого в плане не было.
  `services/ig_funnel_journal.py`: append-only журнал переходов + производные
  счётчики трения. Причина перехода приходит **от вызывающего слоя**, а не из
  регекса по тексту — это то же правило, на нарушении которого ломался бот всю
  предыдущую волну. Два подряд отказа по наличию переводят бота в другой режим:
  признать, извиниться, не предлагать наугад, передать менеджеру.
  Счётчик обрывается на первой нормальной причине, иначе через месяц каждый
  диалог выглядел бы как жалоба.

**Критерий приёмки:** клиент с оплаченным заказом и обменом в пути показывает
пройденный прогресс и помеченную ветвь, а не 0%; после возврата денег стадия и
сделка откатываются с видимой причиной; после двух отказов по наличию бот
меняет поведение, а не предлагает третий вариант.

**Тесты:** 24 новых (`tests_ig_funnel_journal`), 18 перенесены вместе с
арбитром (`tests_ig_state_arbiter`). Прогон IG-домена: **878 тестов**, падений
по моим изменениям нет. Одно падение
(`tests_ig_order_assignments.test_legacy_attribution_backfill_is_guarded_and_reversible`)
воспроизведено **на чистом дереве без моих правок** — предсуществующий флейк
migration-executor при определённой комбинации модулей, занесён в W8.

---

## W6 — исходный план (для истории)

- **IMP-031 (P1)** — `resolve_client_state(client) -> CoherentState`
  (F-STATE-001): производный read-model с явным приоритетом источников
  (провайдерская оплата > заказ > сервисный кейс > анализ диалога),
  единственный источник для UI и промпта. Закрывает класс конфликтов целиком
  вместо попарной починки шести машин.
- **IMP-032 (P1)** — единый мутатор стадии с FSM-таблицей и явным
  `regress_stage(reason, actor)`; запретить прямое присваивание `stage`;
  stage+event в одной транзакции (F-STATE-004: 5 из 15 переходов без события,
  `set_stage` глотает ошибку записи).
- **IMP-033 (P1)** — откат состояния при возврате денег (F-STATE-003, F-STATE-002):
  `set_stage(CHECKOUT, reason="payment_reversed")` + ветка терминально-негативных
  истин в `_sync_legacy_payment_mirror` → `IgDeal.Status.CANCELLED`.
  Снимает псевдо-стадию `payment_reversed` из UI и расхождение на проде.
- **IMP-034 (P1)** — воронка с ветвями (F-SCORE-011, F-STATE-005/006/007,
  IMPR-UX-003): основной lifecycle + side-flows (обмен/возврат/повтор/follow-up),
  признак `off_funnel` вместо погашенного прогресс-бара, `stage=DONE` начинает
  записываться, `start_repeat_episode` перестаёт откатывать стадию не почистив контекст.

---

## W7 — UX админки ✅ ЗАКРЫТА 2026-08-03

- [x] **IMP-035 (P1)** — desktop >1200 px показывает три самостоятельные
  колонки (список / переписка / контекст); на меньшей ширине контекст остаётся
  modal drawer. Дополнительно закрыта новая **F-UX-016**: мобильный drawer
  поднят выше global header на уровне владеющего stacking context.
- [x] **IMP-036 (P1)** — глобальный стоп, «втрачено», «приховати» и удаление
  KB требуют явного подтверждения (F-UX-005).
- [x] **IMP-037 (P1)** — пустые `.catch(()=>{})` устранены; API-ошибки видимы,
  KB проверяет HTTP/`success`, показывает loading/success/error и не очищает
  введённые поля при неудаче (F-UX-006/009).
- [x] **IMP-038 (P1)** — follow-up отображается относительным временем;
  просрочка имеет отдельный бейдж и величину задержки (F-UX-007, IMPR-UX-006).
- [x] **IMP-039 (P1)** — backend/UI пагинация по 100 строк с границами 20–200,
  prev/next и честным диапазоном «показано X–Y з N» (F-UX-004).
- [x] **IMP-040 (P2)** — hidden/follow-up метрики соблюдают выбранный период;
  UI всегда строит все 12 стадий, сохраняет числовой ноль, даёт определения 11
  KPI, keyboard navigation/ARIA для табов, более читаемый контраст и подписи
  Monobank/менеджера вместо provider/override-жаргона (F-SCORE-013,
  F-UX-008/010/012/013).

**Evidence:** commit `bca7e4e2` находится в `origin/main` и на production.
Связанный прогон — **159/159**; Django check, migration drift, inline JavaScript
и diff check зелёные. Browser 390x844 подтвердил topmost close, body scroll lock,
нулевой horizontal overflow и возврат фокуса; 1440x900 — колонки 320/568/380 px.
Production MySQL API вернул page 1 = 100 строк, диапазон 1–100 из 245,
3 страницы; демон после deploy онлайн с пустым `last_error`.

---

## W8 — Наблюдаемость, аналитика, долг

- [x] **IMP-097 (P1) — закрыта 2026-08-03, production `6b86e103`.** Надёжность
  границы доставки и AI cooldown: speculative echo-маркер удаляется после
  однозначного отказа провайдера, но сохраняется при timeout/5xx/unknown; тот же
  контракт применён к `send_text_tagged`. Cooldown пула Gemini больше не блокирует
  явно настроенный custom key; при отсутствии доступного pooled key сообщение
  возвращается в `PENDING` без сжигания попытки и с повторной попыткой после окна.
  Проверено 45/45 `management.tests_ig_audit_fixes`, `manage.py check`,
  migration drift и compileall; production `enabled=True`,
  `transport=instagram_login`, `last_error=''`.

- [x] **IMP-041 (P1) — закрыта и задеплоена 2026-08-04 (`f2a84717`).** Health-check,
  durable heartbeat каждой production cron-задачи и дедуплицированный alert при
  stale/failure (F-SEC-008). Добавлены `InstagramBotTaskHeartbeat`, migration
  `0135`, явные task-specs, `/bot/health/` и проверка здоровья из daemon.
  Production MariaDB: все пять задач успешно завершены без ошибки; endpoint =
  HTTP 200, `bot_state=running`, `cron_unhealthy=0`.
- [x] **IMP-042 (P1) — закрыта и задеплоена 2026-08-04 (`32985a63`).**
  Custom Direct/Gemini credentials теперь хранятся исключительно как versioned
  Fernet ciphertext; runtime property расшифровывает их только в памяти, а UI
  fail-closed при отсутствующем/некорректном `FIELD_ENCRYPTION_KEY`. Migration
  `0136` конвертирует legacy plaintext без изменения DB-колонок. Production:
  key задан в private `.env.production`, env-файлы `0600`, MariaDB migration
  `[X]`, поля были пусты/ENV provider не затронут, daemon и health `running`.
- [ ] **IMP-101 (P2) — открыта.** Закрыть F-SEC-001 независимо от encryption:
  убрать production account IDs, `allowed_senders` и debug reply из model
  defaults, перенести явные значения в singleton/configuration migration,
  сделать fresh install безопасным (`allowed_senders=''` = all) и добавить UI
  warning для непустого whitelist. Нужны regression на fresh settings и
  production config proof; `IMP-042` этого не делает.
- [ ] **IMP-043 — PARTIAL, разделено после W0 (DR-005):**
  - **(P3, заблокировано)** атрибуция рекламы: источника данных не существует
    (0 рекламных полей в 438 payload'ах). Нужен ответ заказчика — запущена ли
    click-to-message реклама. Парсер писать не на чем.
  - **(P2, делаем независимо)** честная плашка в дашборде: пустая `ad_rows`
    (`bot_views.py:3982-4006`) читается как «реклама дала 0 конверсий»,
    правда — «источник неизвестен ни у одного клиента». Это дезинформация.
  - **(P1, не зависит от рекламы)** разделение bot-only / manager-assisted /
    manager-created (F-SCORE-014 — данные в `IgOrderAssignment.Source` есть,
    но не читаются).
- [x] **IMP-077 (P1) — закрыта и задеплоена 2026-08-04 (`221cf37d`). Telegram-алерты: пачки, дубли, ссылки и terminal outcome
  (F-OPS-009).** Историческая жалоба «сразу спам из 10 штук» была воспроизведена по коду:
  `drain_manager_notifications(limit=10)` вызывается в цикле демона каждые
  1.5 секунды и внутри не имеет ни задержки, ни счётчика — до 20 сообщений за
  проход. Что делать:
  1. **Глобальный частотный лимит на поток** уведомлений (не на событие):
     N сообщений в минуту, остальное ждёт следующего окна. Сейчас лимиты есть
     только у 3 точек из 31.
  2. **`dedupe_key` обязателен.** 12 точек его не передают и получают
     `generic:sha256(text)`. Это даёт одновременно и пачку (разные клиенты —
     разные тексты), и потерю алерта (повтор того же текста не дойдёт **никогда**,
     потому что строки `IgBotNotification` не удаляются и TTL у них нет).
     Ключ должен включать сущность и окно времени, а не только текст.
  3. **Схлопывать однотипное в одно сообщение.** Восстановление cron платежей
     (`poll_pending_deals(limit=50)`), `dispatch_due_lifecycle_events(limit=50)` и
     недоступность Gemini (`ig_ai_fallback:{row.pk}` — ключ на **каждое**
     сообщение) дают N уведомлений там, где нужна одна сводка с числом.
  4. **Ссылка в админку в каждом алерте, где есть сущность.** Сейчас она есть
     у 2 из 31. Самое частое уведомление — эскалация «клиенту нужен менеджер» —
     содержит только IGSID, без username и без ссылки на карточку, хотя
     `client_id` уже известен строкой выше. `MANAGEMENT_BASE_URL` в настройках
     есть и используется только в `ig_payment_review`.
  5. **Убрать дубль на одно событие:** `instagram_bot.py:6372` и `:6386`
     срабатывают вместе на одной неудачной отправке.
  6. **Разрешить коллизию ключа** `ig-lifecycle:{event_key}` — один ключ на два
     разных события (`ig_lifecycle.py:320` и `:384`), второе проглатывается молча.
  7. **Мониторить `DEAD_LETTER`/`UNKNOWN`:** они не подбираются дренажом и никем
     не проверяются, то есть потеря алерта происходит бесшумно.
  8. Единый язык и тон: два уведомления в `ig_lifecycle` написаны по-английски,
     остальные — по-украински.

  **Сделано в этом срезе:** новый `services/ig_alerts.py` — глобальный лимит
  потока (6/мин, очередь не теряется, удержание видно в логе), `alert_dedupe_key`
  с сущностью и временным окном (повтор проблемы теперь доходит следующим окном,
  а не «никогда»), ссылки в админку с переживанием обрезки, `summarize_batch` для
  схлопывания пачек. Подключено к дренажу очереди и к шести точкам, включая
  эскалацию — самое частое уведомление, где раньше был только IGSID.
  Тесты: `tests_ig_alerts`, 20.

  **Финальный срез `221cf37d`:** `UNKNOWN` и `DEAD_LETTER` по-прежнему не
  переотправляются автоматически, но раз в минуту проверяются bounded monitor'ом
  и дают один почасовой durable summary со ссылкой в `/bot/`; sample error
  redacted, а полный count не ограничен sample-лимитом. `ig-lifecycle:window:`
  и `ig-lifecycle:delivery:` разделяют два разных события; оба operator-text
  переведены на украинский. Один failed paylink создаёт только actionable
  payment-review alert: generic permanent и link-circuit Telegram-alert
  подавлены для того же failed send, а circuit-state всё равно сохраняется.
  Regression: 75 `tests_ig_notifications`, `tests_ig_lifecycle`,
  `tests_ig_audit_fixes`; production `check`, daemon `running/alive` и нулевой
  terminal backlog подтверждены. Отдельный MariaDB test gate остаётся IMP-094,
  но схема этого среза не менялась.
- [x] **IMP-059 (P1) — закрыта и задеплоена 2026-08-04 (`f2a84717`,
  `244cbbd3`).** Incident evidence больше не ограничено UI-таблицей на 500
  строк: `ig_bot` имеет отдельный rotating warning/error log, а 4xx webhook
  wave (>=5 и >=25% за 5 минут) ставит durable outbox-alert. Health отражает
  `rejections_degraded` до фактического восстановления доли; 71 профильный
  тест, production MariaDB и `/bot/health/` подтверждены.
- [ ] **IMP-100 (P2) — открыта.** Дедуплицировать повторяющиеся записи
  `InstagramBotLog` для UI по безопасному bounded key `(level, event, detail)`:
  хранить счётчик/последнее наблюдение вместо N одинаковых строк, явно показать
  счётчик в admin UI и не менять полный rotating file log. Нужны migration,
  MariaDB concurrency/retention тест и deploy. Это `IMPR-OPS-002`; не является
  условием уже закрытой сохранности incident evidence.
- [ ] **IMP-060 (P2) — открыта.** Вложения (F-DATA-011): не качать URL импортированных
  сообщений, сохранять байты при приёме живого webhook.
- [ ] **IMP-061 (P2) — открыта.** `hub.verify_token` в access-логе (F-SEC-010):
  диагностику подписки без токена в query, затем ротация токена.
- [ ] **IMP-044 (P2) — открыта.** Atomic lease Gemini-ключей + jitter (F-AI-003/004);
  UI-состояние ключей вычисляемое вместо хранимого `last_status` (F-DATA-012);
  data-migration невалидного `gemini_model` + предупреждение о расхождении
  и полный allowlist в селекте (F-AI-013).
- [ ] **IMP-045 (P2) — открыта.** Классификация ~60 `except: pass` по домену (F-DEBT-004),
  правило «`pass` только для телеметрии с комментарием».
- [ ] **IMP-046 (P2/P3) — открыта.** Решения по мёртвому коду: checkout-домен (5 таблиц, 0 записей —
  достроить или удалить), `InstagramBotProcessedMessage`, `send_text_tagged`,
  `resolve_gemini_key`, `ensure_instagram_subscription`, мёртвый CSS и `log_items`
  (F-UX-011), 5 мёртвых типов сигналов (F-SCORE-012), `manager_observation`
  как 56% снапшотов (F-DATA-009).
  **Отдельно:** `IgLifecycleEvent` — не удалять, а достроить: через него правильно
  решаются IMP-021, IMP-008 и дедупликация Meta-событий.
- [ ] **IMP-094 (P1) — детерминированный production-like test gate
  (F-TEST-002/003).** Устранить зависимость полного `management` suite от cwd
  и загрязнения глобального состояния; выделить стабильный обязательный набор
  для deploy. Добавить MariaDB-run для ограничений, которые SQLite не
  проверяет (`varchar(max_length)`, locks/constraints), и держать
  `verify_ig_production_contract --rollback-fixtures` отдельным обязательным
  no-network gate. Уже исправленные fixture/digest/media и 32-символьный
  `failure_kind` не закрывают недетерминированность всего suite. Отдельно
  согласовать `InstagramLoginWebhookSecretTests` с действующим multi-secret
  ingress contract: два assert всё ещё требуют отвергать parent app secret,
  хотя `IMP-063` намеренно принимает оба наших secrets после production 4xx
  incident; это test debt, не регрессия encryption.
  **Локальный progress 2026-08-04 (не закрывает задачу):** ночные time-based
  тесты стабилизированы; detached User-registration notifier и fulfillment
  wake-up изолированы при `TESTING=True`; recovery-schedule failure теперь
  сохраняет terminal unsent state; MariaDB profile fail-closed учитывает
  Django default `localhost`. Полный `management` suite прошёл 2619 тестов
  (3 skipped) из двух CWD. Commit `15147ded` находится в `main` и production;
  остался обязательный отдельный disposable MariaDB run.
- [ ] **IMP-096 (P2) — provenance ролей импортированной переписки
  (F-DATA-015).** Отделить подтверждённые manager/model сообщения от legacy
  import uncertainty, добавить read-only отчёт и dry-run backfill с точным
  источником доказательства. `--apply` допустим только для однозначных provider
  message ID/исходящих registry-событий; текстовое сходство не является
  достаточным основанием менять `role`.

---

## W9 — Product reselection и проверяемая коммерческая семантика

Источник: локальная `codex/instagram-assisted-checkout`. Design/plan уже восстановлены в
`main`; пять code-коммитов остаются patch-unique относительно актуальной базы,
но отсутствуют в historical `origin/codex/instagram-assisted-checkout`.
Source сохранён отдельным remote ref `codex/ig-w9-local-preservation-20260804`
до любого rebase.
Ни один пункт ниже нельзя считать закрытым до переноса на актуальный `main`,
повторных тестов, MariaDB-проверки и production deploy. Полный дизайн и
13-задачный execution plan:
`docs/superpowers/specs/2026-08-02-instagram-product-reselection-intelligence-design.md`
и `docs/superpowers/plans/2026-08-02-instagram-product-reselection-intelligence.md`.

- [ ] **IMP-081 (P1) — PARTIAL, foundation в `main`/production.**
  `bf4e0d80`, `674d6858`, `3678ddf4`: append-only verified product semantics,
  explicit inventory policy, запрет generic/punctuation aliases и
  unauthoritative revocation. Production: migrations `storefront.0088` и
  `fable5.0008`, три InnoDB tables, 77 policies, UPDATE/DELETE triggers.
  Остаток: полноценный runtime/admin consumer и отдельный disposable MariaDB
  test gate; production БД не используется как тестовая.
- [ ] **IMP-082 (P1) — PARTIAL, foundation и prompt parity в `main`/production `0ad694bc`.**
  `7b5d5cc7` + `1c4d6d48`: trusted product references, typed price-aware graph,
  graph digest, canonical option-path hardening и combined color/fit/size
  compatibility. `e44d1440` + `0ad694bc` закрыли F-CAT-007: product 110 prompt
  = variant 81, thermo green, exact 1450 грн, oversize XS/M, без ложного
  product-wide ряда. Остаток: полный print/blank/media/canonical-link topology
  и durable runtime commerce-session consumer.
- [ ] **IMP-083 (P1) — PARTIAL, foundation в `main`/production `0ad694bc`.**
  Explainable hard-filter/ranking, catalog priority/preferences, price filtering
  и fail-closed generic/revoked/BOT_VISION semantics опубликованы. Остаток:
  relaxed alternatives после hard mismatch, durable candidate/session revision
  binding, stale revalidation и перенос trusted URL constraints в durable
  selection вместо временного `exact_reference`.
- [ ] **IMP-084 (P0/P1) — РЕАЛИЗОВАНО В ВЕТКЕ `e9d982df`, НЕ ИНТЕГРИРОВАНО.**
  Warehouse-aware exact availability. Aggregate quantity для строк с одной
  allocation identity и полный checkout wiring остаются открытыми.
- [ ] **IMP-085 (P1) — РЕАЛИЗОВАНО В ВЕТКЕ `dc9889c3`, НЕ ИНТЕГРИРОВАНО.**
  Детерминированный parser составного commerce turn. Reducer, burst ordering,
  durable decisions и подключение до Gemini пока не сделаны.
- [ ] **IMP-086 (P0)** — reservation/allocation lifecycle: резерв последней
  единицы при создании proposal, MariaDB-safe state machine, late-payment
  overbook review, атомарный write-off/reversal и запрет отрицательного склада.
- [ ] **IMP-087 (P0/P1)** — durable commerce session/outbox, state reduction,
  candidate reply anchoring, repeat-purchase/exchange routing и интеграция до
  legacy classifier/Gemini без blind resend через неоднозначную provider boundary.
- [ ] **IMP-088 (P1)** — proposal digest idempotency, manager review UI,
  cache invalidation/freshness каталога (IMPR-CAT-006), read-only audit/backfill,
  unified regression, production-like MariaDB proof,
  интеграция в `main` и deploy.

- [x] **IMP-104 (P0) — authoritative configuration pricing.** Добавлена
  проверяемая связка цены с variant/fit/generic options от `[ITEM:...]` и
  `[PRICE_QUOTED:...]` до proposal, deal и hosted checkout. Unknown,
  disabled, unavailable и zero-choice option contexts, а также ambiguous
  multi-price customer text блокируют оформление; checkout показывает labels,
  unit prices и line totals. Commits `1f5dcb70`, `7fdbe613`, `1f8cead2`,
  production `434428ad`; focused authoritative-price gate 12 tests.

---

## W10 — Улучшения, которые раньше были только идеями без IMP-задач

Этот блок добавлен после полной сверки существовавших тогда 48 `IMPR-*` с
планом; текущая матрица после source reconciliation содержит 50. Он не меняет
порядок W4B: сначала IMP-056–058, затем W9/W10 по зависимостям.

**Coverage gate:** подробный статус каждого ID находится в канонической таблице
`05_IMPROVEMENTS_REGISTER.md`. Ни один ID больше не остаётся вне плана:

- каталог: IMPR-CAT-001, IMPR-CAT-002, IMPR-CAT-003, IMPR-CAT-004,
  IMPR-CAT-005, IMPR-CAT-006;
- фичи: IMPR-FEAT-001, IMPR-FEAT-002, IMPR-FEAT-003, IMPR-FEAT-004,
  IMPR-FEAT-005, IMPR-FEAT-006, IMPR-FEAT-007, IMPR-FEAT-008,
  IMPR-FEAT-009, IMPR-FEAT-010, IMPR-FEAT-011, IMPR-FEAT-012,
  IMPR-FEAT-013, IMPR-FEAT-014, IMPR-FEAT-015;
- продажи/тексты: IMPR-SALES-001, IMPR-SALES-002, IMPR-SALES-003,
  IMPR-SALES-004, IMPR-SALES-005, IMPR-SALES-006, IMPR-SALES-007,
  IMPR-SALES-008, IMPR-SALES-009, IMPR-SALES-010, IMPR-SALES-011,
  IMPR-TXT-001, IMPR-TXT-002, IMPR-TXT-003, IMPR-TXT-004,
  IMPR-TXT-005, IMPR-TXT-006;
- память/инициатива/склад/ops: IMPR-MEM-001, IMPR-FUP-013, IMPR-FUP-014,
  IMPR-FUP-015,
  IMPR-INV-001, IMPR-OPS-002;
- UX: IMPR-UX-001, IMPR-UX-002, IMPR-UX-003, IMPR-UX-004,
  IMPR-UX-005, IMPR-UX-006.

- [ ] **IMP-090 (P1)** — model-authored follow-up (IMPR-FUP-013): модель
  формулирует короткое инициативное сообщение из проверенных фактов, а policy,
  deterministic opt-out и текущий uk/ru/en текст остаются guard/fallback.
  Выполнять после event/claim слоя IMP-056.
- [ ] **IMP-091 (P1/P2, требует продуктовых решений)** — retention-пакет:
  двухшаговый отзыв после доставки, безопасная реактивация, лояльность и
  предзаказ (IMPR-FEAT-008…011). До решения политики скидок, предзаказа и
  сегментов никакой массовой отправки.
- [ ] **IMP-092 (P1)** — операционная приоритизация лидов и честный режим вне
  рабочих часов (IMPR-FEAT-012/013): очередь менеджера по проверяемым фактам,
  расписание/тексты из настроек, сохранение 24-часового окна и opt-out.
- [ ] **IMP-093 (P2)** — аналитический UX (IMPR-UX-002/004/005): sparkline по
  эпизоду, единый timeline сообщений/оплаты/ТТН/FSM и группировка KPI
  «воронка / деньги / сервис / реклама». Не дублировать уже закрытые
  определения метрик IMP-040.

---

## W11 — полное покрытие находок и orphan backlog

- [ ] **IMP-098 (P1/P2) — закрыть находки, которые не имели самостоятельной implementation-задачи.**
  Текущий orphan-набор: F-CORE-003…006, F-PAY-010, F-SCORE-010 и остатки
  F-SEC-004/009 (reviewer PII/log sandbox и явная PII-policy для рабочих
  Telegram/операторских каналов).
  F-CORE-007 при сверке оказался уже закрыт durable outgoing registry в
  IMP-073; F-SCORE-012 имеет самостоятельную открытую задачу IMP-046.
  Для каждого ID обязательны отдельный
  regression-тест, production-like MariaDB proof и запись deployment SHA.
  Матрица ниже является checklist границы: `[x]` только для verified/fixed,
  `[ ]` для open и partial. Она не заменяет подробное evidence в
  `03_FINDINGS_REGISTER.md`.

- [x] **IMP-099 (P1/P2) — supplemental closures восстановлены из progress-history.**
  В канонический план возвращены уже реализованные, но раньше не имевшие
  отдельного `IMP-*`: F-PAT-003, F-OPS-005, F-STATE-009, F-UX-015 и
  F-OPS-007. Evidence: кириллические размеры с контекстным guard, эскалация
  зависшей ТТН, продвижение стадии от заказа, transcript-bound media и payer
  быстрого возврата. Галочка основана на существующем коде/тестах и ранее
  задеплоенном W4/W5-срезе, а не на одном тексте progress.

---

## W12 — доказуемая доставка follow-up и event-driven continuation

Источник требований: dirty worktree `codex/ig-followup-policies`. Его код и
migration `0131` основаны на старой схеме и не переносятся wholesale. Delivery
boundary уже заново реализован на актуальном `main`; materialized event
continuation остаётся отдельным свежим срезом.

- [x] **IMP-102 (P0/P1) — durable follow-up delivery FSM.** Введены явные
  `PROCESSING`, `SENT`, `AMBIGUOUS`, `COMPLETED`, атомарная lease/claim boundary
  и сохранение provider receipt. Blind retry после timeout/неоднозначного
  provider outcome запрещён. Recovery протухшей lease обязан отличать задачу,
  которая гарантированно не отправлялась, от ambiguous side effect; для
  `AMBIGUOUS` нужен наблюдаемый admin queue и ручное audited разрешение.
  Acceptance закрыт коммитами `0d4d38c0`, `0e9e9ba5`, `4cb86743`,
  `414e639e`: concurrent claim, worker restart, timeout/5xx/receipt, stale lease
  recovery, receipt-first finalization без resend, lock-safe sender/recovery
  race и audited manual resolution. 23/23 focused и 160/160 expanded тестов,
  Django check, migration drift и compileall зелёные. Production HEAD
  `414e639e`, migration `management.0141` applied; один daemon `running/alive`
  на `instagram_login`, error и delivery-review очереди пусты.
- [x] **IMP-103 (P1) — materialized event follow-ups.** Создавать продолжение
  policy из точного business event с immutable `event_key`, payload и
  абсолютным policy timeline, а не из polling-угадывания текущего snapshot.
  Непосредственно перед send повторно проверять invoice/restock fact и
  отменять/перестраивать stale task без клиентского сообщения. Acceptance:
  duplicate/out-of-order event idempotency, absolute schedule across restart,
  paid invoice/restocked item suppression and audited policy continuation.
  Закрыто `4dfff3a2` + `35d3bd93`, migration `0143`, focused gate 180 tests,
  production `434428ad`; combined event/FSM/checkout/restock gate 255 tests.

### Finding coverage matrix — 179 уникальных F-идентификаторов

Итог матрицы: **134 `[x]` / 39 `OPEN [ ]` / 6 `PARTIAL [ ]`**. Статус
считается по факту текущего `main`, тестов и production evidence, а не по тому,
что ID когда-то упоминался в progress или feature-ветке.

| Check | Finding | Status | Canonical task |
|---|---|---|---|
| [x] | F-AI-001 | FIXED/VERIFIED | IMP-011 |
| [x] | F-AI-002 | FIXED/VERIFIED | IMP-011 |
| [ ] | F-AI-003 | OPEN | IMP-044 |
| [ ] | F-AI-004 | OPEN | IMP-044 |
| [x] | F-AI-005 | FIXED/VERIFIED | IMP-029 |
| [x] | F-AI-006 | FIXED/VERIFIED | IMP-029 |
| [x] | F-AI-007 | FIXED/VERIFIED | IMP-029 |
| [x] | F-AI-008 | FIXED/VERIFIED | IMP-029 |
| [ ] | F-AI-009 | PARTIAL (`042c48c8`: runtime authority; остаток сценариев) | IMP-028 |
| [ ] | F-AI-010 | OPEN | IMP-028 |
| [ ] | F-AI-011 | OPEN | IMP-028 |
| [ ] | F-AI-012 | OPEN | IMP-028 |
| [ ] | F-AI-013 | OPEN | IMP-044 |
| [x] | F-AI-014 | FIXED/VERIFIED | IMP-064 |
| [x] | F-AI-015 | FIXED/VERIFIED | IMP-076 |
| [x] | F-AI-016 | FIXED/VERIFIED | IMP-078 |
| [x] | F-AI-017 | FIXED/VERIFIED | IMP-097 |
| [x] | F-CAT-001 | FIXED/VERIFIED | IMP-067 |
| [x] | F-CAT-002 | FIXED/VERIFIED | IMP-067 |
| [x] | F-CAT-003 | FIXED/VERIFIED | IMP-080 |
| [ ] | F-CAT-004 | OPEN | IMP-084/086 |
| [x] | F-CAT-005 | FIXED/VERIFIED | IMP-081 |
| [x] | F-CAT-006 | FIXED/VERIFIED | IMP-081 |
| [x] | F-CAT-007 | FIXED/VERIFIED | IMP-082; `e44d1440`/`0ad694bc` |
| [x] | F-CAT-008 | FIXED/VERIFIED | IMP-104 |
| [x] | F-CAT-009 | FIXED/VERIFIED | IMP-104 |
| [x] | F-CAT-010 | FIXED/VERIFIED | IMP-104 |
| [x] | F-CORE-001 | FIXED/VERIFIED | IMP-008 |
| [x] | F-CORE-002 | FIXED/VERIFIED | IMP-012 |
| [ ] | F-CORE-003 | OPEN | IMP-098 |
| [ ] | F-CORE-004 | OPEN | IMP-098 |
| [ ] | F-CORE-005 | OPEN | IMP-098 |
| [ ] | F-CORE-006 | OPEN | IMP-098 |
| [x] | F-CORE-007 | FIXED/VERIFIED | IMP-073 |
| [x] | F-CORE-009 | FIXED/VERIFIED | IMP-001 |
| [x] | F-CORE-010 | FIXED/VERIFIED | IMP-004 |
| [x] | F-CORE-011 | FIXED/VERIFIED | IMP-001/004/063 |
| [x] | F-CORE-012 | FIXED/VERIFIED | IMP-063 |
| [x] | F-CORE-013 | FIXED/VERIFIED | IMP-068 |
| [x] | F-CORE-014 | FIXED/VERIFIED | IMP-069 |
| [x] | F-CORE-015 | FIXED/VERIFIED | IMP-073 |
| [x] | F-CORE-016 | FIXED/VERIFIED | IMP-074 |
| [x] | F-CORE-017 | FIXED/VERIFIED | IMP-075 |
| [x] | F-CORE-018 | FIXED/VERIFIED | IMP-097 |
| [ ] | F-CTX-001 | PARTIAL (`042c48c8`: compact/bounded sources; adaptive context остаётся) | IMP-028 |
| [x] | F-CTX-002 | FIXED/VERIFIED | IMP-016/052 |
| [ ] | F-CTX-003 | PARTIAL (`042c48c8`: order resolves conflicts; duplicate legacy text remains) | IMP-028 |
| [x] | F-CTX-004 | FIXED/VERIFIED | IMP-078 |
| [ ] | F-DATA-001 | OPEN | IMP-046 |
| [ ] | F-DATA-002 | OPEN | IMP-046 |
| [ ] | F-DATA-003 | OPEN | IMP-046 |
| [ ] | F-DATA-004 | OPEN | IMP-046 |
| [x] | F-DATA-005 | FIXED/VERIFIED | IMP-013 |
| [x] | F-DATA-006 | FIXED/VERIFIED | IMP-002 |
| [ ] | F-DATA-009 | OPEN | IMP-046 |
| [ ] | F-DATA-010 | OPEN | IMP-046 |
| [x] | F-DATA-011 | FIXED/VERIFIED | IMP-003 |
| [ ] | F-DATA-012 | OPEN | IMP-044 |
| [x] | F-DATA-013 | FIXED/VERIFIED | IMP-013 |
| [x] | F-DATA-014 | FIXED/VERIFIED | IMP-013 |
| [ ] | F-DATA-015 | OPEN | IMP-096 |
| [ ] | F-DATA-016 | OPEN | IMP-095 |
| [ ] | F-DEBT-001 | OPEN | IMP-046 |
| [ ] | F-DEBT-002 | OPEN | IMP-046 |
| [ ] | F-DEBT-003 | OPEN | IMP-046 |
| [ ] | F-DEBT-004 | OPEN | IMP-045 |
| [x] | F-DEBT-005 | FIXED/VERIFIED | IMP-004 |
| [ ] | F-DEBT-006 | OPEN | IMP-094 |
| [ ] | F-DEBT-007 | OPEN | IMP-094 |
| [x] | F-FUP-001 | FIXED/VERIFIED | IMP-047 |
| [x] | F-FUP-002 | FIXED/VERIFIED | IMP-048 |
| [x] | F-FUP-003 | FIXED/VERIFIED | IMP-049 |
| [x] | F-FUP-004 | FIXED/VERIFIED | IMP-055 |
| [x] | F-FUP-005 | FIXED/VERIFIED | IMP-053 |
| [x] | F-FUP-006 | FIXED/VERIFIED | IMP-054 |
| [x] | F-FUP-007 | FIXED/VERIFIED | IMP-054 |
| [x] | F-FUP-008 | FIXED/VERIFIED | IMP-053 |
| [x] | F-FUP-009 | FIXED/VERIFIED | IMP-056 |
| [x] | F-FUP-010 | FIXED/VERIFIED | IMP-052 |
| [x] | F-FUP-011 | FIXED/VERIFIED | IMP-050/052 |
| [x] | F-FUP-012 | FIXED/VERIFIED | IMP-050/052 |
| [x] | F-FUP-013 | FIXED/VERIFIED (`414e639e`) | IMP-102 |
| [x] | F-OBJ-001 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-002 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-003 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-004 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-005 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-006 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-007 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OBJ-008 | FIXED/VERIFIED | IMP-057 |
| [x] | F-OPS-001 | FIXED/VERIFIED | IMP-009/051 |
| [ ] | F-OPS-002 | OPEN | IMP-046 |
| [x] | F-OPS-003 | FIXED/VERIFIED | IMP-020 |
| [x] | F-OPS-004 | FIXED/VERIFIED | IMP-059 |
| [x] | F-OPS-005 | FIXED/VERIFIED | IMP-099 |
| [x] | F-OPS-006 | FIXED/VERIFIED | IMP-062 |
| [x] | F-OPS-007 | FIXED/VERIFIED | IMP-099 |
| [x] | F-OPS-008 | FIXED/VERIFIED | IMP-059 |
| [x] | F-OPS-009 | FIXED/VERIFIED | IMP-077 |
| [x] | F-PAT-001 | FIXED/VERIFIED | IMP-017 |
| [x] | F-PAT-002 | FIXED/VERIFIED | IMP-017 |
| [x] | F-PAT-003 | FIXED/VERIFIED | IMP-099 |
| [x] | F-PAT-004 | FIXED/VERIFIED | IMP-017 |
| [x] | F-PAT-005 | FIXED/VERIFIED | IMP-017 |
| [x] | F-PAY-001 | FIXED/VERIFIED | IMP-010 |
| [ ] | F-PAY-002 | OPEN | IMP-087/088 |
| [ ] | F-PAY-003 | OPEN | IMP-087/088 |
| [x] | F-PAY-004 | FIXED/VERIFIED | IMP-051 |
| [x] | F-PAY-005 | FIXED/VERIFIED | IMP-050 |
| [ ] | F-PAY-006 | OPEN | IMP-087/088 |
| [x] | F-PAY-007 | FIXED/VERIFIED | IMP-021 |
| [ ] | F-PAY-008 | OPEN | IMP-043 |
| [x] | F-PAY-009 | FIXED/VERIFIED | IMP-022 |
| [ ] | F-PAY-010 | OPEN | IMP-098 |
| [x] | F-PAY-011 | FIXED/VERIFIED | IMP-024 |
| [x] | F-PAY-012 | FIXED/VERIFIED | IMP-013 |
| [x] | F-PAY-013 | FIXED/VERIFIED | IMP-071 |
| [x] | F-PAY-014 | FIXED/VERIFIED | IMP-089 |
| [x] | F-PAY-015 | FIXED/VERIFIED | `93ae8684` / DR-011 |
| [x] | F-SCORE-001 | FIXED/VERIFIED | IMP-019 |
| [x] | F-SCORE-002 | FIXED/VERIFIED | IMP-015 |
| [x] | F-SCORE-003 | FIXED/VERIFIED | IMP-014 |
| [x] | F-SCORE-004 | FIXED/VERIFIED | IMP-013 |
| [x] | F-SCORE-005 | FIXED/VERIFIED | IMP-013 |
| [x] | F-SCORE-006 | FIXED/VERIFIED | IMP-015 |
| [ ] | F-SCORE-007 | OPEN | IMP-093 |
| [x] | F-SCORE-008 | FIXED/VERIFIED | IMP-018 |
| [x] | F-SCORE-009 | FIXED/VERIFIED | IMP-016 |
| [ ] | F-SCORE-010 | OPEN | IMP-098 |
| [x] | F-SCORE-011 | FIXED/VERIFIED | IMP-034 |
| [ ] | F-SCORE-012 | OPEN | IMP-046 |
| [x] | F-SCORE-013 | FIXED/VERIFIED | IMP-040 |
| [ ] | F-SCORE-014 | PARTIAL | IMP-043 |
| [x] | F-SCORE-015 | FIXED/VERIFIED | IMP-013/015/019/031 |
| [ ] | F-SEC-001 | OPEN | IMP-101 |
| [x] | F-SEC-002 | FIXED/VERIFIED | IMP-005 |
| [x] | F-SEC-003 | FIXED/VERIFIED | IMP-006 |
| [ ] | F-SEC-004 | PARTIAL | IMP-007/098 |
| [x] | F-SEC-005 | FIXED/VERIFIED | IMP-042 |
| [x] | F-SEC-006 | FIXED/VERIFIED | IMP-025 |
| [x] | F-SEC-007 | FIXED/VERIFIED | IMP-012 |
| [x] | F-SEC-008 | FIXED/VERIFIED | IMP-041 |
| [ ] | F-SEC-009 | PARTIAL | IMP-006/098 |
| [ ] | F-SEC-010 | OPEN | IMP-061 |
| [x] | F-SEC-011 | FIXED/VERIFIED | IMP-042 |
| [x] | F-STAT-001 | FIXED/VERIFIED | IMP-058 |
| [x] | F-STAT-002 | FIXED/VERIFIED | IMP-058 |
| [x] | F-STAT-003 | FIXED/VERIFIED | IMP-058 |
| [x] | F-STAT-004 | FIXED/VERIFIED | IMP-058 |
| [x] | F-STATE-001 | FIXED/VERIFIED | IMP-031 |
| [x] | F-STATE-002 | FIXED/VERIFIED | IMP-033 |
| [x] | F-STATE-003 | FIXED/VERIFIED | IMP-033 |
| [x] | F-STATE-004 | FIXED/VERIFIED | IMP-032 |
| [x] | F-STATE-005 | FIXED/VERIFIED | IMP-034 |
| [x] | F-STATE-006 | FIXED/VERIFIED | IMP-034 |
| [x] | F-STATE-007 | FIXED/VERIFIED | IMP-034 |
| [x] | F-STATE-008 | FIXED/VERIFIED | IMP-018 |
| [x] | F-STATE-009 | FIXED/VERIFIED | IMP-099 |
| [x] | F-STATE-010 | FIXED/VERIFIED | IMP-079 |
| [x] | F-TEST-001 | FIXED/VERIFIED | IMP-022 |
| [ ] | F-TEST-002 | OPEN | IMP-094 |
| [x] | F-TEST-003 | FIXED/VERIFIED | IMP-055 |
| [x] | F-TXT-001 | FIXED/VERIFIED | IMP-022 |
| [x] | F-UX-001 | FIXED/VERIFIED | IMP-035 |
| [x] | F-UX-002 | FIXED/VERIFIED | IMP-020 |
| [x] | F-UX-003 | FIXED/VERIFIED | IMP-020 |
| [x] | F-UX-004 | FIXED/VERIFIED | IMP-039 |
| [x] | F-UX-005 | FIXED/VERIFIED | IMP-036 |
| [x] | F-UX-006 | FIXED/VERIFIED | IMP-037 |
| [x] | F-UX-007 | FIXED/VERIFIED | IMP-038 |
| [x] | F-UX-008 | FIXED/VERIFIED | IMP-040 |
| [x] | F-UX-009 | FIXED/VERIFIED | IMP-037 |
| [x] | F-UX-010 | FIXED/VERIFIED | IMP-040 |
| [ ] | F-UX-011 | OPEN | IMP-046 |
| [x] | F-UX-012 | FIXED/VERIFIED | IMP-040 |
| [x] | F-UX-013 | FIXED/VERIFIED | IMP-040 |
| [x] | F-UX-014 | FIXED/VERIFIED | IMP-018 |
| [x] | F-UX-015 | FIXED/VERIFIED | IMP-099 |
| [x] | F-UX-016 | FIXED/VERIFIED | IMP-035 |

### Improvement coverage matrix — 51 уникальное IMPR-идентификатор

Итог матрицы: **17 `[x]` / 34 `[ ]`**. В незакрытый остаток входят
21 `PARTIAL`, 12 `OPEN` (из них 3 decision-gated) и 1 `REFRAMED`; галочка не
ставится, пока полезный остаток не реализован и не задеплоен.

| Check | Improvement | Status | Canonical task / остаток |
|---|---|---|---|
| [x] | IMPR-CAT-001 | DONE | IMP-027 |
| [ ] | IMPR-CAT-002 | REFRAMED | IMP-067; настоящий учёт варианта — IMP-084/086 |
| [x] | IMPR-CAT-003 | DONE | IMP-080 |
| [ ] | IMPR-CAT-004 | PARTIAL (`0ad694bc`: typed graph/ranker + prompt parity; availability wiring remains) | IMP-082/084 |
| [x] | IMPR-CAT-005 | DONE | IMP-067; catalog completeness `3191e08c` |
| [ ] | IMPR-CAT-006 | OPEN | IMP-088 |
| [x] | IMPR-CAT-007 | DONE | IMP-104; production `434428ad` |
| [ ] | IMPR-FEAT-001 | PARTIAL (`0ad694bc`: explainable candidate foundation + prompt parity; runtime/review remains) | IMP-082/083/088 |
| [ ] | IMPR-FEAT-002 | OPEN | IMP-084/086 |
| [ ] | IMPR-FEAT-003 | PARTIAL | IMP-028/053/056; остаток IMP-083 |
| [ ] | IMPR-FEAT-004 | PARTIAL | IMP-056; durable subscription/warehouse — IMP-087 |
| [ ] | IMPR-FEAT-005 | PARTIAL | `IgDealItem` есть; остаток IMP-085/087/088 |
| [x] | IMPR-FEAT-006 | DONE | IMP-053/056 |
| [x] | IMPR-FEAT-007 | DONE | IMP-021 |
| [ ] | IMPR-FEAT-008 | OPEN, decision-gated | IMP-091 |
| [ ] | IMPR-FEAT-009 | PARTIAL | одношаговый flow IMP-024; двухшаговый — IMP-091 |
| [ ] | IMPR-FEAT-010 | OPEN, decision-gated | IMP-091 |
| [ ] | IMPR-FEAT-011 | OPEN, decision-gated | IMP-091 |
| [ ] | IMPR-FEAT-012 | OPEN | IMP-092 |
| [ ] | IMPR-FEAT-013 | PARTIAL | IMP-054/078; операционный остаток IMP-092 |
| [ ] | IMPR-FEAT-014 | PARTIAL | hosted checkout `c696ee9e`; остаток IMP-087/088 |
| [ ] | IMPR-FEAT-015 | PARTIAL | access token/`Kind.SHARE` есть; E2E — IMP-087/088 |
| [ ] | IMPR-FUP-013 | OPEN | IMP-090 после IMP-056 |
| [x] | IMPR-FUP-014 | DONE | IMP-102; production `414e639e` |
| [x] | IMPR-FUP-015 | DONE | IMP-103; production `434428ad` |
| [ ] | IMPR-INV-001 | OPEN | IMP-081/084/086 |
| [x] | IMPR-MEM-001 | DONE | IMP-030 |
| [ ] | IMPR-OPS-002 | OPEN | IMP-100; incident retention закрыт IMP-041/059 |
| [ ] | IMPR-SALES-001 | PARTIAL | каталог размеров есть; протокол — IMP-028 |
| [ ] | IMPR-SALES-002 | PARTIAL | post-sale guard есть; prompt acceptance — IMP-028 |
| [ ] | IMPR-SALES-003 | PARTIAL | `042c48c8`: максимум один contextual upsell; конкретная вторая позиция/корзина — IMP-085/087 |
| [ ] | IMPR-SALES-004 | PARTIAL | `042c48c8`: discount как price fact, не rescue offer; сценарий «дорого» — IMP-028 |
| [ ] | IMPR-SALES-005 | PARTIAL | IMP-053/057; полный prompt-протокол — IMP-028 |
| [ ] | IMPR-SALES-006 | PARTIAL | IMP-053; выбор/событие — IMP-028/056/083 |
| [ ] | IMPR-SALES-007 | PARTIAL | `042c48c8`: не более одного вопроса; последовательность discovery — IMP-028 |
| [ ] | IMPR-SALES-008 | PARTIAL | hard limits IMP-052/053 + один CTA/без давления `042c48c8`; remaining dialog acceptance — IMP-028 |
| [ ] | IMPR-SALES-009 | PARTIAL | ложный stock убран IMP-067; остаток IMP-028/084 |
| [ ] | IMPR-SALES-010 | PARTIAL | `042c48c8`: один чёткий CTA; concrete order summary/close — IMP-028 |
| [ ] | IMPR-SALES-011 | OPEN | IMP-028 |
| [x] | IMPR-TXT-001 | DONE | IMP-021 |
| [x] | IMPR-TXT-002 | DONE | IMP-022 |
| [x] | IMPR-TXT-003 | DONE | IMP-022 |
| [x] | IMPR-TXT-004 | DONE | IMP-024 |
| [x] | IMPR-TXT-005 | DONE | IMP-053 |
| [ ] | IMPR-TXT-006 | OPEN | IMP-028 |
| [x] | IMPR-UX-001 | DONE | IMP-031/035 |
| [ ] | IMPR-UX-002 | OPEN | IMP-093 |
| [x] | IMPR-UX-003 | DONE | IMP-034 |
| [ ] | IMPR-UX-004 | PARTIAL | shipment timeline IMP-062; единый timeline — IMP-093 |
| [ ] | IMPR-UX-005 | PARTIAL | definitions IMP-040; grouping — IMP-093 |
| [x] | IMPR-UX-006 | DONE | IMP-038 |

## Что НЕ входит в план и почему

- **Переписывание `instagram_bot.py` (282 KB) на модули.** Рефакторинг без
  функциональной ценности при живых P0. Возвращаться после W1–W4.
- **Смена модели Gemini.** Проверено по документации: `gemini-3.6-flash` —
  актуальная топовая Flash-модель, логика приоритета (model-major по всем
  6 ключам) корректна. Менять нечего, кроме мусорного значения в БД.
- **Массовое удаление «мёртвых» таблиц** до решения по каждому домену.
- **Structured output вместо тегов** (F-AI-010): контракт тегов пронизывает весь
  пайплайн, менять только после golden-тестов и стабилизации W1–W5.
- **Предзаказ, лояльность, реактивация базы** больше не потеряны: они учтены
  как decision-gated IMP-091 и не реализуются без продуктовой политики.

## Решения, которые нужны от заказчика

1. ~~**Промокод 10% за UGC** (IMP-024)~~ — решено в W4: вариант B,
   staff-only ручная выдача после проверки evidence полученного заказа.
2. **Судьба checkout-домена** (F-PAY-002): достроить (даёт резерв стока, TTL ссылки,
   share-токен для оплаты другим человеком) или удалить 5 таблиц и ~66 KB кода.
3. **Судьба `IgLifecycleEvent`** — рекомендация: достроить.
4. **Приоритет между W5 (качество продаж) и W7 (UX админки)** после закрытия W1–W4.
