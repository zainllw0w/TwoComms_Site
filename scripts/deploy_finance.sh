#!/usr/bin/env bash
set -euo pipefail

# RETIRED: use ./deploy.sh --target-sha <40-character-lowercase-commit-sha>.
printf '%s\n' 'RETIRED: use ./deploy.sh --target-sha <40-character-lowercase-commit-sha>.' >&2
exit 64
