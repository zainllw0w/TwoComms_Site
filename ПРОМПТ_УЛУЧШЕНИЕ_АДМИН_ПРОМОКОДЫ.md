# 🎯 ПРОМПТ: Улучшение Админ-Панели Промокодов

## 📋 ЗАДАЧА

Необходимо улучшить интерфейс админ-панели для управления промокодами в Django-проекте TwoComms. Текущий шаблон имеет проблемы с дизайном, функциональностью и удобством использования.

---

## 🔍 КОНТЕКСТ ПРОЕКТА

### Расположение файлов

**Основные файлы:**
- `twocomms/twocomms_django_theme/templates/pages/admin_promocodes.html` - основной шаблон админ-панели промокодов
- `twocomms/storefront/views/promo.py` - views и логика работы с промокодами
- `twocomms/storefront/models.py` (классы `PromoCode`, `PromoCodeGroup`, `PromoCodeUsage`) - модели данных
- `twocomms/storefront/urls.py` - маршруты для промокодов
- `twocomms/twocomms_django_theme/static/css/promocodes.css` - стили промокодов

**Интеграция в админ-панель:**
- Админ-панель промокодов открывается через `/admin-panel/?section=promocodes&tab=<tab_name>`
- Подключается через `twocomms/twocomms_django_theme/templates/pages/admin_panel.html` (строка ~1637: `{% include 'pages/admin_promocodes.html' %}`)

### Модели данных

**PromoCode (Промокод):**
- `code` - код промокода (уникальный)
- `promo_type` - тип (regular/voucher/grouped)
- `discount_type` - тип скидки (percentage/fixed)
- `discount_value` - значение скидки
- `group` - ForeignKey к PromoCodeGroup (может быть NULL)
- `max_uses`, `current_uses` - ограничения использования
- `one_time_per_user` - одноразовое использование на пользователя
- `min_order_amount` - минимальная сумма заказа
- `valid_from`, `valid_until` - период действия
- `is_active` - статус активности

**PromoCodeGroup (Группа промокодов):**
- `name` - название группы
- `description` - описание
- `one_per_account` - **ВАЖНО**: если True, то пользователь, использовавший хотя бы один промокод из группы, больше не сможет использовать другие промокоды из этой группы
- `is_active` - статус активности

### Логика работы

**Групповая логика (критически важно):**
Если группа имеет `one_per_account=True`, то после использования любого промокода из этой группы конкретным пользователем, этот пользователь больше не сможет использовать другие промокоды из этой группы (проверка через `PromoCodeUsage` модель).

**AJAX endpoints:**
- `/admin-panel/promocode/<id>/get-form/` - получение данных промокода для редактирования
- `/admin-panel/promocode/<id>/edit-ajax/` - обновление промокода через AJAX
- `/admin-panel/promocode/<id>/change-group/` - изменение группы промокода (для drag & drop)
- `/admin-panel/promo-group/<id>/get-form/` - получение данных группы
- `/admin-panel/promo-group/<id>/edit-ajax/` - обновление группы через AJAX

---

## ❌ ТЕКУЩИЕ ПРОБЛЕМЫ

### 1. Проблемы с дизайном
- Fade background перекрывает вкладки модальных окон (z-index конфликты)
- Стили не применяются правильно, элементы "подвинуты" на бок
- Неправильное применение auto-layout
- Некоторые элементы выходят за рамки контейнера

### 2. Проблемы с функциональностью
- Drag & Drop работает, но промокоды и группы находятся в разных вкладках/местах, что усложняет перетаскивание
- Модальные окна имеют проблемы с backdrop (затемнением фона), который перекрывает элементы интерфейса
- Неудобная навигация между вкладками во время drag & drop

### 3. Проблемы с UX
- Неочевидное управление промокодами
- Статистика неудобно расположена
- Редактирование требует много кликов

---

## ✅ ТРЕБОВАНИЯ К УЛУЧШЕНИЮ

### 1. Дизайн (КРИТИЧЕСКИ ВАЖНО)
- **Исправить все проблемы с z-index**: backdrop модальных окон не должен перекрывать вкладки и элементы управления
- **Правильный auto-layout**: все элементы должны корректно располагаться в контейнерах, ничего не должно "вылазить" за рамки
- **Красивый современный дизайн**: использовать существующую дизайн-систему проекта (классы `ds-*`, темная тема)
- **Адаптивность**: интерфейс должен корректно работать на всех размерах экранов

### 2. Drag & Drop улучшения
- **Оптимизировать расположение**: если промокоды и группы находятся в разных местах, нужно продумать удобную схему для drag & drop
- **Возможные решения**:
  - Сделать группы видимыми на вкладке промокодов (например, sidebar или область справа)
  - Или сделать split-view: промокоды слева, группы справа
  - Или добавить визуальные индикаторы групп прямо на карточках промокодов
- **Визуальная обратная связь**: четкие индикаторы при перетаскивании (highlight drop zones, анимации)

### 3. Функциональность
- **Управление промокодами**:
  - Просмотр всех промокодов с фильтрацией
  - Создание/редактирование/удаление через модальные окна (AJAX)
  - Быстрое переключение активности
  - Статистика по каждому промокоду
  
- **Управление группами**:
  - Просмотр групп с промокодами внутри
  - Создание/редактирование/удаление групп
  - Перетаскивание промокодов в группы и обратно
  - Настройка правил группы (one_per_account)
  
- **Статистика**:
  - Общая статистика использования
  - Статистика по группам
  - Статистика по отдельным промокодам
  - Экспорт статистики (CSV/Excel/PDF) - уже реализовано

### 4. Дополнительные функции (на усмотрение ИИ)
Можно добавить полезные функции:
- Массовые операции (активация/деактивация нескольких промокодов)
- Копирование промокодов
- Быстрое создание промокодов на основе шаблона
- Предпросмотр промокода перед сохранением
- Поиск и фильтрация по различным критериям (уже есть частично)

---

## 🛠️ ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### Используемые технологии
- **Backend**: Django (Python)
- **Frontend**: 
  - Bootstrap 5 (модальные окна)
  - Vanilla JavaScript (AJAX, drag & drop)
  - CSS с использованием дизайн-системы проекта (классы `ds-*`)
  
### Существующая инфраструктура
- Формы: `PromoCodeForm`, `PromoCodeGroupForm` в `views/promo.py`
- AJAX endpoints уже реализованы
- Drag & Drop JavaScript функции: `handlePromoDragStart`, `handlePromoDragEnd`, `handleGroupDragOver`, `handleGroupDrop`

### Что НЕ нужно менять
- **Не менять модели данных** (PromoCode, PromoCodeGroup) - они работают правильно
- **Не менять бизнес-логику** в views - логика корректна
- **Не создавать отдельный шаблон** - промокоды должны быть частью админ-панели (`admin_panel.html`)

### Что нужно исправить/улучшить
- **Исправить шаблон** `admin_promocodes.html`: 
  - CSS проблемы (z-index, layout)
  - Улучшить структуру для drag & drop
  - Исправить модальные окна (backdrop проблемы)
- **При необходимости улучшить JavaScript** для drag & drop (если нужно для лучшего UX)
- **Добавить CSS стили** для правильного отображения (в `promocodes.css`)

---

## 📐 ДЕТАЛЬНЫЕ ТРЕБОВАНИЯ К ИНТЕРФЕЙСУ

### Вкладка "Промокоды"
- Список всех промокодов в виде карточек (grid layout)
- Фильтры: все/групповые/ваучеры/обычные
- Фильтр по группам (dropdown)
- Поиск по коду
- Сортировка
- Кнопка "Создать промокод" (открывает модальное окно)
- На каждой карточке:
  - Код промокода
  - Тип и скидка
  - Статистика использования
  - Группа (если есть)
  - Кнопки: редактировать, активировать/деактивировать, удалить
  - Возможность drag & drop (draggable)

### Вкладка "Группы"
- Список групп в виде карточек
- В каждой карточке группы:
  - Название и описание
  - Количество промокодов в группе
  - Статистика использования группы
  - Кнопки: просмотреть промокоды группы, редактировать, удалить
  - **Drop zone** для перетаскивания промокодов
- Кнопка "Создать группу" (модальное окно)

### Вкладка "Статистика"
- Общая статистика (уже реализовано, проверить дизайн)
- Графики использования
- Топ промокодов и групп

### Модальные окна
- **Создание/редактирование промокода**: форма со всеми полями
- **Создание/редактирование группы**: форма с полями группы
- **Подтверждение удаления**: модальное окно с предупреждением
- Все модальные окна должны:
  - Иметь правильный z-index (не перекрывать вкладки)
  - Backdrop не должен мешать интерфейсу
  - Корректно работать на мобильных устройствах

---

## 🔬 МЕТОДОЛОГИЯ РАБОТЫ

### Обязательно использовать:

1. **Последовательное мышление (Sequential Thinking)**
   - Проанализировать текущую структуру
   - Выявить все проблемы
   - Предложить решения
   - Проверить совместимость с существующим кодом

2. **Глубокий анализ проекта**
   - Изучить все связанные файлы
   - Понять архитектуру админ-панели
   - Изучить CSS дизайн-систему проекта
   - Проверить JavaScript функции

3. **Context7 для поиска решений**
   - Использовать Context7 для поиска примеров реализации drag & drop
   - Поиск решений проблем с z-index и модальными окнами
   - Поиск лучших практик для админ-панелей промокодов
   - Ссылки на документацию Bootstrap 5 модальных окон

### Процесс работы:

1. **Исследование**
   - Изучить `admin_promocodes.html` полностью
   - Изучить `promo.py` views
   - Изучить модели в `models.py`
   - Изучить CSS файлы (`promocodes.css`, `styles.css`)
   - Изучить JavaScript в `admin_promocodes.html`

2. **Анализ проблем**
   - Найти все CSS конфликты (z-index, positioning)
   - Найти проблемы с layout
   - Определить проблемы с drag & drop UX
   - Составить список всех найденных проблем

3. **Проектирование решения**
   - Спроектировать улучшенную структуру интерфейса
   - Предложить оптимальную схему для drag & drop
   - Спроектировать исправления для модальных окон

4. **Реализация**
   - Исправить CSS (z-index, layout)
   - Улучшить HTML структуру
   - При необходимости улучшить JavaScript
   - Протестировать все функции

5. **Проверка**
   - Убедиться, что все функции работают
   - Проверить адаптивность
   - Убедиться, что ничего не сломалось в других частях админ-панели

---

## 🎨 ДИЗАЙН ТРЕБОВАНИЯ

### Цветовая схема
Использовать существующую темную тему проекта:
- Фон: темные оттенки с градиентами
- Акцентные цвета: фиолетовый (#667eea, #8b5cf6), розовый (#f093fb, #f5576c)
- Текст: светлые оттенки с правильным контрастом

### Компоненты дизайн-системы
Использовать классы из дизайн-системы:
- `ds-*` классы для компонентов
- `ds-btn`, `ds-card`, `ds-modal` и т.д.
- Существующие стили в `promocodes.css`

### Анимации
- Плавные переходы при hover
- Fade-in анимации для карточек (уже есть)
- Плавные анимации при drag & drop

---

## 📝 СПЕЦИФИЧЕСКИЕ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Z-index конфликты
Проблема: backdrop модальных окон (обычно z-index: 1040 в Bootstrap) может перекрывать элементы с меньшим z-index.

**Решение:**
- Проверить z-index всех элементов интерфейса
- Модальные окна должны иметь z-index выше, чем вкладки
- Backdrop должен быть ниже модального окна, но выше основного контента
- Использовать правильную иерархию: контент < backdrop < модальное окно

### Drag & Drop оптимизация

**Текущая реализация:**
- Промокоды на вкладке "Промокоды" (draggable)
- Группы на вкладке "Группы" (drop zones)
- Они в разных местах - неудобно

**Возможные решения:**

**Вариант 1: Split-view на вкладке промокодов**
- Левая часть: список промокодов
- Правая часть: список групп (compact view)
- Можно перетаскивать промокоды в группы справа

**Вариант 2: Визуальные индикаторы на карточках промокодов**
- На каждой карточке промокода показать, в какой группе он находится
- Добавить dropdown для быстрой смены группы
- Оставить drag & drop как дополнительную функцию

**Вариант 3: Улучшенная навигация**
- При начале drag промокода автоматически переключаться на вкладку групп
- Или показывать группы в sidebar во время drag

**Рекомендация:** Реализовать вариант, который будет наиболее удобным для администратора. Возможно, комбинацию вариантов.

### Layout проблемы

**Проблемы:**
- Элементы "подвинуты" на бок
- Неправильное применение auto-layout

**Решение:**
- Проверить все flexbox/grid контейнеры
- Убедиться, что width/max-width установлены правильно
- Проверить margin/padding всех элементов
- Использовать правильные breakpoints для адаптивности

---

## 🔗 ССЫЛКИ НА КОНТЕКСТ ПРОЕКТА

### Ключевые файлы для изучения:

1. **Шаблон:**
   ```
   twocomms/twocomms_django_theme/templates/pages/admin_promocodes.html
   ```
   - Строки 1-400: структура HTML, вкладки
   - Строки 636-1007: модальные окна
   - Строки 1049-2100: JavaScript (AJAX, drag & drop)

2. **Views:**
   ```
   twocomms/storefront/views/promo.py
   ```
   - Строки 152-318: функция `get_promo_admin_context` - контекст для шаблона
   - Строки 324-432: views для промокодов (create, edit, delete, toggle)
   - Строки 437-525: views для групп
   - Строки 867-914: AJAX endpoint для drag & drop (`admin_promocode_change_group`)

3. **Модели:**
   ```
   twocomms/storefront/models.py
   ```
   - Строки 431-450: `PromoCodeGroup`
   - Строки 452-600+: `PromoCode` (методы `can_be_used_by_user`, `record_usage`)

4. **URLs:**
   ```
   twocomms/storefront/urls.py
   ```
   - Строки 81-100: все маршруты для промокодов

5. **CSS:**
   ```
   twocomms/twocomms_django_theme/static/css/promocodes.css
   twocomms/twocomms_django_theme/static/css/styles.css (секция промокодов)
   ```

6. **Интеграция в админ-панель:**
   ```
   twocomms/twocomms_django_theme/templates/pages/admin_panel.html
   ```
   - Строка ~1635: `{% elif section == 'promocodes' %}`
   - Строка ~1637: `{% include 'pages/admin_promocodes.html' %}`

---

## 🚀 КРИТЕРИИ УСПЕХА

### Обязательные требования:
✅ Все CSS проблемы исправлены (z-index, layout)
✅ Модальные окна работают корректно, не перекрывают интерфейс
✅ Drag & Drop работает удобно и интуитивно
✅ Все функции работают (создание, редактирование, удаление, активация)
✅ Интерфейс выглядит красиво и профессионально
✅ Адаптивный дизайн работает на всех устройствах
✅ Статистика отображается корректно
✅ Все элементы находятся в правильных местах (auto-layout)

### Дополнительные улучшения (желательно):
⭐ Улучшенный UX для drag & drop
⭐ Дополнительные функции управления (массовые операции)
⭐ Улучшенная визуализация статистики
⭐ Оптимизация производительности

---

## 📌 ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Не ломать существующий функционал**
   - Все существующие функции должны продолжать работать
   - AJAX endpoints не должны быть изменены (только при необходимости)
   - Модели данных не изменять

2. **Совместимость**
   - Интерфейс должен работать в существующей админ-панели
   - Не создавать отдельные страницы
   - Использовать существующую дизайн-систему

3. **Производительность**
   - Минимизировать количество DOM операций
   - Оптимизировать CSS (избегать тяжелых селекторов)
   - Использовать event delegation для динамических элементов

4. **Доступность**
   - Поддержка клавиатурной навигации
   - Правильные ARIA атрибуты
   - Семантичный HTML

---

## 🎯 ФИНАЛЬНЫЙ РЕЗУЛЬТАТ

После выполнения задачи должен получиться:

1. **Красивый современный интерфейс** для управления промокодами
2. **Полностью функциональный** drag & drop для перетаскивания промокодов в группы
3. **Удобное управление** всеми функциями промокодов через понятный интерфейс
4. **Корректная работа** всех модальных окон без конфликтов z-index
5. **Правильный auto-layout**, где все элементы находятся в правильных местах
6. **Профессиональный дизайн**, соответствующий дизайн-системе проекта

---

## 💡 ИНСТРУКЦИИ ПО ИСПОЛЬЗОВАНИЮ Context7

Используй Context7 для поиска:
- "Bootstrap 5 modal z-index backdrop issues" - для решения проблем с модальными окнами
- "HTML5 drag and drop between different sections" - для улучшения drag & drop
- "CSS grid flexbox layout issues fixed positioning" - для решения layout проблем
- "Django admin panel best practices UI UX" - для лучших практик админ-панелей

---

## 🔐 SSH ПОДКЛЮЧЕНИЕ (если необходимо)

Если нужно проверить на сервере:
```bash
sshpass -p 'trs5m4t1' ssh -o StrictHostKeyChecking=no qlknpodo@195.191.24.169 "bash -lc 'source /home/qlknpodo/virtualenv/TWC/TwoComms_Site/twocomms/3.13/bin/activate && cd /home/qlknpodo/TWC/TwoComms_Site/twocomms && git pull'"
```

---

## 📞 ЧТО НУЖНО СДЕЛАТЬ

**Краткое резюме задачи:**

1. Изучи текущую структуру админ-панели промокодов
2. Найди все проблемы (CSS, layout, drag & drop UX)
3. Исправь все проблемы с дизайном и функциональностью
4. Сделай интерфейс красивым, удобным и функциональным
5. Убедись, что все работает корректно

**Главное:** Сделай интерфейс **идеальным** для управления промокодами. Администратор должен иметь возможность легко и удобно управлять промокодами, группами, просматривать статистику и перетаскивать промокоды в группы без проблем.

---

**Удачи! Сделай это как можно лучше! 🚀**














