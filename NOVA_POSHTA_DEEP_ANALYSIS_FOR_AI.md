# 🔍 Глубочайший анализ интеграции Nova Poshta API - Документация для AI

**Дата создания:** 2025-01-30  
**Версия:** 1.0  
**Цель:** Максимально подробная документация для другого AI о текущей реализации интеграции с Nova Poshta API

---

## 📋 Содержание

1. [Источники информации](#источники-информации)
2. [Структура проекта](#структура-проекта)
3. [Текущая реализация](#текущая-реализация)
4. [Анализ кода](#анализ-кода)
5. [Проблемы и их причины](#проблемы-и-их-причины)
6. [Документация API Nova Poshta](#документация-api-nova-poshta)
7. [Рекомендации по исправлению](#рекомендации-по-исправлению)

---

## 🔗 Источники информации

### 1. Официальная документация API
- **URL:** https://api.novapost.com/developers/index.html#overview
- **Версия API:** v2.0
- **Endpoint:** `https://api.novaposhta.ua/v2.0/json/`
- **Метод:** POST с JSON payload

### 2. Веб-поиск и анализ
Информация получена из:
- Официальной документации Nova Poshta API
- Статей об интеграции с Nova Poshta
- Документации библиотеки `requests` (Python)
- Лучших практик работы с API

### 3. Анализ кода проекта
Файлы проекта:
- `twocomms/orders/nova_poshta_service.py` - основной сервис
- `twocomms/orders/management/commands/update_tracking_statuses.py` - команда обновления
- `twocomms/orders/models.py` - модели Order и DropshipperOrder
- `twocomms/twocomms/settings.py` - настройки API ключа
- `setup_nova_poshta_cron.sh` - скрипт настройки cron

### 4. Context7 проверка
- Библиотека `requests` (/psf/requests) - проверена документация по обработке ошибок
- Лучшие практики логирования и обработки исключений

---

## 📁 Структура проекта

### Основные компоненты:

```
twocomms/
├── orders/
│   ├── nova_poshta_service.py          # Основной сервис работы с API
│   ├── models.py                       # Модели Order и DropshipperOrder
│   ├── management/
│   │   └── commands/
│   │       └── update_tracking_statuses.py  # Команда для обновления статусов
│   ├── telegram_notifications.py       # Уведомления в Telegram
│   └── facebook_conversions_service.py # Интеграция с Facebook Pixel
├── twocomms/
│   └── settings.py                     # Настройки (NOVA_POSHTA_API_KEY)
└── setup_nova_poshta_cron.sh           # Скрипт настройки cron
```

### Модели данных:

**Order (twocomms/orders/models.py:7-56)**
- `tracking_number` (CharField, max_length=50) - номер ТТН
- `shipment_status` (CharField, max_length=100) - статус посылки
- `shipment_status_updated` (DateTimeField) - время последнего обновления
- `status` (CharField) - статус заказа ('new', 'prep', 'ship', 'done', 'cancelled')
- `payment_status` (CharField) - статус оплаты

**DropshipperOrder (twocomms/orders/models.py:320-543)**
- Аналогичные поля `tracking_number`, `shipment_status`
- Метод `check_np_status_and_update()` - использует несуществующий метод `track_parcel()`

---

## 💻 Текущая реализация

### 1. Основной сервис (nova_poshta_service.py)

#### Класс NovaPoshtaService

**Инициализация:**
```python
API_URL = "https://api.novaposhta.ua/v2.0/json/"
api_key = getattr(settings, 'NOVA_POSHTA_API_KEY', '')
```

**Метод get_tracking_info() (строки 24-63):**
- Получает статус посылки по ТТН
- Использует метод API: `TrackingDocument.getStatusDocuments`
- Формат запроса:
```json
{
  "apiKey": "...",
  "modelName": "TrackingDocument",
  "calledMethod": "getStatusDocuments",
  "methodProperties": {
    "Documents": [
      {
        "DocumentNumber": "TTN_NUMBER",
        "Phone": ""
      }
    ]
  }
}
```

**Проблемы:**
1. ❌ Нет обработки поля `errors` в ответе API
2. ❌ Нет логирования структуры ответа
3. ❌ Нет проверки типа данных `data` (может быть не массив)
4. ❌ Использует `print()` вместо логирования
5. ❌ Нет проверки на пустой ответ (`data` может быть `[]`)

**Метод update_order_tracking_status() (строки 65-119):**
- Обновляет статус посылки для заказа
- Вызывает `_update_order_status_if_delivered()` для автоматического изменения статуса

**Проблемы:**
1. ❌ Сравнение статусов без нормализации (пробелы, регистр)
2. ❌ Не передает `StatusCode` в метод определения доставки
3. ❌ Нет логирования изменений

**Метод _update_order_status_if_delivered() (строки 121-174):**
- Определяет, получена ли посылка
- Меняет статус заказа на 'done'
- Меняет payment_status на 'paid'
- Отправляет Purchase событие в Facebook

**Проблемы:**
1. ❌ НЕ использует `StatusCode` из API (StatusCode 9 = получено)
2. ❌ Только проверка по ключевым словам в тексте
3. ❌ Может не распознать статус, если текст отличается
4. ❌ Нет логирования, почему статус не распознан

### 2. Команда обновления (update_tracking_statuses.py)

**Функционал:**
- Обновляет все заказы с ТТН
- Поддерживает `--order-number` для конкретного заказа
- Поддерживает `--dry-run` для тестирования

**Проблемы:**
1. ⚠️ Нет обработки исключений при обновлении множества заказов
2. ⚠️ Нет логирования в файл (только stdout)
3. ⚠️ Нет rate limiting для API запросов

### 3. Cron job

**Настройка:**
- Запускается каждые 5 минут: `*/5 * * * *`
- Логирование: `/home/qlknpodo/TWC/TwoComms_Site/twocomms/logs/nova_poshta_cron.log`

**Проблемы:**
1. ⚠️ Нет проверки, что cron действительно запускается
2. ⚠️ Нет мониторинга ошибок
3. ⚠️ Нет уведомлений при критических ошибках

---

## 🔍 Анализ кода

### Проблема 1: Неполная обработка ответа API

**Текущий код (nova_poshta_service.py:55-58):**
```python
data = response.json()

if data.get('success') and data.get('data'):
    return data['data'][0] if data['data'] else None
```

**Что не обрабатывается:**
1. Поле `errors` в ответе API - если API возвращает ошибку, она игнорируется
2. Поле `warnings` - предупреждения не учитываются
3. Структура `data` - может быть не массив, а объект
4. Пустой массив `data` - возвращает `None`, но не логирует почему

**Правильная структура ответа API:**
```json
{
  "success": true,
  "data": [
    {
      "Number": "TTN_NUMBER",
      "Status": "Отримано одержувачем",
      "StatusCode": 9,
      "StatusDescription": "Отримано одержувачем",
      "DateCreated": "2025-01-30 12:00:00",
      // ... другие поля
    }
  ],
  "errors": [],
  "warnings": [],
  "info": {}
}
```

### Проблема 2: Неиспользование StatusCode

**Текущий код (nova_poshta_service.py:121-148):**
```python
def _update_order_status_if_delivered(self, order, status, status_description):
    delivered_keywords = ['отримано', 'получено', ...]
    is_delivered = any(keyword in status_lower or keyword in description_lower 
                      for keyword in delivered_keywords)
```

**Проблема:**
- Метод НЕ получает `StatusCode` из `tracking_info`
- Определение "получено" только по тексту, который может отличаться
- StatusCode 9 = "Получено получателем" - это надежный индикатор

**Правильный подход:**
```python
def _update_order_status_if_delivered(self, order, status, status_description, status_code=None):
    # Проверка по коду статуса (надежнее)
    is_delivered_by_code = status_code == 9
    
    # Проверка по ключевым словам (резервный вариант)
    is_delivered_by_keywords = any(...)
    
    is_delivered = is_delivered_by_code or is_delivered_by_keywords
```

### Проблема 3: Отсутствие логирования

**Текущий код:**
- Использует `print()` вместо логирования
- Нет структурированных логов
- Нет уровней логирования (DEBUG, INFO, WARNING, ERROR)

**Что нужно:**
```python
import logging
logger = logging.getLogger(__name__)

logger.debug(f"API response: {data}")
logger.info(f"Order {order.order_number}: status updated")
logger.warning(f"API returned errors: {data.get('errors')}")
logger.error(f"Failed to get tracking info: {e}")
```

### Проблема 4: Несуществующий метод в DropshipperOrder

**Код в models.py:511:**
```python
status_info = np_service.track_parcel(self.tracking_number)
```

**Проблема:**
- Метод `track_parcel()` НЕ существует в `NovaPoshtaService`
- Правильный метод: `get_tracking_info()`

**Исправление:**
```python
status_info = np_service.get_tracking_info(self.tracking_number)
```

### Проблема 5: Неполная обработка исключений

**Текущий код:**
```python
except Exception as e:
    print(f"Ошибка при получении статуса посылки {ttn_number}: {e}")
    return None
```

**Проблемы:**
- Все исключения обрабатываются одинаково
- Нет различия между сетевой ошибкой, ошибкой API, ошибкой парсинга
- Нет логирования типа ошибки

**Правильный подход:**
```python
except requests.exceptions.RequestException as e:
    logger.error(f"Network error: {e}")
    return None
except ValueError as e:
    logger.error(f"JSON parsing error: {e}")
    return None
except Exception as e:
    logger.exception(f"Unexpected error: {e}")
    return None
```

---

## 🐛 Проблемы и их причины

### Почему посылки не обновляются на сайте?

#### Причина 1: API ключ истек
- **Факт:** API ключи Nova Poshta имеют срок действия 3 месяца
- **Симптомы:** Все запросы возвращают ошибку
- **Проверка:** Проверить логи на ошибки авторизации

#### Причина 2: Статус не распознается как "получено"
- **Факт:** Метод `_update_order_status_if_delivered()` не использует `StatusCode`
- **Симптомы:** Статус посылки обновляется, но статус заказа не меняется
- **Проверка:** Логировать все статусы, которые не распознаны

#### Причина 3: Ошибки API не обрабатываются
- **Факт:** Метод `get_tracking_info()` не проверяет поле `errors`
- **Симптомы:** API возвращает ошибку, но код считает что все ОК
- **Проверка:** Логировать полный ответ API

#### Причина 4: Cron не запускается
- **Факт:** Cron job может не работать из-за проблем с правами, путями, переменными окружения
- **Симптомы:** Статусы не обновляются вообще
- **Проверка:** Проверить логи cron, проверить что задача в crontab

#### Причина 5: Формат ответа API изменился
- **Факт:** API может вернуть данные в другом формате
- **Симптомы:** Код падает с ошибкой или возвращает None
- **Проверка:** Логировать структуру ответа API

---

## 📚 Документация API Nova Poshta

### Метод: getStatusDocuments

**Endpoint:** `https://api.novaposhta.ua/v2.0/json/`

**Запрос:**
```json
{
  "apiKey": "YOUR_API_KEY",
  "modelName": "TrackingDocument",
  "calledMethod": "getStatusDocuments",
  "methodProperties": {
    "Documents": [
      {
        "DocumentNumber": "20450012345678",
        "Phone": "+380501234567"
      }
    ]
  }
}
```

**Ответ (успех):**
```json
{
  "success": true,
  "data": [
    {
      "Number": "20450012345678",
      "Status": "Отримано одержувачем",
      "StatusCode": 9,
      "StatusDescription": "Отримано одержувачем",
      "DateCreated": "2025-01-30 12:00:00",
      "DateLastMovementStatus": "2025-01-30 14:30:00",
      "RecipientFullName": "Іван Петренко",
      "RecipientPhone": "+380501234567",
      "CityRecipient": "Київ",
      "WarehouseRecipient": "Відділення №1",
      // ... другие поля
    }
  ],
  "errors": [],
  "warnings": [],
  "info": {}
}
```

**Ответ (ошибка):**
```json
{
  "success": false,
  "data": [],
  "errors": [
    "Невірний номер ТТН",
    "ТТН не знайдено"
  ],
  "warnings": [],
  "info": {}
}
```

### Статусы и их коды:

| StatusCode | Статус | Описание |
|------------|--------|----------|
| 1 | Прийнято | Посылка принята в отделении |
| 2 | Відправлено | Посылка отправлена |
| 3 | Прибуло в місто | Посылка прибыла в город получателя |
| 4 | Прибуло в відділення | Посылка прибыла в отделение |
| 5 | Отримано | Посылка получена (старый формат) |
| 6 | Відмова | Отказ от получения |
| 7 | Відправлено | Отправлено (дубликат?) |
| 8 | Невідомо | Неизвестный статус |
| **9** | **Отримано одержувачем** | **Получено получателем** |
| 10 | Повернено відправнику | Возвращено отправителю |
| 11 | Відмова | Отказ от получения |

**КРИТИЧЕСКИ ВАЖНО:** StatusCode 9 = "Получено получателем" - это единственный надежный способ определить, что посылка получена.

---

## 🔧 Рекомендации по исправлению

### 1. Исправить обработку ответа API

```python
def get_tracking_info(self, ttn_number, phone=None):
    # ... код запроса ...
    
    data = response.json()
    
    # Проверяем ошибки API
    if data.get('errors') and len(data.get('errors', [])) > 0:
        error_msg = ', '.join(data.get('errors', []))
        logger.error(f"API errors for TTN {ttn_number}: {error_msg}")
        return None
    
    # Проверяем успешность
    if not data.get('success'):
        logger.warning(f"API returned success=false for TTN {ttn_number}")
        return None
    
    # Проверяем наличие данных
    if not data.get('data'):
        logger.warning(f"No data in API response for TTN {ttn_number}")
        return None
    
    # Обрабатываем массив или объект
    if isinstance(data['data'], list):
        if len(data['data']) == 0:
            logger.warning(f"Empty data array for TTN {ttn_number}")
            return None
        tracking_data = data['data'][0]
    else:
        tracking_data = data['data']
    
    # Логируем структуру для отладки
    logger.debug(f"Tracking info: Status={tracking_data.get('Status')}, "
                f"StatusCode={tracking_data.get('StatusCode')}")
    
    return tracking_data
```

### 2. Использовать StatusCode для определения доставки

```python
def _update_order_status_if_delivered(self, order, status, status_description, status_code=None):
    # Проверка по коду статуса (надежнее текста)
    is_delivered_by_code = status_code == 9
    
    # Проверка по ключевым словам (резервный вариант)
    delivered_keywords = ['отримано', 'получено', 'доставлено', 'вручено', 
                          'отримано отримувачем', 'получено получателем']
    status_lower = (status or '').lower().strip()
    description_lower = (status_description or '').lower().strip()
    is_delivered_by_keywords = any(
        keyword in status_lower or keyword in description_lower
        for keyword in delivered_keywords
    )
    
    is_delivered = is_delivered_by_code or is_delivered_by_keywords
    
    # Логируем проверку
    logger.debug(f"Order {order.order_number}: StatusCode={status_code}, "
                f"is_delivered_by_code={is_delivered_by_code}, "
                f"is_delivered_by_keywords={is_delivered_by_keywords}")
    
    if is_delivered and order.status != 'done':
        # ... код обновления статуса ...
```

### 3. Добавить логирование

```python
import logging

logger = logging.getLogger(__name__)

# В методе get_tracking_info:
logger.debug(f"Requesting Nova Poshta API for TTN: {ttn_number}")
logger.debug(f"API response: {data}")

# В методе update_order_tracking_status:
logger.info(f"Order {order.order_number}: shipment_status changed from '{old_status}' to '{full_status}'")

# В методе _update_order_status_if_delivered:
logger.info(f"✅ Order {order.order_number}: status changed to 'done' (StatusCode={status_code})")
```

### 4. Исправить вызов в DropshipperOrder

```python
# В models.py, метод check_np_status_and_update:
status_info = np_service.get_tracking_info(self.tracking_number)  # Было: track_parcel()
```

### 5. Улучшить обработку исключений

```python
try:
    response = requests.post(self.API_URL, json=payload, timeout=10)
    response.raise_for_status()
    data = response.json()
except requests.exceptions.Timeout:
    logger.error(f"Timeout getting status for TTN {ttn_number}")
    return None
except requests.exceptions.RequestException as e:
    logger.error(f"Network error getting status for TTN {ttn_number}: {e}")
    return None
except ValueError as e:
    logger.error(f"JSON parsing error for TTN {ttn_number}: {e}")
    return None
except Exception as e:
    logger.exception(f"Unexpected error getting status for TTN {ttn_number}: {e}")
    return None
```

---

## 📊 Структура данных

### Модель Order:
- `tracking_number` - номер ТТН (может быть пустым)
- `shipment_status` - статус посылки (текст из API)
- `shipment_status_updated` - время последнего обновления
- `status` - статус заказа ('new', 'prep', 'ship', 'done', 'cancelled')
- `payment_status` - статус оплаты

### Поток данных:

```
Cron (каждые 5 мин)
  ↓
update_tracking_statuses.py
  ↓
NovaPoshtaService.update_all_tracking_statuses()
  ↓
Для каждого заказа с ТТН:
  ↓
get_tracking_info(ttn_number)
  ↓
API Nova Poshta
  ↓
Получение ответа
  ↓
update_order_tracking_status(order)
  ↓
Обновление shipment_status
  ↓
_update_order_status_if_delivered()
  ↓
Если получено:
  - status = 'done'
  - payment_status = 'paid'
  - Facebook Purchase event
  - Telegram уведомления
```

---

## ⚠️ Критические моменты

1. **API ключ:** Срок действия 3 месяца, нужно регулярно обновлять
2. **StatusCode 9:** Единственный надежный индикатор "получено"
3. **Обработка ошибок:** API может вернуть ошибку в поле `errors`, нужно проверять
4. **Логирование:** Без логирования невозможно отладить проблемы
5. **Телефон:** Передача телефона в запрос улучшает точность поиска

---

## 🎯 Итоговая оценка

**Что работает:**
- ✅ Базовый запрос к API
- ✅ Обновление статуса посылки
- ✅ Автоматическое изменение статуса заказа (частично)
- ✅ Интеграция с Telegram и Facebook

**Что не работает или работает плохо:**
- ❌ Определение "получено" только по тексту (не использует StatusCode)
- ❌ Нет обработки ошибок API
- ❌ Нет логирования для отладки
- ❌ Несуществующий метод `track_parcel()` в DropshipperOrder
- ❌ Неполная обработка исключений

**Приоритет исправлений:**
1. 🔴 КРИТИЧНО: Добавить использование StatusCode для определения доставки
2. 🔴 КРИТИЧНО: Добавить обработку ошибок API (поле `errors`)
3. 🟡 ВАЖНО: Добавить логирование
4. 🟡 ВАЖНО: Исправить вызов `track_parcel()` на `get_tracking_info()`
5. 🟢 ЖЕЛАТЕЛЬНО: Улучшить обработку исключений

---

**Конец документации**














