# Рабочий процесс TwoComms

## Деплой на продакшен (важно)

Деплой выполняется через одну SSH-команду, которую пользователь даёт
в сообщении. **Команда содержит секрет (пароль) и не должна попадать
в git, в коммиты, в файлы репозитория, в скрипты, в логи и в публичные
ответы.** Используй её только при выполнении задачи деплоя через
`execute_bash` без сохранения в файлы.

Шаблон команды (без секрета):

- `ssh` под пользователем `qlknpodo@195.191.24.169` с `StrictHostKeyChecking=no`,
- внутри `bash -lc`: активировать venv в
  `/home/qlknpodo/virtualenv/TWC/TwoComms_Site/twocomms/3.14`,
- `cd /home/qlknpodo/TWC/TwoComms_Site/twocomms`,
- `git pull`, при необходимости `python manage.py migrate --no-input`,
  `python manage.py collectstatic --no-input`, и `touch tmp/restart.txt`
  для рестарта Passenger.

Правила:

- Полный формат команды (включая пароль) пользователь даёт в сообщении.
  Бери его оттуда — НЕ хардкодь и НЕ сохраняй в `.md`, `.sh`, `.py` и т.п.
- НИКОГДА не добавляй пароль в:
  - содержимое любых файлов в репозитории;
  - тексты коммитов или PR;
  - примеры команд в документации;
  - содержимое тестов.
- Если нужно сослаться на команду в документах — описывай шаги
  словами, без подстановки реального пароля и без шаблона
  `ssh<пробел>...<пароль>`.
- Перед деплоем: убедись, что коммит и `git push` выполнены. После
  деплоя — кратко отчитайся, что прошло (pull, миграции, рестарт).

### Чек-лист деплоя

После `git pull` на сервере выполняй шаги именно в таком порядке. Шаги
1, 4 и 6 — обязательные всегда; остальные — по флагам ниже. Все шаги
в одной SSH-сессии под активированным venv.

1. **`git pull`** (или `git fetch && git reset --hard origin/main` если
   на сервере накопились конфликты).
2. **`python manage.py migrate --no-input`** — если коммит трогает
   `models.py` или содержит файлы в `*/migrations/`.
3. **`python manage.py collectstatic --no-input`** — если коммит
   меняет файлы в `static/` (CSS, JS, изображения, шрифты, manifest)
   или добавляет новые. Это перезаписывает `staticfiles/staticfiles.json`,
   из которого `ManifestStaticFilesStorage` читает хеш-маппинг.
   Если этот шаг пропустить, любая ссылка на новый ассет через
   `{% static %}` валит весь рендер с
   `ValueError: Missing staticfiles manifest entry for '...'`
   и сайт уходит в 500.
4. **`python manage.py compress --force`** — если коммит трогает CSS/JS,
   которые попадают внутрь `{% compress %}` блоков (по умолчанию это
   `base.html` и шаблоны, что наследуют его). Без него django-compressor
   валит рендер с
   `OfflineGenerationError: ... key "..." is missing from offline manifest`.
   Делать всегда после `collectstatic`, потому что compressor читает
   уже хешированные пути.
5. **Telegram / cron перезапуски** — если правил `accounts/telegram_bot.py`,
   `cron/*.sh` и т.п. (специфика конкретной фичи).
6. **`touch tmp/restart.txt`** — рестарт Passenger. Делать **последним**:
   процесс перечитает `staticfiles.json` и compressor manifest при
   следующем запросе.

После рестарта ждать ~5–10 секунд и делать sanity-curl минимум на
`/`, `/admin-panel/`, `/product/<slug>/` (хотя бы один товар) —
ожидать 200 (или 302 для админки, если без авторизации).

### Когда какой шаг нужен (быстрый матчер)

- Изменён `*.py` в `models.py` или `migrations/` → нужен шаг 2.
- Добавлены/удалены файлы в `static/` или `twocomms_django_theme/static/` → нужен шаг 3.
- Изменены CSS/JS, на которые ссылаются шаблоны → нужны шаги 3 и 4.
- Добавлены новые шаблоны с `{% static %}` на новые файлы → нужны 3 и 4.
- Изменены только `views/`, `services/`, `templates/` без новых
  ассетов → нужен только 1 и 6.
- Если при ошибке 500 в логе Django видно `ManifestStaticFilesStorage`
  / `OfflineGenerationError` — это значит шаг 3 или 4 был пропущен.
  Откати в обратном порядке: `compress --force` → `collectstatic --no-input`
  → `touch restart.txt`.

## Защита от циклов и тупиков

Если при выполнении задачи я ловлю одну и ту же ошибку дважды подряд:

1. Не повторять тот же подход в третий раз.
2. **Делать шаг назад** и проверить базовые предположения:
   - В какой директории сейчас выполняется команда (`cwd`).
   - Какая команда реально исполнилась — учесть, что shell остаётся в
     директории предыдущего вызова (если использовать `cd && ...` внутри
     команды), а не там, где запущен `execute_bash`.
   - Использовать `cwd` параметр инструмента вместо `cd` внутри команды,
     чтобы директория не "залипала" и я её видел в выводе.
3. Если упёрся в инфраструктурный нюанс (например, `npm install` сработал,
   но `node_modules` не там, где ищу) — переключаюсь на альтернативу:
   - редактирование исходных CSS/HTML без билда,
   - билд CSS прямо на сервере во время деплоя,
   - запросить у пользователя путь / факт.
4. **Никогда не задавать пользователю вопрос "отменить или нет?"** —
   принимать решение самостоятельно, делая безопасный безопасный
   шаг и сообщая, что именно я попробовал по-другому.

## Изменения CSS

Источник истины — `twocomms/twocomms_django_theme/static/css/styles.css`.
Файлы `styles.purged.css`, `styles.min.css`, `styles.direct.css` —
производные:

- `styles.purged.css` собирается локальным `npm run build:css`
  (postcss + purgecss) на основе `styles.css`. Если node-окружения нет
  локально, можно собрать на сервере во время деплоя
  (`npm install --omit=dev && npm run build:css`) или править
  `styles.purged.css` вручную аналогичным блоком в конце файла,
  потому что он используется в продакшене как основной.
- `styles.min.css` и `styles.direct.css` не используются в шаблоне
  напрямую (см. `templates/base.html`), их можно не трогать в текущих
  фичах, но безопаснее синхронизировать.

При добавлении новой стилистики:

1. Дописать блок в конец `styles.css` под понятным заголовком
   `/* === <Тема> === */`.
2. Добавить тот же блок в конец `styles.purged.css` (purgecss не
   будет вырезать классы, которые присутствуют в шаблонах).
3. Если правило ссылается на классы, которые мы только что
   добавили в HTML/JS, проверить, что эти классы используются
   в шаблонах — иначе purgecss их выкинет при следующем билде.

## Структура работы с проектом

- Корень репозитория: `/Users/zainllw0w/TwoComms/site`.
- Django-проект: `/Users/zainllw0w/TwoComms/site/twocomms`.
- Запускать `manage.py` из `twocomms/` с переменной окружения
  `SECRET_KEY=test_local_secret`, если нет `.env`.
- Тесты: `python manage.py test storefront.tests.<имя>` —
  есть пред-существующие 301-редиректы из-за i18n+SECURE_SSL_REDIRECT,
  это не баг моих правок, см. историю фейлов.

## Git

- Коммитить только то, что нужно — никаких лишних артефактов
  (`_seo_check.py`, `tmp/*`, и т.п. — оставлять untracked).
- Сообщение коммита: первая строка короткая, остальные —
  тело с описанием по пунктам.
- Перед `git push` всегда `git status` для проверки.
